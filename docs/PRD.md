# 产品需求文档（PRD）：轻量级单用户视频媒体服务器

> 需求的单一真源（WHAT / WHY），也是产品的**需求登记册 + 路线图**——全生命周期都在记，不是一次性文档。每个需求在 §4 加一行 FR（带优先级/期 + 状态），交付即标版本；§4 FR 表是迭代中最常动的部分（🔥 高频），分期（§7）只是其中很小、很静的一页粗线条规划。单功能的详细规格放 `docs/specs/`，PRD 只保留"一行 FR + 期 + 状态"的索引级。

## 1. 背景与目标

构建一款轻量级、单用户的私有视频媒体服务器。后端采用 Go 语言开发，前端使用 React + TypeScript 构建，前端编译产物通过 `go:embed` 完全内嵌于 Go 二进制文件中，实现单文件无额外依赖的便捷部署。系统核心定位为"个人家庭影音中心"，聚焦于高兼容性播放、极致的硬件转码性能，以及 Web 端对动态 TS 流的完美追随播放。

### 非目标

- 不做多用户权限管理与认证体系（单用户模式）
- 不做移动端原生 App（PWA 或后续阶段考虑）
- 不做在线流媒体订阅或分享功能
- 不做分布式集群部署
- 不做实时弹幕、评论等社交功能

## 2. 角色

- **家庭用户**：通过 Web 界面浏览、播放本地或 NAS 上的视频资源，无需关心底层格式差异。

## 3. 用户故事

- 作为家庭用户，我希望将多个硬盘/目录的视频和图片统一汇聚到一个媒体库中，以便集中浏览和管理。
- 作为家庭用户，我希望直接添加 SMB 网络共享路径作为媒体库源，以便播放 NAS 上的视频而无需手动挂载。
- 作为家庭用户，我希望新增媒体文件后媒体库自动更新，以便省去手动扫描的等待。
- 作为家庭用户，我希望能为单个媒体库目录追加自定义图片/视频后缀，以便索引特殊格式且不影响其他目录。
- 作为家庭用户，我希望所有视频格式都能直接播放，以便不用关心文件格式差异。
- 作为家庭用户，我希望在网络波动时视频自动切换码率不卡顿，以便获得流畅的观看体验。
- 作为家庭用户，我希望拖动进度条后画面立即响应，以便快速定位想看的内容。
- 作为家庭用户，我希望在转码进行中就能开始边下边播，以便不用等整个文件转完。
- 作为家庭用户，我希望通过硬件加速降低转码 CPU 负载，以便在低配设备上也能流畅转码。

## 4. 功能需求（FR）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-01 | 多目录聚合管理：支持添加多个本地根目录，将分散的视频/图片汇聚到一个媒体库，并支持按目录绑定自定义媒体后缀 | P1 | 已交付@v0.2.0 |
| FR-02 | SMB 协议支持：原生连接 Windows SMB（CIFS）网络共享，支持添加远程网络路径作为媒体库源 | P2 | 已交付@v0.6.2（修复拨号缺省端口 445；真机本地 SMB 共享端到端验证：连接/扫描入库 smb:// 文件/错误凭据优雅失败） |
| FR-03 | 实时路径监控：利用 fsnotify/inotify 监听所有目录（含本地及 SMB），文件新增/删除/移动时自动增量更新媒体库 | P1 | 已交付@v0.2.0 |
| FR-04 | 全格式兼容播放：集成 FFmpeg 解析所有主流视频格式（MP4/MKV/AVI/MOV/WebM/RMVB/TS/HEVC/AV1 等）及外挂字幕（SRT/ASS/SUP 等） | P1 | 已交付@v0.2.0 |
| FR-05 | 智能播放策略：浏览器兼容格式（H.264+AAC 的 MP4）直出播放，不兼容格式无缝切入转码模式 | P1 | 已交付@v0.2.0 |
| FR-06 | TS 流（MPEG-TS）强制输出：转码输出固定采用 HLS（m3u8+ts）切片流格式 | P1 | 已交付@v0.2.0 |
| FR-07 | 自适应码率（ABR）：提供多码率阶梯转换，根据客户端带宽动态切换（1080p→720p→480p） | P2 | 已交付@v0.3.0 |
| FR-08 | 后端流式输出：FFmpeg 转码数据通过 HTTP Response Writer 实时刷新给客户端，禁止等待整个文件转完再返回 | P1 | 已交付@v0.2.0 |
| FR-09 | 并发 Range 请求支持：Seek 时后端能中断旧连接并迅速开启新 Range 请求线程 | P1 | 已交付@v0.2.0 |
| FR-10 | Intel 硬件加速转码：支持 Intel QSV 和 VAAPI | P1 | 开发中（检测/编码器查找 + 单测就绪；验收机无 Intel 核显，硬件真机待验） |
| FR-11 | NVIDIA 硬件加速转码：支持 NVIDIA NVENC/NVDEC | P1 | 开发中（检测/编码器查找 + 单测就绪；验收机无 NVIDIA GPU，硬件真机待验） |
| FR-12 | 自动降级机制：优先检测硬件加速能力，不支持时自动降级为软件解码 | P1 | 已交付@v0.2.0 |
| FR-13 | 单用户认证：极简账号+密码登录体系 | P1 | 已交付@v0.4.0 |

> Bug 修复记录：
> - 刷新页面退出登录 → `auth.ts` `init()` 未被调用，store 状态不持久化。修复：`AppLayout` 挂载时调用 `init()` 从 localStorage 恢复。
> - 手机端导航不可用 → `AppShell` 侧边栏在移动端自动隐藏但无汉堡菜单。修复：Header 添加 `Burger` 按钮控制抽屉。
| FR-14 | 时间轴视图：按媒体文件添加时间或修改时间倒序排列的"信息流"浏览模式 | P1 | 已交付@v0.2.0 |
| FR-15 | 文件目录视图：传统文件资源管理器风格，展示完整文件夹层级结构 | P1 | 已交付@v0.2.0 |
| FR-16 | mpegts.js 播放内核：前端强制使用 mpegts.js 操作 MSE API 进行 TS 流播放（不走 TS 的直出场景允许原生 video） | P1 | 已交付@v0.2.0 |
| FR-17 | "边下边播"与实时追随：转码或文件持续写入时，播放器自动追播新数据，延迟控制在 3-5 秒内 | P1 | 已交付@v0.2.0 |
| FR-18 | 播放末端缓冲等待：播放追上文件写入末端时自动进入缓冲等待，新数据写入时自动恢复 | P1 | 已交付@v0.2.0 |
| FR-19 | 精准进度拖拽：后端全面支持 HTTP Range，配合固定 GOP 实现毫秒级 Seek 响应 | P1 | 已交付@v0.2.0 |
| FR-20 | 双进度条视觉反馈：显示当前播放进度和缓冲区加载进度 | P2 | 已交付@v0.3.0 |
| FR-21 | 系统诊断页：展示系统信息（OS/架构/CPU 数/主机名/Go 版本/应用版本/ffmpeg 版本）与编解码器实测结果（软件 + QSV/VAAPI/NVENC/AMF/VideoToolbox 的 H.264/H.265 试编码），支持一键复制报告。P2 配套，支撑 FR-10/FR-11 真机验收 | P2 | 已交付@v0.6.2（真机：/api/system/info + codec-test 实测，软件/AMD AMF 成功、QSV/VAAPI/NVENC 如实失败、报告可复制） |
| FR-22 | 跨平台打包：Makefile 一键产出单二进制发布包（前端 go:embed 内嵌 + 随包附带 ffmpeg/ffprobe + 启动自动发现同目录捆绑版），支持 Windows 与 Linux（CGO 各平台原生构建）。P2 配套 | P2 | 已交付@v0.6.2（Windows 单二进制真机全验证：go:embed 内嵌前端/ffmpeg 自动发现/版本注入；Linux 同构建路径，本机无 Linux 环境待真机） |
| FR-23 | 存储库管理页精简：只展示存储库卡片（含扫描进度与已索引媒体数量），移除页内媒体文件列表；卡片可点击跳转到对应目录视图 | P2 | 已交付@v0.4.0 |
| FR-24 | 设置子系统：新增 SQLite `settings` 表与设置页，支持运行期配置（每盘符回收站路径、扫描周期等）；为回收站与定时扫描提供配置真源 | P2 | 已交付@v0.4.0 |
| FR-25 | 软删除与回收站：删除媒体改为数据库软删标记（源文件不动），新增回收站页展示被软删项，支持还原 | P2 | 已交付@v0.4.0 |
| FR-26 | 回收站清理：必须先配置对应盘符回收站路径才允许清理；清理时把源文件移动到回收站目录并按删除日期分类，之后由用户手动清理 | P2 | 已交付@v0.5.0 |
| FR-27 | 增量与全量扫描 + 已删文件对账：支持手动增量更新与全量扫描；全量扫描时对账数据库记录，源文件已不存在的标记为软删（进回收站） | P2 | 已交付@v0.5.0 |
| FR-28 | 定时扫描：可配置周期的后台定时扫描任务，周期持久化于设置 | P2 | 已交付@v0.5.0 |
| FR-29 | 扫描任务队列 + 页眉任务展示：多扫描任务排队执行，页眉常驻展示当前进行中的任务，点开展示任务列表与各自进度 | P2 | 已交付@v0.5.0 |
| FR-30 | 文件名双模式编辑：区分「系统内显示名」（仅数据库）与「真实文件名」（磁盘改名）两种修改，均需二次确认 | P2 | 已交付@v0.4.0 |
| FR-31 | 媒体时间与 EXIF 提取：后端提取图片 EXIF / 视频媒体元数据，拍摄时间为主、缺失时依次降级为文件名日期解析 → 创建时间 → 修改时间；同时提取完整 EXIF（相机/镜头/光圈/快门/ISO/GPS），存字段供时间轴排序与 EXIF 展示（FR-38） | P2 | 已交付@v0.6.2（图片 EXIF 真机端到端验证：相机/镜头/光圈/快门/ISO/GPS/media_time 全正确；补齐视频时长/编码探测；降级链已验） |
| FR-32 | 时间轴缩放与拖动：时间轴支持按时间拖动与放大/缩小（类相册体验），按媒体时间（FR-31）组织 | P3 | 已交付@v0.10.0（缩放@v0.6.0 + 分组时间源降级链增强；拖动经 FR-68 交付，真机拖动手感待真机验） |
| FR-33 | 目录资源管理器视图：多种展示方式（文件详情 / 大-中-小图标）+ 排序方式与分类方式切换 + 单选/Shift 多选 | P3 | 已交付@v0.6.0 |
| FR-34 | 文件详情面板：选中项右侧展示详情、左侧图片/视频预览，支持全屏模式、上一项/下一项快捷键、鼠标滚轮放大缩小 | P3 | 已交付@v0.6.0 |
| FR-35 | 媒体筛选与表达式查询引擎：后端支持按时间范围/类型/大小/目录/文件名的结构化筛选，并实现 everything 式表达式搜索解析（→ 安全 SQL 查询） | P3 | 已交付@v0.6.0 |
| FR-36 | 前端筛选/搜索 UI：时间轴与目录视图接入内置筛选控件与表达式搜索框（消费 FR-35 引擎） | P3 | 已交付@v0.6.0 |
| FR-37 | HEIC/RAW 图片支持：识别 RAW 扩展名；服务端经外部 ImageMagick（magick）把 HEIC/RAW 转 JPEG 供浏览器显示与缩略图生成，带转换结果缓存 | P2 | 已交付@v0.6.2（修复 IM7 magick 参数顺序；真机 ImageMagick 7.1.2 验证 HEIC/RAW→JPEG + 缩略图 + 缓存命中） |
| FR-38 | EXIF 详情展示：文件详情面板展示完整 EXIF（拍摄时间/相机/镜头/光圈/快门/ISO/GPS 坐标），含「在外部地图打开」链接（消费 FR-31） | P2 | 已交付@v0.6.2（真机真实 EXIF 照片端到端：相机/镜头/光圈 f/1.8/快门/ISO/GPS 全部正确渲染，截图验收） |
| FR-39 | 照片地图视图：基于 EXIF GPS 的照片地理分布视图（leaflet + OSM 在线瓦片） | P3 | 已交付@v0.6.2（真机真实 GPS 照片端到端：OSM 在线瓦片 + 标记落点正确，截图验收） |
| FR-40 | 用户相册/合集：新增相册数据模型，支持跨目录手动归类、相册增删改、相册浏览视图 | P2 | 已交付@v0.4.0 |
| FR-41 | 收藏与标签：媒体标星收藏 + 自定义标签（打标签、按标签筛选），新增收藏/标签数据模型 | P2 | 已交付@v0.4.0 |
| FR-42 | 下载原文件：图片与视频原始文件下载端点（鉴权后），不再限于图片 | P2 | 已交付@v0.5.0 |
| FR-43 | 分享链接：token 化只读公开访问指定媒体/相册，带过期与范围，免登访问；作为鉴权的受控例外，依赖 FR-13 鉴权修复 | P2 | 已交付@v0.5.0 |
| FR-44 | 续播与观看状态：持久化每媒体上次播放位置 + 已看/未看标记 + 首页「继续观看」 | P2 | 已交付@v0.4.0 |
| FR-45 | 移动端 PWA：vite-plugin-pwa（manifest + Service Worker，可安装到主屏 + 离线壳）+ 响应式细节完善 | P2 | 已交付@v0.6.2（真机浏览器：SW 注册并 activated + manifest 可安装 + 离线壳从缓存渲染、断网时 API 正确失败；物理手机加主屏属 OS 级手势） |
| FR-46 | 远程自更新：服务器 UI 鉴权后检测 GitHub releases（正式版=最新正式 release / 测试版=最新预发布 dev，频道持久化于设置），下载对应平台产物并校验 checksum 后替换二进制、自动重启，保留旧二进制可回滚 | P4 | 开发中（后端 internal/update + 前端 UI + 单测完成；检测端到端真机验证：旧版 0.12.0 正确检测到 v0.13.0 有更新；修复下载大产物被 30s client 超时掐断改靠 context；完整下载→替换→重启因本机无法访问 GitHub 下载 CDN 未跑通，待可达环境真机复验） |
| FR-47 | 正式发布 CI：GitHub Actions 按版本号自动打 tag，windows/linux 原生 runner 构建（前端 embed + 版本注入），创建 GitHub Release（产物 + checksums） | P4 | 已交付@v0.7.0（归真：`.github/workflows/release.yml` tag 推送触发，复用 build.yml 多平台原生构建创建正式 Release；v0.7.0~v0.13.0 各版持续产出 windows/linux 二进制 + checksums，自更新检测真机已确认产物存在；见 ADR-0032） |
| FR-48 | 预发布 CI：GitHub Actions 普通 push 触发多平台构建，发布 prerelease（标 prerelease，上传打包产物） | P4 | 已交付@v0.7.0（归真：`.github/workflows/prerelease.yml` push main 触发，滚动 dev 预发布；dev 频道检测真机已确认；见 ADR-0032）。修复（FIX-1）：dev 版本号改为 `<最新正式版 tag 基线>-dev.<提交距离>.g<短SHA>`（基线不再 +1、提交距离为 0 时跳过发布），消除「发完正式版即凭空高一位、测试版一直有更新」；配合后端同基线判定改按提交距离序号比较（见 ADR-0042 扩展 ADR-0032）。 |
| FR-49 | 硬件加速检测统一为实测真源 + 持久化缓存：以编码器实测（codec-test）作为硬件加速能力唯一真源，结果按 ffmpeg 版本持久化于 SQLite（版本变更失效重测 + 手动重测），`/api/transcode/hwaccel` 与转码选码改读缓存；能力模型重构为 per-codec，补齐 AMD AMF 及全硬件家族（VAAPI/VideoToolbox/Vulkan）与 AV1/HEVC/VP9 探测展示 | P5 | 已交付@v0.8.0 |
| FR-50 | 转码目标编码可配置：服务端持久化「首选目标编码优先级」设置，单/多码率转码管道按所选编码参数化输出（扩展 FR-06 固定 H.264/TS 约束） | P5 | 已交付@v0.8.0 |
| FR-51 | 高级编码 fMP4/CMAF 分片输出：非 H.264 编码经 fMP4/CMAF 分片 + 浏览器原生 MSE 播放路径输出，H.264 维持 mpegts.js+TS 路径不变（新 ADR 扩展播放内核决策） | P5 | 已交付@v0.8.0 |
| FR-52 | 前端客户端能力探测 + 自适应播放器：以 `MediaSource.isTypeSupported` 探测客户端可解码编码，H.264 走 mpegts.js、高级编码走 MSE 播 fMP4，统一自适应播放入口 | P5 | 已交付@v0.8.0 |
| FR-53 | 端到端编码协商：播放发起时按「首选优先级 ∩ 客户端能力 ∩ 硬件可产出」协商实际输出编码与播放路径，含降级兜底，会话记录实际编码与路径 | P5 | 已交付@v0.8.0 |
| FR-54 | 可收缩左侧导航：左侧导航栏支持收缩为图标态与展开切换，状态持久化（移动端抽屉行为不变） | P6 | 已交付@v0.9.0 |
| FR-55 | 系统信息与设置合并为单页 + 顶部 tab：参考宝塔面板，「系统信息」（运行环境/FFmpeg/硬件加速/编解码测试/应用更新）与「设置」分 tab，原 /system 与 /settings 两导航项合并为一个入口 | P6 | 已交付@v0.9.0 |
| FR-56 | 设置功能补齐：查看全部环境变量 key/value（敏感项如 JWT_SECRET/SMB 主密码脱敏）+ ffmpeg/ffprobe 路径运行期可配置并支持检测（持久化设置 + 运行时应用 + 可用性/版本检测） | P6 | 已交付@v0.9.0 |
| FR-57 | 页脚版本号 + 开源协议页：页脚显示版本与「开源协议」链接；协议页列出依赖（前端全部 + 后端 go.mod 直接依赖，构建期生成嵌入、运行时不联网）与项目自身 MIT 协议/作者 | P6 | 已交付@v0.9.0 |
| FR-58 | 页眉「更新可用」提示：消费自更新检查的缓存结果，有更新时页眉显示提示，点击跳转到更新区 | P6 | 已交付@v0.10.0（提示+跳转@v0.9.0；精确定位经 FR-59 交付） |
| FR-59 | 系统诊断页拆分子 tab + 硬件加速卡片网格 + 页眉精确跳转：将「系统信息」拆为运行环境/FFmpeg/硬件加速/编解码/应用更新多个子 tab（消除长滚动），硬件加速卡片改一行 2-3 列网格，页眉「更新可用」点击精确定位到应用更新子 tab（修复 FR-58 跳转，扩 FR-55） | P7 | 已交付@v0.10.0 |
| FR-60 | 运行环境信息补齐：系统诊断「运行环境」补充内存/磁盘可用/运行时长/PID/工作目录/数据库路径等更多运行环境字段（扩 FR-21） | P7 | 已交付@v0.10.0 |
| FR-61 | 版本号与开源协议入口移入导航：移除页脚，将版本号与「开源协议」链接放入左侧导航（适配收缩态与移动端抽屉），取代 FR-57 页脚展示 | P7 | 已交付@v0.10.0 |
| FR-62 | 应用更新交互完善：进入默认用缓存展示，「检查更新」改「获取更新」走强制刷新（force），更新/回滚确认改 Mantine 模态框替代原生弹窗（扩 FR-46） | P7 | 已交付@v0.10.0 |
| FR-63 | 运行期配置设置页可编辑热更新：将安全可热更的运行期工具路径做成设置页可编辑、保存即生效——本期补齐 magick 路径设置键（扫描周期/ffmpeg·ffprobe 已分别由 FR-28/FR-56 实现）；敏感项（JWT/SMB）与启动期项（端口/DB 路径/debug 模式）保持只读（扩 FR-56） | P7 | 已交付@v0.10.0 |
| FR-64 | 存储库后缀列管：新增删除后缀端点，前端可列出（内置/自定义标识）并删除自定义后缀，后缀管理支持视频/图片/全部筛选视图（扩 FR-01） | P7 | 已交付@v0.10.0 |
| FR-65 | 存储库卡片网格布局：存储库列表由单列铺满改为一行 2-3 个网格，重排卡内结构（扩 FR-23） | P7 | 已交付@v0.10.0 |
| FR-66 | 目录浏览聚合多库：目录浏览顶层聚合所有存储库为单一虚拟根、逐库下钻，新增库即时反映（取代当前单库视图，扩 FR-15） | P7 | 已交付@v0.10.0 |
| FR-67 | 时间轴刷新按钮：时间轴页新增手动刷新按钮，重载首页数据（扩 FR-14） | P7 | 已交付@v0.10.0 |
| FR-68 | 时间轴可拖动 scrubber 预览：日期轴改为可拖动时间滑块，拖动时浮层预览该时间段日期/缩略图、松手跳转（补齐 FR-32 未实现的拖动） | P7 | 已交付@v0.10.0（scrubber+浮层预览+松手跳转已交付并单测；真机拖动滚动手感待真机验） |
| FR-69 | 列表多选与右键批量操作：时间轴与目录浏览支持 ctrl 点选/shift 连选/ctrl+a 全选/反选/复选框模式 + 右键常用菜单，批量删除（软删进回收站），新增后端批量软删端点（扩 FR-33） | P7 | 已交付@v0.10.0 |
| FR-70 | 感知哈希去重：基于文件哈希/感知哈希（pHash）检出重复或近似媒体，提供批量清理候选 | P7 | 已交付@v0.11.0 |
| FR-71 | 离线人脸/场景聚类相册：本地（不联网）对图片做人脸/场景聚类，自动生成人物/地点/事物智能相册 | P7 | 计划（暂留 backlog：人脸聚类需 ML 运行时+预训练模型，属重型件、与「简单优先/禁重型件」架构不变量张力大；待单独立项写 ADR 评估技术栈后再启动） |
| FR-72 | 「那年今日」回忆流：按媒体时间自动生成「X 年前的今天」回忆，首页回忆卡片 | P7 | 已交付@v0.11.0 |
| FR-73 | 媒体健康巡检：后台扫描损坏（ffprobe 失败）/0 字节/无法生成缩略图的问题媒体，列出问题清单 | P7 | 已交付@v0.11.0 |
| FR-74 | 命令面板（Ctrl+K）：全局快捷命令面板，快速跳转/搜索/加库/扫描/切主题等 | P7 | 已交付@v0.12.0 |
| FR-75 | 观看热力与统计：每视频观看次数、续播位置热力、最近观看时间线（复用 FR-44） | P7 | 已交付@v0.12.0 |
| FR-76 | GPS 旅程轨迹视图：把同一行程带 GPS 的照片连成地图轨迹（扩 FR-39） | P7 | 已交付@v0.11.0 |
| FR-77 | 转码预设与预生成队列：自定义目标编码/分辨率预设，手动将媒体加入转码队列预热首播（复用 FR-49~53） | P7 | 已交付@v0.13.0 |
| FR-78 | 分享增强：带密码/限次/二维码的分享链接（扩 FR-43） | P7 | 已交付@v0.13.0 |
| FR-79 | 外部播放器深链与扫码续播：生成 VLC/IINA 可打开的流地址、扫码在手机从续播点接力 | P7 | 已交付@v0.12.0 |
| FR-80 | 后端出站网络代理设置：运行期可配置 HTTP/HTTPS/SOCKS5 代理（settings 持久化、保存即生效、空则直连），后端外部 HTTP 出站经该代理；首个消费者 FR-46 自更新检测/下载，解决直连 GitHub 不可达 | P4 | 已交付@v0.14.0 |
| FR-81 | 缩略图显示增强：透明源（PNG 等）转码时加中性灰底消除深色卡片上的纯黑、前端按原比例自适应显示（contain + 统一背景）消除竖图/正方图裁切、缺图先显骨架占位并在生成完成后自动替换/加载失败兜底、按列宽提供多尺寸 srcset 降流量与解码成本（扩 FR-D） | P8 | 已交付@v0.15.0 |
| FR-82 | 登录页打磨：空表单时登录按钮禁用态明确可辨（不致误判为坏按钮）、新增品牌图形（原创 SVG logo）消除整页大片留白（扩 FR-13） | P8 | 已交付@v0.15.0 |
| FR-83 | 左侧导航分组：按「浏览 / 管理 / 系统」分组并加分隔，降低查找成本；兼容收缩态与命令面板对导航项的复用（扩 FR-54） | P8 | 已交付@v0.15.0 |
| FR-84 | 主题切换过渡与对比度：亮暗切换加约 150ms 颜色过渡（带 prefers-reduced-motion 兜底）、全局上调 `dimmed` 次要文案对比度至 WCAG AA（扩 FR-G） | P8 | 已交付@v0.15.0 |
| FR-85 | 播放页操作收纳与影院模式：头部次要操作收进「更多 ⋯」菜单消除窄屏换行铺满、新增「收起导航」影院模式（播放页本地态、退出自动恢复）让视频区变宽（扩 FR-30，新增播放页影院态） | P8 | 已交付@v0.15.0 |
| FR-86 | 移动端筛选折叠：移动端将筛选控件收进「筛选」抽屉、搜索框常驻，使首屏直达媒体而非被筛选区推到很下（扩 FR-36/FR-45） | P8 | 已交付@v0.15.0 |
| FR-87 | 设置页改进：每盘符回收站路径改结构化「盘符→路径」键值编辑器（前端序列化、后端 JSON 契约不变）、设置项按「扫描 / 网络 / 工具 / 回收站」分区、JWT_SECRET 未设时设置页内联警示（扩 FR-24/FR-55/FR-56） | P8 | 已交付@v0.15.0 |
| FR-88 | 空态体验统一：统计页少数据/空态加引导文案并隐藏无意义图表、转码/巡检/重复项空态加居中插画（原创 SVG）+ 引导文案 + 一键 CTA（扩 FR-75/FR-77，覆盖 FR-70/FR-73） | P8 | 已交付@v0.15.0 |
| FR-89 | 代理连通性测试：新增 `POST /api/system/proxy/test`，用临时 client 探测目标（默认 api.github.com）可达性、不污染运行期代理真源；前端代理输入旁加「测试」按钮（仿 ffmpeg 检测）（扩 FR-80） | P8 | 已交付@v0.15.0 |
| FR-90 | 自更新下载进度与重试：新增后端下载进度上报端点 + io.Copy 计数包装、前端展示下载进度条与失败重试入口；配 FR-80 代理在可达环境补完整「下载→替换→重启」真机验收（扩 FR-46） | P8 | 开发中（v0.15.0 交付下载进度上报端点 + 前端进度条/失败重试 UI + 单测，UI 真机在位；真实大文件下载时进度条填充与完整「下载→替换→重启」e2e 受本机 GitHub 下载 CDN 不可达 + 替换运行中进程高风险所限未实机观测，待可达环境复验） |
| FR-91 | 批量操作扩展：列表多选支持批量加相册、批量打标签（复用 FR-40/FR-41 单项端点），以及批量打包下载（新增后端 zip 流端点，边写边 flush、SMB 项跳过）（扩 FR-69，复用 FR-42） | P8 | 已交付@v0.15.0 |
| FR-92 | 设计系统 token 基础：在主题定义并接入间距/圆角/阴影/字号刻度/密度/焦点环 token，全站组件改用 token 取代散落硬编码（前端设计系统地基，其余界面 FR 依赖之） | P9 | 已交付@v0.16.0 |
| FR-93 | 色彩体系与语义色：定义品牌紫色板与 success/danger/warning/info/neutral 语义色 token，导航激活态与链接由蓝/绿收敛回品牌紫，徽标/按钮按语义色规范（扩 FR-G） | P9 | 已交付@v0.16.0 |
| FR-94 | 组件规范统一：按钮四档层级（primary/secondary/tertiary/ghost）、卡片层次阴影、徽标分类规范，原生日期控件换 Mantine DatePicker（请求参数不变）（扩 FR-G，含 ref） | P9 | 已交付@v0.16.0 |
| FR-95 | 应用框架重构：顶栏分组+用户头像下拉菜单、侧栏激活态 pill+放大 logo、深层页面包屑、收起导航入口前移（扩 FR-54/FR-55/FR-61） | P9 | 已交付@v0.16.0 |
| FR-96 | 全局微交互与动效：卡片/按钮/导航 hover 过渡与抬升、路由切换动画、全局顶部加载进度条，全部带 prefers-reduced-motion 兜底 | P9 | 已交付@v0.16.0 |
| FR-97 | 无障碍与可读性：统一焦点可见环、对比度全面核 WCAG AA、触控目标≥44px、长文本 title/截断（扩 FR-G） | P9 | 已交付@v0.16.0（亮色 dimmed 对比度/进度条与导航可访问名 v0.16.1 真机 Lighthouse 收尾至 a11y 100） |
| FR-98 | 空态/加载/反馈统一：EmptyState 覆盖照片地图/空相册/搜索无结果，列表骨架屏，通知与错误态（插画+重试）统一（扩 FR-88） | P9 | 已交付@v0.16.0 |
| FR-99 | 媒体卡与网格重设计：信息密度优化、hover 操作浮层、视频时长角标+播放叠层、选中态强反馈、sticky 批量操作条、响应式列数（扩 FR-D/FR-E/FR-69） | P9 | 已交付@v0.16.0 |
| FR-100 | 浏览页信息架构 A：时间轴筛选分组与日期轴视觉、相册卡封面+自适应网格、管理页后缀折叠分组（扩 FR-14/FR-40/FR-64） | P9 | 已交付@v0.16.0 |
| FR-101 | 浏览页信息架构 B：系统诊断键值定宽排版+monospace、统计页可视化精修、回收站/巡检/重复项统一媒体行组件（扩 FR-60/FR-75） | P9 | 已交付@v0.16.0 |
| FR-102 | 直接播放：图片灯箱遇视频内嵌播放器自动播（去「打开播放」按钮）、播放页静音自动播取代「点击播放」遮罩并提供取消静音入口（扩 FR-16/FR-34） | P9 | 已交付@v0.16.0 |
| FR-103 | 播放页全屏沉浸布局：播放路由 100dvh 铺满视口不可纵向滚动、视频填满剩余高度、媒体信息收纳出文档流、控件悬浮自动隐藏（扩 FR-85） | P9 | 已交付@v0.16.0 |
| FR-104 | 播放器内核与控件增强：全屏/画中画/键盘快捷键/倍速/±10s/进度条 hover 预览/控件自动隐藏/记忆音量/字幕样式（扩 FR-16） | P9 | 已交付@v0.16.0 |
| FR-105 | 图片灯箱交互增强：缩放后拖拽平移、双击放大、移动端捏合、上下一项环绕、相邻预加载、幻灯片、位置计数、旋转（扩 FR-34） | P9 | 已交付@v0.16.0 |
| FR-106 | 图片查看器 UI 重做：EXIF 图标化+单位格式化、GPS 改站内地图、工具栏齐全（收藏/标签/分享/旋转/下载）、沉浸可折叠信息栏、复制 EXIF/路径（扩 FR-34/FR-38/FR-39） | P9 | 已交付@v0.16.0 |
| FR-107 | 统一媒体查看器框架：抽出图+视频同一查看器框架，一致导航/全屏/工具栏，播放页与灯箱复用同内核收敛重复（ref，扩 FR-34/FR-85） | P9 | 已交付@v0.16.0 |
| FR-108 | 登录用户修改密码：已登录用户校验当前密码后设置新密码（后端 change-password 端点 + 前端设置/系统页入口），bcrypt 存储、即时生效（扩 FR-13） | P10 | 已交付@v0.17.0 |
| FR-109 | 首次初始化引导：取消 admin/admin 默认建号，无用户时首次访问展示初始化引导页配置用户名+密码并创建首个账户登录；加「是否需初始化」检测与「完成初始化」端点及前端路由守卫（扩 FR-13，见 ADR-0040） | P10 | 已交付@v0.17.0 |
| FR-110 | 运行时调试日志开关与默认安静日志：默认降噪 GORM 日志（不刷 record-not-found/SQL），设置页加运行时「调试日志」开关（区别于启动期只读的 JIANVIDEO_DEBUG），开启输出详细日志、关闭恢复安静，开关持久化并启动读取（扩 FR-56/FR-63） | P10 | 已交付@v0.17.0 |
| FR-111 | 发布说明源自 CHANGELOG：正式发布从 CHANGELOG 对应版本段抽取作为 Release notes（替代 generate_release_notes），并修预发布抽取标题不匹配（`## 未发布版本`→`## 未发布`）（扩 FR-47/FR-48） | P10 | 已交付@v0.17.0 |
| FR-112 | 应用更新面板交互重构：「检查更新」「获取更新」合并为单个「检查更新」按钮（点击=force 强制直连重查）；进入页面仅展示上次本地缓存、无缓存则不显示更新区、有缓存直接显示缓存版本且不自动联网；更新成功后清理本地缓存并重新拉取（扩 FR-62/FR-46） | P11 | 已交付@v0.18.0 |
| FR-113 | 系统/设置页结构重构：去掉两级 tab，改为一级 tab（运行环境/硬件加速/编解码/应用更新/设置），「设置」并入为一级 tab；每个 tab 内提供左侧锚点导航分区、点击滚动定位并高亮当前区块（取代 FR-55 两级结构，扩 FR-59） | P11 | 已交付@v0.18.0（行为级单测全绿；一级 tab + 锚点导航视觉走查待真机） |
| FR-114 | 页眉刷新按钮：页眉新增刷新按钮，仅重载当前路由页面的内容数据（不整页 reload、不重载导航/页眉、不重置登录态）（扩 FR-67/FR-95） | P11 | 已交付@v0.18.0 |
| FR-115 | 导航交互完善：展开/收缩按钮移至左侧导航底部右下角；点击 logo 切换展开/收缩（移除 logo 回首页，回首页改走「时间轴」导航项）；补齐所有导航项的 hover 背景与过渡态（扩 FR-54/FR-95/FR-96） | P11 | 已交付@v0.18.0（行为级单测全绿；收缩按钮位置/logo 切换/hover 视觉走查待真机） |
| FR-116 | 左侧导航可拖拽调整宽度：navbar 右边缘拖拽手柄调整展开态宽度，夹紧 160–360px，宽度持久化（localStorage），仅展开态生效、收缩态与移动端抽屉不受影响（扩 FR-54/FR-115） | P11 | 已交付@v0.19.0（真机预览验证：拖拽调宽生效并持久化；同批真机反馈打磨 v0.19.0 收口） |
| FR-117 | 首页概览看板：根路由 `/` 由时间轴改为系统总览看板（KPI 媒体总数/视频图片拆分、总时长、占用空间、媒体库/相册数 + 媒体构成与各库分布 + 观看概览 + 系统状态 + 任务队列 + 继续观看），时间轴移至 `/timeline`、导航「浏览」组顶部新增「概览」；新增 `GET /api/library/summary` 一次聚合端点（取代 AC-13「时间轴=首页」，见 ADR-0043） | P12 | 已交付@v0.20.0 |
| FR-118 | 统计页内容趋势扩展：`/stats` 顶部分「观看 / 媒体」tab，各 tab 当前值卡（含 sparkline）+ 时序折线图；媒体增长、累计时长/容量、观看活跃趋势由现有时间字段（`added_at`/`media_time`/观看时间戳）聚合得出（无新存储）；引入 Recharts 图表库（扩 FR-75，见 ADR-0045） | P12 | 已交付@v0.20.0 |
| FR-119 | 系统监控页：新增 `/monitor` 页展示 CPU/内存/磁盘/转码并发当前值 + 时序折线（range 1h/24h/7d）；新增定时采样器 + SQLite 样本表 + 保留期裁剪 + `GET /api/system/metrics`（采样只落 SQLite、不引时序库，采样器独立服务层、`db` 仅样本读写，见 ADR-0044） | P12 | 已交付@v0.20.0 |
| FR-120 | 时间轴页苹果风重设计：右侧时间标尺悬停即弹出该时段缩略图预览（升级 FR-68 拖动浮层）、年/月/日/所有缩放、方形密铺日期分组网格；顶部「那年今日」+「最近查看」（新增媒体打开 `viewed_at` 记录 + `recently-viewed` 端点）（扩 FR-14/FR-32/FR-68/FR-72） | P12 | 已交付@v0.20.0 |
| FR-121 | 目录浏览重做为资源管理器：左导航树 + 可点地址栏 + 工具栏 + 名称/修改日期/类型/大小详情列 + 状态栏 + 视图模式；聚合改为按真实磁盘路径前缀跨库重建单一目录树（盘符/共享为根，传 `D:\` 库即整盘可浏览），排序后端参数化；移除全局页级面包屑 `PageBreadcrumbs`；只读浏览，不做真动磁盘文件操作（取代 ADR-0037，见 ADR-0046） | P12 | 已交付@v0.20.0 |
| FR-122 | Go 静态检查门禁：新增 `.golangci.yml`（全套 linter：govet/staticcheck/errcheck/ineffassign/revive/bodyclose/sqlclosecheck 等），Makefile 加 `lint` 目标（gofmt 检查 + goimports 检查 + golangci-lint run，help 列出，只管 Go）并固定工具版本，修复全部存量告警至全绿（落地 static-analysis.md 既定工具链） | P13 | 已交付@v0.20.1 |
| FR-123 | 前端 Prettier 落地与 ESLint 告警清零：引入 prettier + eslint-config-prettier 及配置、format/format:check/lint:fix 脚本，eslint 配置接入 eslint-config-prettier 关闭冲突格式规则，全量 `prettier --write` 格式化（format-only 独立提交、行为不变）并修复全部 eslint 告警（落地 static-analysis.md 既定工具链，扩 eslint 现有配置） | P13 | 已交付@v0.20.1 |
| FR-124 | 前端 Mock API 端点补全：审计 `frontend/src/api` 实际调用的全部后端端点，补齐 MSW handlers 至 100% 覆盖、消除未处理请求，并校准 mock 数据形状贴近真实响应（扩现有 MSW 测试基建） | P13 | 已交付@v0.20.1 |
| FR-125 | 前端关键浅测补强：审计 render-only 浅测，对关键页面/组件补行为与交互断言（点击/表单/API 往返/状态转换），提升测试有效性（扩 FR-H 测试基线） | P13 | 已交付@v0.20.1 |
| FR-126 | 前端测试覆盖率阈值门禁：引入 @vitest/coverage-v8，vitest 覆盖率配置加 thresholds（lines/statements 75、functions 60、branches 75，按实测务实定档），先实测当前值定档，覆盖率不达标即失败（扩 FR-125） | P13 | 已交付@v0.20.1 |
| FR-127 | 前端 E2E 接入 CI 并扩关键场景：现有 Playwright E2E 接入 CI 回归门，并补关键流程 spec（播放/转码、目录浏览、设置等），复用已配 retries/SW 禁用/独立 DB（扩现有 e2e 基线） | P13 | 已交付@v0.20.1 |
| FR-128 | CI 质量门工作流：新增 `.github/workflows/ci.yml`（PR 与 push main 触发）统一执行 Go（make lint + go test）与前端（lint + format:check + 覆盖率门禁）质量门、E2E 独立 job 失败挡合并，build.yml 仍专管发版（首次在 CI 设质量门，见 ADR-0047） | P13 | 已交付@v0.20.1 |
| FR-129 | 动态文档标题：路由切换时浏览器标签页标题随当前页变为「<页面名> - JianVideo」，无名路由回退「JianVideo」（新增统一文档标题 hook） | P14 | 开发中 |
| FR-130 | 导航分组重构（新增「洞察」组）：左侧导航改为「浏览(概览/时间轴/目录/相册/地图) · 洞察(统计/监控) · 管理(库管理/回收站/巡检/重复项/转码/系统)」——统计移出浏览、监控移出系统、系统并入管理（扩 FR-83/FR-55） | P14 | 开发中 |
| FR-131 | 导航紧凑化与动画完善：压缩导航项间距/内边距/字号至更紧凑密度，完善展开收缩与项进入过渡（扩 FR-54/FR-115/FR-96） | P14 | 开发中 |
| FR-132 | 页眉居中全局搜索框：页眉中部新增居中搜索入口，统一搜索全部媒体（名称/路径/显示名），与命令面板/各页搜索协同（扩 FR-74/FR-35） | P14 | 开发中 |
| FR-133 | 全局统一固定高度布局：app-shell 页眉/导航固定，各页内容区固定高度+内部滚动并预留滚动条宽度，消除概览↔时间轴↔目录切换的布局跳动（扩 FR-95） | P14 | 开发中 |
| FR-134 | 页面标题统一收敛：新增统一 PageHeader 组件，隐藏各页冗余大标题（时间轴/目录浏览/相册等），当前位置由导航高亮+动态标题(FR-129)表明（扩 FR-95） | P14 | 开发中 |
| FR-135 | 引入 framer-motion 动效库：引入 framer-motion 承担列表进入/共享元素过渡等 CSS 难做动效，prefers-reduced-motion 下全禁（扩 FR-96，见 ADR-0048） | P14 | 开发中 |
| FR-136 | 高级搜索与可搜字段扩展：搜索面板（Everything 式筛选器）+ 后端搜索扩展至 显示名/EXIF（相机/镜头），表达式 token 扩展（camera:/lens: 等）（扩 FR-35/FR-36/FR-132） | P15 | 开发中 |
| FR-137 | 媒体备注与备注搜索：media_files 新增 notes 备注字段（迁移）+ 查看器/详情编辑入口 + 纳入基础搜索（扩 FR-136/FR-34） | P15 | 开发中 |
| FR-138 | 时间轴导航形态重构：移除左侧竖向日期轴，右侧标尺升级为唯一完整时间轴（年/月刻度 + hover/拖动缩略图预览 + 点击跳转），媒体网格占满主区（取代 FR-120/FR-B 左轴形态，见 ADR-0049） | P16 | 开发中 |
| FR-139 | 时间轴工具栏与分类筛选：筛选/视图控件重排并冻结吸顶，新增「分类」筛选（复用标签 FR-41，内置映射 favorite 的收藏夹）（扩 FR-100/FR-41） | P16 | 开发中 |
| FR-140 | 缩略图左下角常驻收藏：缩略图左下角常驻可点收藏（复用 favorite API），与左上选择/右下时长互不冲突（扩 FR-41/FR-99） | P16 | 开发中 |
| FR-141 | 时间轴大数据集性能：单日大量媒体卡片级虚拟滚动 + 缩略图按网格列宽/DPR 自适应尺寸（扩 FR-120/FR-E） | P16 | 开发中 |
| FR-142 | 时间轴精准定位：日期跳转输入（YYYY/-MM/-DD）+ 缩放粒度切换视口锁定不漂移（扩 FR-32/FR-120） | P16 | 计划 |
| FR-143 | 时间轴移动端交互：窄屏时间轴条防误触（月份标签/隐藏细轨）、下拉刷新、筛选抽屉优化（扩 FR-86/FR-45） | P16 | 计划 |
| FR-144 | 时间轴媒体库筛选：时间轴新增按媒体库（图库）筛选下拉（含多选模式入口），复用后端 library_id 过滤（扩 FR-06/FR-100） | P16 | 计划 |
| FR-145 | 回忆轮播与跳转：最近查看/往年今日横向轮播（左右滚动按钮），卡片点击主跳到那天并按日期筛选、并提供卡片播放入口（扩 FR-72/FR-120/FR-44） | P16 | 计划 |
| FR-146 | 时间轴分组头聚合信息：日期分组头展示数量 + 主要设备/相机 + 地点，并支持「选当天/当月全部」组级全选（扩 FR-138/FR-31） | P17 | 计划 |
| FR-147 | 地名逆地理编码：由 GPS 经纬度（FR-31）解析可读地名（如 杭州·西湖）供聚合/筛选展示（见 ADR-0050） | P17 | 开发中 |
| FR-148 | 来源 App 识别：按文件名启发式推断媒体来源（截图/各 App 导出）供聚合展示，无规则命中则不显示（扩 FR-31） | P17 | 开发中 |
| FR-149 | Web 上传入库：页眉上传文件，落到可配置默认上传位置（设置可配 + 上传时可选），按可配置目录/文件命名规则（保留原样/整齐两档）落盘并自动触发扫描入库（扩 FR-01/FR-57，见 ADR-0051） | P18 | 开发中 |
| FR-150 | 目录浏览多标签：Browse 页 Windows 资源管理器式多标签（每标签=一个目录位置，开/关/新增/切换）（扩 FR-121） | P19 | 开发中 |
| FR-151 | 浏览标签与位置持久化：打开的标签列表与上次浏览位置缓存到后端（复用 settings 键 open_tabs/last_opened_path），重启恢复（扩 FR-150/FR-121） | P19 | 开发中 |

> 以下为前端优化专项（2026-03-04 启动）：
| FR-F | 前端代码结构拆分：LibraryPage 拆分为多个子组件 + hooks，VideoPlayer 重构，样式统一 Mantine，删除模板代码 | P1 | 已交付@v0.1.0 |
| FR-C | 后端异步扫描 + SSE 进度推送：扫描改为 goroutine 异步，新增 SSE 进度端点，前端展示扫描进度条 | P1 | 已交付@v0.1.0 |
| FR-D | 缩略图系统：后端 ffmpeg 生成视频/图片缩略图，前端加载缩略图替代原图 | P1 | 已交付@v0.1.0 |
| FR-A | 页面流程重设计：拆为存储库管理页、时间轴视图页、目录浏览页，Tab 切换导航 | P1 | 已交付@v0.1.0 |
| FR-B | 时间轴视图重做：左侧日期轴 + 右侧缩略图网格，按日期分组展示 | P1 | 已交付@v0.1.0 |
| FR-E | 虚拟列表 + 懒加载：react-virtual 虚拟滚动，图片 loading="lazy"，千条列表流畅滚动 | P1 | 已交付@v0.1.0 |
| FR-G | 暗色模式 + 路由守卫 + 全局错误处理：Mantine ColorScheme 切换，ProtectedRoute，统一错误 toast | P2 | 已交付@v0.1.0 |
| FR-H | 测试同步更新：parseWebVTT 提取为公共工具，重构后全量测试绿 | P1 | 已交付@v0.1.0 |

> 状态取值：计划 / 开发中 / 已交付@vX.Y.Z。优先级：P1(MVP) / P2 / P3 / P4 / P5 / P6 / P7。
> 标 `已交付` 是有门的：只有该 FR 的 §6 / spec 验收标准全部满足、对应测试 / 实机验收通过后，才由 `sdd-release-version` 在发版时统一标 `已交付@vX.Y.Z`——开发 / 修复过程中不得自行预标。

## 5. 非功能需求（NFR）

- **NFR-01 转码性能**：硬件加速开启时，1080p H.264→H.265 转码帧率不低于源视频帧率的 90%。
- **NFR-02 首帧延迟**：Seek 后首帧画面出现时间不超过 2 秒（依赖 GOP 对齐与 I 帧命中）。
- **NFR-03 追播延迟**：边下边播模式下，播放器末端与文件写入末端的延迟控制在 3-5 秒内。
- **NFR-04 内存占用**：单个转码任务的内存占用不超过 512MB。
- **NFR-05 并发能力**：支持至少 3 个并发转码流（硬件加速受硬件能力限制时可降级）。
- **NFR-06 跨平台**：最终交付物为单个可执行文件，支持 Windows/Linux/macOS。
- **NFR-07 无外部依赖**：运行时无需预装 FFmpeg、Node.js 等外部依赖，所有配置通过配置文件或环境变量控制。
- **NFR-08 媒体库规模**：支持不低于 10,000 个视频文件的元数据管理，目录浏览响应时间不超过 500ms。

## 6. 验收标准

- **AC-01**：添加本地目录后，递归扫描可将目录内所有内置图片/视频文件在 30 秒内出现在媒体库中（需用户确认）。
- **AC-02**：删除或移动文件后，媒体库在 10 秒内自动更新（需用户确认）。
- **AC-03**：H.264+AAC 的 MP4 文件在浏览器中直接播放，不触发转码（需用户确认）。
- **AC-04**：H.265/MKV 等非浏览器兼容格式自动触发转码，3 秒内开始播放（需用户确认）。
- **AC-05**：拖动进度条后 2 秒内画面更新（需用户确认）。
- **AC-06**：转码进行中即可开始播放，无需等待转码完成（需用户确认）。
- **AC-07**：硬件加速转码时 CPU 占用率低于软件转码的 50%（需用户确认）。
- **AC-08**：最终交付为单个可执行文件，在 Windows/Linux/macOS 上均可运行（需用户确认）。
- **AC-09**：登录后刷新页面，认证状态保持，右上角仍显示用户名（需用户确认）。
- **AC-10**：手机端（< 768px）侧边栏默认折叠，点击汉堡菜单按钮可展开/收起（需用户确认）。
- **AC-11**：自定义后缀绑定到单个媒体库目录，删除目录时关联自定义后缀一并清理，其他目录不受影响。
- **AC-12**：Windows 路径下文件目录视图可正常列出子目录和文件，面包屑不出现 `/D:` 这类错误路径。
- **AC-13**（FR-117 起修订）：时间轴视图迁至 `/timeline`，按添加时间倒序展示媒体，图片点击打开预览、视频点击进入播放页；根路由 `/` 改为「概览」数据看板（见 AC-21），导航「浏览」组提供「概览」与「时间轴」两入口（见 ADR-0043）。
- **AC-14**（FR-21）：`/system` 页加载后展示当前系统信息（OS/架构/CPU 数/主机名/Go 版本/应用版本）与 ffmpeg 可用性及版本；点击「测试编解码器」后展示各候选编码器的「是否编入当前 ffmpeg / 试编码是否成功」，「复制结果」可将纯文本报告写入剪贴板。
- **AC-15**（FR-21，需用户确认）：在装有 Intel/NVIDIA 的机器上运行打包产物，`/system` 页对相应硬件编码器（如 `h264_qsv`/`h264_nvenc`）报告试编码成功。
- **AC-16**（FR-22，需用户确认）：`make package` 在 Windows 与 Linux 各自产出单二进制发布包，随包 ffmpeg/ffprobe 被启动时自动发现，无需手动配置即可运行与播放。
- **AC-17**（FR-108，需用户确认）：已登录用户用错误的当前密码改密被拒绝；用正确的当前密码改密成功后，旧密码登录失败、新密码登录成功。
- **AC-18**（FR-109，需用户确认）：全新库（无任何用户）首次访问跳转初始化引导页，设置账号密码后以该账号登录成功且系统中不存在 admin/admin 默认账户；已有用户的库直接进入登录页、不再触发引导。
- **AC-19**（FR-110，需用户确认）：默认启动与运行日志中无 GORM `record not found`/SQL 噪音；设置页开启「调试日志」后输出 SQL/请求详细日志、关闭后停止；重启后保持上次开关状态。
- **AC-20**（FR-111，需真实发布/脚本确认）：正式发布产物的 Release 正文等于 CHANGELOG 对应版本段内容；预发布正文的「自上个正式版以来的变更」段为 CHANGELOG 未发布段实际内容（非「暂无记录」）。
- **AC-21**（FR-117，需用户确认）：根路由 `/` 渲染「概览」数据看板——KPI（媒体总数 + 视频/图片拆分、视频总时长、占用空间、媒体库数、相册数）、媒体构成与各库分布、观看概览（可跳 `/stats`）、系统状态、任务队列、继续观看；空库时各卡显示零值而非报错。`GET /api/library/summary` 单次聚合返回 total/video_count/image_count/total_size/total_duration/library_count/by_library。
- **AC-22**（FR-118，需用户确认）：`/stats` 顶部「观看 / 媒体」两 tab 可切换并经 `?tab=` 记忆；观看 tab 含已看/未看/追看中当前值卡 + 观看活跃趋势折线 + 既有续播热力/各库各格式分布/Top 榜；媒体 tab 含媒体总数/视频/图片/总时长/占用当前值卡 + 媒体增长与累计容量/时长折线 + 媒体构成；折线可 hover 显示精确值；空库整页空态、空趋势显占位不报错。`GET /api/library/trends` 返回按天新增媒体序列（date/count/size/duration，升序）。
- **AC-23**（FR-119，需用户确认）：`/monitor` 展示 CPU/内存/磁盘/转码并发当前值卡 + 时序折线（range 1h/24h/7d 可切、折线可 hover 看精确值），刚启动无样本时显占位不报错；导航「系统」组含「监控」入口。`GET /api/system/metrics?range=` 由后台采样器（15s 周期、SQLite `metric_samples`、7 天保留）下采样返回 points + current 快照。
- **AC-24**（FR-120，需用户确认）：`/timeline` 右侧时间标尺鼠标 hover（非仅拖动）即在指针处弹出该时段日期 + 数量 + 缩略图预览，点击/松手跳转该分组；缩放支持年/月/日/所有；顶部展示「最近查看」（有数据横向卡片流可点击进入、空则不渲染），打开媒体后其进入「最近查看」；多选/详情/批量既有交互不回归。`PUT /api/library/media/:id/viewed` 记录查看、`GET /api/library/recently-viewed` 倒序返回。
- **AC-25**（FR-121，需用户确认）：`/browse` 为资源管理器布局（左导航树 + 可点地址栏 + 工具栏 + 名称/修改日期/类型/大小详情列 + 状态栏 + 视图模式）；目录树按**真实磁盘路径跨库合并**（加 `D:\1` 与 `D:\1\2` 在 `D:` 下合成 `1 → 2` 单一树；加 `D:\` 库则整盘可浏览）；排序经后端 `sort`（name/size/type/time）生效；地址栏路径段可点跳转；多选/详情/批量/筛选既有交互不回归；全局页级面包屑 `PageBreadcrumbs` 已移除。`GET /api/library/browse?parent_path=&sort=` 跨库前缀聚合（见 ADR-0046 取代 ADR-0037）。
- **AC-26**（FR-122）：`make lint` 在本地与 CI 均全绿——`gofmt -l`/`goimports -l` 无输出、`golangci-lint run`（全套 linter）0 issue；修复后受影响 Go 包 `go test` 仍全绿（证明修复未改变行为）。
- **AC-27**（FR-123）：`npm run format:check` 全绿、`npm run lint` 0 error/warning；全量格式化为独立提交、与 eslint 告警修复提交分开；`npm run build`（tsc -b + vite build）与 `npm test` 均通过（格式化未破坏行为）。
- **AC-28**（FR-124）：可列出 `frontend/src/api` 全部后端端点清单且 MSW handlers 100% 覆盖；测试在 `onUnhandledRequest:'error'` 下运行无未处理请求；`npm test` 全绿。
- **AC-29**（FR-125）：选定的关键浅测均含实质行为/交互断言（非仅渲染存在性）；`npm test` 全绿；覆盖率较补强前不下降。
- **AC-30**（FR-126，需先实测定档）：`npm run test:coverage` 产出覆盖率且不低于配置阈值（实测务实定档 lines/statements 75、functions 60、branches 75）；阈值写入 vitest 覆盖率配置；构造低于阈值用例可使其失败（门禁生效）。
- **AC-31**（FR-127）：新增关键流程 spec 本地 `npm run e2e` 通过；CI 中 E2E job（headless Chromium）通过、失败时阻断合并；覆盖播放/转码、目录浏览、设置等关键流程。
- **AC-32**（FR-128，需 CI 实跑确认）：`ci.yml` 在 PR 与 push main 触发，依次跑 Go、前端、E2E 门；任一门失败即整体 fail 并阻断合并（构造失败用例验证）；本地 `make lint` 与 `npm run lint/format:check/test:coverage/e2e` 与 CI 行为一致；`build.yml` 发版职责不变（见 ADR-0047）。
- **AC-33**（FR-129）：切换各路由后浏览器标签页标题为「<页面名> - JianVideo」；未知路由回退「JianVideo」。
- **AC-34**（FR-130）：左侧导航呈现「浏览/洞察/管理」三组；统计与监控在洞察组、系统为管理组末项；各项跳转正确。
- **AC-35**（FR-131，需真机走查）：导航较前更紧凑（间距/字号下调）；展开/收缩与项过渡流畅；prefers-reduced-motion 下禁用动画。
- **AC-36**（FR-132）：页眉居中搜索框输入关键词可搜全部媒体（名称/路径/显示名命中）；快捷键可聚焦。
- **AC-37**（FR-133，需真机走查）：概览↔时间轴↔目录切换时页面无横向宽度跳动；内容区内部滚动、页眉/导航不滚。
- **AC-38**（FR-134）：各页不再显示冗余大标题；当前页由导航高亮+标签页标题表明；PageHeader 组件统一控制。
- **AC-39**（FR-135）：framer-motion 入 package.json 并用于指定动效；prefers-reduced-motion 开启时相关动效全禁（守护测试通过）。
- **AC-40**（FR-136）：高级搜索可按类型/大小/时间/图库/分类组合；命中显示名与 EXIF（相机/镜头）；`camera:` 等表达式 token 生效。
- **AC-41**（FR-137）：可对媒体编辑备注并持久化；备注内容可被基础搜索命中。
- **AC-42**（FR-138，需真机走查）：时间轴无左侧竖轴、网格占满主区；右侧标尺 hover/拖动出日期+缩略图预览、点击跳转；旧左轴缺陷消失（取代 AC-24 左轴项）。
- **AC-43**（FR-139，需真机走查）：筛选/视图控件分区且滚动时冻结吸顶；分类筛选=标签筛选，内置收藏夹映射 favorite。
- **AC-44**（FR-140）：缩略图左下角常驻收藏按钮点击切换收藏，且不遮挡左上选择框/右下时长。
- **AC-45**（FR-141，需真机）：单日 1000+ 卡片滚动流畅（卡片级虚拟化）；缩略图请求尺寸随网格列宽/DPR 变化。
- **AC-46**（FR-142）：日期输入跳转到对应分组；切换年/月/日粒度后视口停留在原日期范围。
- **AC-47**（FR-143，需真机移动端）：窄屏时间轴条不易误触、下拉可刷新、筛选抽屉可用。
- **AC-48**（FR-144）：时间轴选择某图库后仅展示该库媒体（library_id 过滤）；「全部图库」恢复全量；含多选模式入口。
- **AC-49**（FR-145）：最近查看/往年今日可左右轮播；点击卡片跳到那天并按日期筛选时间轴；卡片提供播放入口。
- **AC-50**（FR-146）：分组头显示数量+主要设备+地点；「选当天/当月全部」可一键选中该日/该月全部媒体。
- **AC-51**（FR-147）：有 GPS 的媒体可解析出可读地名并在聚合/筛选展示；无 GPS 不显示地名。
- **AC-52**（FR-148）：文件名命中来源规则的媒体展示来源 App（如 微信/截图）；无命中不显示。
- **AC-53**（FR-149，需真机）：可在页眉上传文件；落到设置默认位置或上传时所选位置；按所选命名规则落盘；上传后自动扫描入库可见。
- **AC-54**（FR-150，需真机走查）：Browse 支持多标签开/关/新增/切换，每标签独立目录位置与筛选态。
- **AC-55**（FR-151）：重启/刷新后恢复上次打开的标签与浏览位置（后端 settings 持久化）。

## 7. 分期（路线）

各期只描述**主题 / 目标**；**具体哪个 FR 属于哪期，以 §4 FR 表的优先级 / 期列为唯一来源**——加需求即在表里标期，本节不重复列编号、不随 FR 增长而改。

- **第一期（MVP）**：核心媒体库 + 全格式转码播放 + 基础 Web 界面。实现多目录聚合、实时路径监控、FFmpeg 流式转码、HLS/TS 输出、mpegts.js 播放内核、边下边播追播机制、精准 Seek、硬件加速（Intel QSV + NVIDIA NVENC）、单用户认证、时间轴视图。
- **第二**期：SMB 网络共享支持 + 自适应码率（ABR）+ 文件目录视图 + 双进度条 + 字幕支持增强。
- **第三**期**：性能优化 + 更大规模媒体库支持 + 可选 PWA + 运维工具完善。
- **第四期（发布与分发工程）**：Go 模块社区化（`github.com/wcpe/JianVideo`）、GitHub Actions 多平台原生构建与自动发布（正式 release 按版本号打 tag + 普通 push 发 prerelease）、服务器端二进制自更新（拉取 GitHub release、校验 checksum、替换、自动重启、回滚）。
- **第五期（高级编码与自适应播放）**：硬件加速检测统一为实测真源 + 持久化缓存，补齐 AMD AMF 及全硬件家族（VAAPI/VideoToolbox/Vulkan）与 AV1/HEVC/VP9 探测；转码目标编码可配置；非 H.264 编码经 fMP4 + 浏览器原生 MSE 播放路径输出，按客户端能力自适应降级（H.264 维持 mpegts.js+TS 不变）。
- **第六期（界面与运维完善）**：可收缩左侧导航、系统信息与设置合并为 tab 化单页（参考宝塔）、环境变量查看与 ffmpeg 路径配置/检测、页脚版本与开源协议页、页眉更新提示，以及自更新体验修复（TTL 缓存 / 放宽超时 / 网络失败优雅降级 / release notes markdown 渲染）。
- **第七期（界面交互与管理完善 + 创新储备）**：系统诊断页拆子 tab 与运行环境补齐、版本与开源协议入口移入导航、应用更新交互完善（缓存优先 / 强制刷新 / 模态框确认）、运行期配置设置页热更新、存储库后缀列管与卡片网格、目录浏览多库聚合、时间轴刷新与拖动预览、列表多选与右键批量删除（FR-59~69）；并登记一批创新储备（感知哈希去重、离线聚类相册、那年今日回忆、媒体健康巡检、命令面板、观看统计、GPS 轨迹、转码预设队列、分享增强、外部播放器深链，FR-70~79，按需逐项启动）。同时修复亮色模式黑底（硬编码深色 token）与时间轴缩放，归真 FR-32 拖动、收窄 FR-58 跳转范围。
- **第八期（界面体验打磨与可用性增强）**：基于真机走查的体验优化批次——缩略图透明托底/比例自适应/骨架占位/多尺寸响应式、登录页与品牌打磨、左导航分组、主题切换过渡与 `dimmed` 对比度达标、播放页操作收纳与影院模式、移动端筛选折叠、设置页结构化编辑与分区、空态体验统一、代理连通性测试、自更新下载进度与重试、批量操作扩展（加相册/打标签/打包下载）（FR-81~91）。
- **第九期（界面设计系统与播放/图片体验）**：基于两轮设计走查（200+ 点）的体验大改——先落设计系统地基（token / 色板 / 组件规范），再做应用框架 / 动效 / 无障碍 / 空态统一、媒体卡与各浏览页重设计，以及播放（直接播放 / 全屏沉浸 / 播放器控件）与图片（灯箱交互 / 查看器 UI / 统一媒体查看器框架）体验。严格地基优先：FR-92~94 串行先落，再分波推进 FR-95~107（FR-92~107）。
- **第十期（账户安全与运维完善）**：账户安全与运维体验补齐——登录用户修改密码、首次初始化引导（取消 admin/admin 默认建号、由用户配置账号密码）、运行时调试日志开关与默认安静日志、发布说明源自 CHANGELOG（正式版读取 + 修预发布抽取标题不匹配）；并修在线更新「重启后失效」缺陷（FR-108~111 + 自更新持久化修复）。
- **第十一期（界面交互打磨与发布渠道修复）**：基于使用反馈的界面交互打磨与更新渠道修复——应用更新面板单按钮化与缓存展示规则、系统/设置页改一级 tab + 每 tab 左侧锚点导航、页眉刷新当前页、路由淡入仅内容区、导航收缩入口移底部右下与 logo 切换收展及 hover 补齐、左侧导航可拖拽调宽（FR-112~116）；并修测试版频道「一直有更新」（dev 版本号策略 + 后端比较逻辑，配套 ADR 取代/扩展 ADR-0032）与路由淡入范围缺陷。v0.19.0 续做真机走查打磨：版本号入页眉、协议入底/收缩态隐藏、收缩动画、按钮配色统一、锚点 sticky 与高亮死区/触底修复、运行环境复制按钮、通知堆积白屏修复。
- **第十二期（首页概览与数据可视化 + 浏览体验重做）**：把首页从时间轴改为系统总览看板（新增库存储/时长/类型聚合接口，时间轴移至 `/timeline`），统计页扩为「观看 / 媒体」分 tab 的结果 + 趋势可视化、新增系统资源实时监控页（指标采样持久化于 SQLite），时间轴重做为苹果「照片」式侧栏标尺悬停预览 + 最近查看，目录浏览重做为 Windows 资源管理器式真实路径树聚合（FR-117~121，引入 Recharts 图表库，配 ADR-0043 取代时间轴首页决策 / ADR-0044 指标采样 / ADR-0045 Recharts / ADR-0046 取代 ADR-0037 目录聚合）。
- **第十三期（工程质量门禁与 CI 自动化）**：把 `.claude/rules/static-analysis.md` 与 `testing-and-quality.md` 既定但尚未自动化的质量要求落到工具与 CI——Go 静态检查门禁（golangci-lint 全套 + make lint）、前端 Prettier 落地与 ESLint 告警清零、前端 Mock API 端点补全与关键浅测补强、单测覆盖率阈值门禁、Playwright E2E 接入 CI 并扩关键场景，最后由新增 `ci.yml`（PR 与 push main 触发）统一门禁、E2E 独立 job 失败挡合并（FR-122~128，首次在 CI 设质量门，配 ADR-0047；build.yml 发版职责不变）。`ci.yml` 首版在 Linux runner 存在多项环境 / 平台失败（依赖安装、lint action、go test 依赖、Node 20 缺 navigator、e2e 库目录），于 v0.20.2 逐项修复至 go-quality / web-quality / e2e 三 job 全绿。
- **第十四期（应用框架与导航壳层）**：补齐应用外壳与导航体验——动态标签页标题、导航新增「洞察」组（统计/监控）与紧凑化+动画、页眉居中全局搜索、全局统一固定高度消除布局跳动、统一 PageHeader 收敛冗余页面标题，并引入 framer-motion 动效库（FR-129~135，配 ADR-0048）。
- **第十五期（统一搜索）**：高级搜索面板与可搜字段扩展（显示名/EXIF/表达式 token），新增媒体备注字段与备注搜索（FR-136~137，扩 FR-35/36）。
- **第十六期（时间轴体验重构）**：时间轴导航形态重构（移除左轴、右侧唯一完整时间轴+预览、网格占满）、工具栏冻结+分类筛选、左下角收藏、大数据集性能、精准定位、移动端交互、图库筛选、回忆轮播+跳那天（FR-138~145，配 ADR-0049 取代 FR-120 左轴形态）。
- **第十七期（媒体聚合元数据）**：时间轴分组头聚合信息（数量/设备/地点）与选当天/当月全部，地名逆地理编码与来源 App 识别（FR-146~148，配 ADR-0050 逆地理编码）。
- **第十八期（上传与入库）**：Web 端上传入库——可配置默认上传位置与目录/文件命名规则、落盘后自动扫描入库（FR-149，配 ADR-0051）。
- **第十九期（目录浏览多标签）**：Browse 重做为 Windows 资源管理器式多标签，打开标签与上次位置持久化到后端（FR-150~151，扩 FR-121）。

> **分期不会堆到上百**：期是粗粒度路线图横轴，数量很少（走到成熟通常 3~6 个），一期含很多 FR、跨很多版本。**产品成熟（1.0 后稳态迭代）就不再加"第 N 期"**，改按版本（CHANGELOG / tag）+ 功能（FR 表 / specs）组织。

## 8. 术语表

| 术语 | 含义 |
|---|---|
| HLS | HTTP Live Streaming，Apple 提出的基于 HTTP 的流媒体协议，使用 m3u8 索引 + ts 切片 |
| TS / MPEG-TS | MPEG Transport Stream，传输流容器格式，用于 HLS 切片 |
| MSE | Media Source Extensions，浏览器提供的 JavaScript API，允许 JavaScript 向媒体元素注入媒体数据 |
| FFmpeg | 开源多媒体处理框架，用于视频解码、编码、转码、流媒体处理 |
| QSV | Intel Quick Sync Video，Intel 核显硬件加速编解码技术 |
| NVENC / NVDEC | NVIDIA GPU 硬件加速编解码技术 |
| VAAPI | Video Acceleration API，Linux 视频硬件加速接口 |
| ABR | Adaptive Bitrate Streaming，自适应码率流媒体，根据网络带宽动态切换码率 |
| GOP | Group of Pictures，视频编码中两个关键帧之间的帧组，影响 Seek 精度 |
| I 帧 | 帧内编码帧（关键帧），可独立解码，Seek 对齐的基准点 |
| SMB / CIFS | Server Message Block / Common Internet File System，Windows 网络文件共享协议 |
| fsnotify / inotify | Linux 文件系统事件监听机制（fsnotify 是 Go 的跨平台封装） |
| go:embed | Go 1.16+ 提供的将静态文件内嵌到二进制文件的指令 |
| WAL | Write-Ahead Logging，SQLite 的预写日志模式，提升并发读写性能 |
| mpegts.js | 支持 MPEG2-TS 实时流播放的 JavaScript 库，基于 MSE API |
