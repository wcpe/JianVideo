# 变更日志

本项目所有重要变更记录于此。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 未发布

### 变更

### 修复

## 0.25.0-rc.2（2026-07-22，候选发布）

### 修复

- 修复 `@jianvideo/render-pixi` 导出指向源码入口，使独立前端 typecheck 可解析网格会话类型。
- 修复 render-pixi lint：排除 benchmark 脚本、去除 `import()` 类型注解、模板字符串数字插值转字符串。

## 0.25.0-rc.1（2026-07-22，候选发布）

### 变更

- **P4 高密度媒体浏览器主路径（`0.25.x`）**：交付搜索筛选、元数据详情、批量转码/索引移动、剧集连播、Pixi 网格、图片编辑导出与视频粗剪导出。
- **FR2-014 发布入口简化为推送 tag**：推送 `vX.Y.Z-rc.N` / `vX.Y.Z` 后，CI 在固定 SHA 上跑四项质量门与双平台构建，再为**已有 tag** 创建 draft Release、回下载校验并公开；工作流不再创建 tag，也不再依赖 GitHub App / Ruleset / production 环境审批。
- **自更新候选频道归真**：原“测试版”频道收口为“候选版 RC”，只选择合法公开的 `X.Y.Z-rc.N` Release；历史滚动 `dev` Release 与 `dev` Actions 实验工件不再作为更新目标。
- **FR2-046 搜索筛选增强**：媒体列表支持 `duration_*` / `width_*` / `height_*` 筛选与 `duration`/`resolution` 排序；裸词搜索可命中同 Space 推断片名；时间轴/浏览页增加最短时长与最小分辨率控件。
- **FR2-032 详情元数据收口**：详情面板分区展示标签、离线影视推断与嵌入元数据；加载失败中文提示不阻断预览；点击标签可回写列表筛选。
- **FR2-053 批量转码与索引层移动**：新增 `batch-transcode` / `batch-move` API；前端批量条/右键/浏览工具栏可入队转码或改库归属（不搬磁盘文件）；删除/打标签/加相册/zip 行为保持。
- **FR2-047 播放列表与剧集连播**：新增 `GET /api/library/media/:id/next-episode` 与 `GET /api/albums/:id/neighbor`；播放页支持自动连播开关（localStorage）、合集上一首/下一首；合集页可顺序播放并带 `albumId` 上下文。
- **FR2-009 Pixi 高密度媒体浏览器**：`packages/render-pixi` 扩展生产网格会话（窗口化、纹理池、缩略图限流、hover 预览判定）；前端新增 `/media-grid` 媒体网格页接真实分页 API；附 `media-ui-target-1m` 窗口化 Benchmark 报告（`.tmp/`）。
- **FR2-038 图片编辑导出**：`POST /api/library/media/:id/image-export` 入队 `media.image.export`；ImageMagick 调色导出到 `exports/`，不改原文件；详情面板编辑器预览 + 重置 + 导出；产物 `GET /api/library/exports/:task_id/download`。
- **FR2-039 视频粗剪导出**：`POST /api/library/media/:id/clip-export` 入队 `media.video.clip`；ffmpeg 优先 stream copy、失败重编码；播放页/详情入口；原文件不变。

### 修复

- 修复省流量阻断与后端快照短暂滞后的播放状态竞态：核心仍保有播放意图时强制暂停底层播放器并停止加载，解除阻断后恢复播放。

## 0.24.0-rc.1（2026-07-19，候选发布）

### 新增

- **P3 播放体验**：共享播放页与 `packages/player-core` 支持暂停态逐帧、阶梯快进快退、播放刻度、清晰度选择、省流量、0.5–2 倍速、A-B 循环、画中画、Media Session 与移动手势。
- **FR2-029 时间轴预览**：新增 Space 隔离的 sprite/VTT 预览帧轨、generation 指针、可重建缓存、幂等生成与显式重建，桌面悬停与移动端长按均可定位预览。
- **FR2-044 字幕与多音轨**：新增统一轨道清单、稳定轨道 ID、外挂与上传字幕、内嵌字幕转换、真实音轨 HLS 重载及任务级资产隔离。
- **FR2-045 跨端续播与观看历史**：新增 `watch_states` 服务端真源、revision 并发控制、继续观看、观看历史和离开页面可靠刷盘。
- **FR2-060 章节与书签**：新增内嵌章节解析、Space 隔离的服务端书签真源、revision 并发控制与播放页跳转联动。

### 变更

- 前端播放、媒体详情、继续观看与最近播放统一使用共享 API 客户端和服务端状态，减少直接请求及重复状态维护。
- 播放控制按请求代次与播放源代次隔离，降低快速切源、并发 Seek、音轨切换和组件卸载时的迟到回调竞态。
- P3 九项功能规格保持实现与验收完成，PRD 发布状态同步为 `候选发布@v0.24.0-rc.1`；待 final RC 人工确认后再收口为正式 `v0.24.0`。

### 修复

- 收口真实逐帧画面身份、预览帧定位、字幕与音轨切换、停止加载、移动手势、清晰度与循环播放、章节书签 Space 隔离及并发竞态。
- 修复 SystemPage 测试中的异步时序抖动，稳定全仓质量门。
- 稳定播放器切源后的逐帧能力断言：等待播放核心能力与界面提示共同收敛，避免全量 E2E 偶发读取旧状态；生产播放器逻辑不变。
- 稳定 FR-43 视频分享下载 E2E：保留真实点击下载事件，改由同一浏览器上下文读取公开下载地址并将响应字节写入测试临时目录，避免 Windows Chromium 无法可靠等待视频附件完成事件；生产下载接口不变。

### 升级说明

- 启动时会通过内置追加迁移创建或升级时间轴预览、观看状态、章节与书签相关表及索引；迁移保持可重复执行，并为章节与书签补齐媒体删除级联约束。
- 时间轴预览、HLS 与音轨重载产物仍属于可重建缓存；升级与清理不会修改原媒体文件。
- Android 与 iOS 的原生播放体验仍按路线图留到 P7 收口，本版本交付范围为 Web 与安装态 PWA。

## 0.23.0（2026-07-13）

### 新增
- **FR2-026 多码率自动转码与自适应播放**：新增显式 `transcode.hls.abr` 通用任务与 `POST /api/play/:id/hls-abr`，默认从运行期 `transcode_abr_ladder` 读取 1080p/720p/480p 阶梯，按源分辨率跳过高档，低于 480p 时仅生成不放大的 `source` 档；扫描入库不自动触发高成本转码。单个 ffmpeg 进程通过 `filter_complex split` 生成共享 GOP 的 H.264/TS variants 与 `master.m3u8`，支持优先级、进度、取消、失败重试、硬件策略及软件回退；master 与逐档目录登记为 `cache_assets`，通用缓存清理或强制重建后可重新生成。播放页继续原文件直连优先，仅在直连失败且 `abr-h264` 已可用时切到 hls.js ABR，并展示实际 `LEVEL_SWITCHED` 档位；异步加载 hls.js 时增加失效令牌，避免媒体切换/卸载后残留初始化。新增 ladder/任务/API/缓存单测、前端事件与直连回退测试、串行 Playwright 专项，以及真实 ffmpeg/ffprobe 单二进制多档、source-only、清理重建和直连验收。
- **FR2-008 视频 HLS 预览与转码任务队列**：新增 `transcode.hls.preview` 通用任务类型，以 Space/media/profile 隔离产物和幂等键，支持优先级、进度、取消、失败重试、最多三次尝试与强制重建；旧 `POST/GET /api/transcode/tasks` 保持响应兼容，但运行期真源改为通用任务中心。H.264 preview 只生成一个 TS 质量档与单 variant master，高级编码沿用单档 fMP4/CMAF，明确不混入 FR2-026 多码率 ABR。新增 `GET /api/play/:id/hls-status`、显式 profile HLS URL 与旧 master/历史文件布局兼容；播放页保留 FR-53 高级编码协商，H.264 改为直连优先、加载失败后仅在已有 preview 时回退 HLS。HLS 产物按 profile 目录登记 `cache_assets`，缓存清理后可重新入队重建，强制重建只删除目标 profile。新增独立 migration 转换旧转码镜像而不改既有 migration ID，并补 Go 单元/迁移/API/缓存回归、前端回退测试及真实 ffmpeg Playwright 验收。
- **FR2-030 文件自带元数据解析**：新增 `media_metadata` 当前结果表及 `(space_id, media_id, source)` 唯一键；视频通过 ffprobe 保存容器、视频/音频/字幕流、帧率与标签，图片通过标准库和现有 `imagemeta` 能力保存 EXIF、IPTC、XMP 可解析子集。扫描新增媒体会幂等入队 `metadata.parse`，大小或 mtime 变化会标记 stale 并刷新；`metadata.backfill` 支持 checkpoint、进度和自动重试。新增查询、单文件刷新、批量回填 API 与详情面板基础展示，解析结果只写 SQLite，不修改原媒体；补齐真实多流/VFR/图片样本、单二进制验收和 SQLite 体积 benchmark。
- **FR2-059 智能封面/海报**：新增 `media_covers` 与 `cover_candidates` 可信选择模型，视频按 10%/30%/50%/70%/90% 时间点通过真实 ffmpeg 生成本地 JPEG 候选，图片复用现有缩略图生成能力；`cover.generate` / `cover.refresh` 经通用任务队列执行，产物按 Space/media/fingerprint 隔离并登记 `cache_assets(kind=cover)`。新增封面查询、生成、选择和候选图片 API，人工选择同时保存来源、时间点与指纹，缓存清理后保留选择语义并在重建时恢复新的 `selected_asset_id`；源视频失效时刷新失败且不静默换帧。详情面板支持生成、等待任务、选择候选，列表缩略图与播放器 poster 统一经现有缩略图路由优先展示当前封面。生成与选择写审计事件，补齐迁移/API/Space 隔离/缓存恢复单测、真实 ffmpeg/ffprobe 单二进制验收与串行 Playwright 专项。
- **FR2-048 存储与缓存管理**：新增 `cache_assets` 缓存资产真源、数据目录白名单校验、缩略图/HLS/图片代理/封面缓存登记点，以及封面类型和 `covers/` 白名单。新增缓存盘点、按类型统计、dry-run 与真实清理 API；dry-run 同步预览，真实盘点/清理只返回 `202 + task_id`，由通用 `WorkerRegistry` 后台执行、更新进度并响应取消/重试。清理只删除白名单目录下登记的可重建缓存，不触碰原媒体、数据库、WAL/SHM、审计或备份。HLS 按目录级资产聚合登记，segment 请求不同步写访问时间。前端 `/storage-cache` 页面会轮询任务终态后刷新统计；盘点/清理写安全审计。新增 Go 单元/集成测试、前端组件/API 测试、Playwright E2E 与缓存统计 benchmark。
- **FR2-007 存储库、Space 归属与数据库索引基线**：新增最小 Space owner 元数据迁移、`library_paths`/`media_files`/`tags`/扫描任务/预生成任务的 `space_id` 字段与媒体查询组合索引；后端接入 `X-JianVideo-Space-Id`，媒体列表、详情、目录浏览、统计、扫描入口、标签、回收站、上传和任务状态按 Space 隔离，旧 `page/page_size` 响应同步返回 `next_cursor`。补齐 P2 owner-only 权限闭环：统一守卫要求当前认证用户匹配 `spaces.owner_user_id`，非 owner 不可读、写或列举该 Space 资源，HLS/stream 直出路径同时校验 owner 与媒体归属，审计显式 Space 查询按实际目标授权；设置页新增“存储与 Space”区块，展示当前 Space、数据目录、SQLite 索引库路径和已注册目录数，并可进入媒体库管理页维护目录。新增媒体查询 repository、cursor 编解码测试、owner/非 owner 权限测试、跨 Space 集成测试与 FR2-007 SQLite benchmark harness，报告输出到 `.tmp/benchmark/fr2-007/`。媒体分页改用 tuple cursor，并通过移除干扰规划器的宽泛删除索引、增加删除态专用 partial index，使 1000 万资产下三类目标查询 p95 降至 5.297ms、8.119ms、0.551ms，且无临时 B-Tree 排序。
- **FR2-017 v0.20 到 v2 数据迁移与升级安全（已验收）**：新增 `internal/migration` 版本化 schema 迁移框架，启动期以 migration registry 替代无版本记录的全局 `InitSchema + AutoMigrate`。Runner 在备份和写入前执行只读预检：settings 已登记非法值与未知高风险 key 形成 blocker，普通未知历史 key 形成 warning 并原样保留，诊断不输出设置值；单步 `Up + Validate + succeeded 状态` 在同一事务内提交，任一失败整体回滚并可靠记录失败。迁移前用 SQLite `VACUUM INTO` 在数据库同目录 `backups/` 创建一致性快照并执行 `PRAGMA integrity_check`；失败保留备份，支持停服恢复。真实 v0.20 fixture 覆盖用户、媒体库、媒体、settings、相册、标签、扫描/转码任务、分享、健康问题和自定义后缀；旧任务以 `scan:<id>` / `transcode:<id>` 幂等映射到通用任务，重复执行不新增重复项。新增备份恢复、原媒体 hash/mtime 不变及 Go 单二进制从旧库启动迁移测试。`-migration-dry-run` 已实现 JSON 计划输出、warning 成功退出、blocker 非零退出及无写入/不启动服务保证。`.tmp/benchmark/fr2-017/` 的 1m/5m/10m 实测中，完整迁移分别为 57.197s/216.203s/596.426s，备份分别为 2.526s/8.540s/18.245s，备份与主库 integrity、迁移前后计数、媒体指纹均通过，关键索引无缺失。
- **FR2-022 外部工具自动下载与代理**：新增 `internal/tools` 工具源 registry、受控安装器与 `tool.download` 系统级任务，支持 ffmpeg/ffprobe/ImageMagick 自定义本机测试源下载、SHA-256 校验、zip/tar.gz 安全解压、路径穿越与链接条目拒绝、版本探测、进度上报、取消/重试和成功后热应用 `ffmpeg_path`/`ffprobe_path`/`magick_path`。内置源接入 `tools-v1.0.0` GitHub Release 的 Linux、Windows、macOS 双架构可信工具包，按当前运行平台返回 ffmpeg/ffprobe/magick 三个带固定大小与 SHA-256 的可下载源，并拒绝错配平台或架构。新增 `/api/system/tools`、`/api/system/tools/sources`、`/api/system/tools/download`，代理测试支持可选 `target`，设置页工具路径区可发起下载并轮询任务状态。
- **FR2-024 配置分层边界**：新增 `internal/settings` 类型化 registry 与 `GET /api/settings/definitions`，`PUT /api/settings` 只允许已登记运行期 key 并在写入前做类型校验；`GET /api/settings` 回读已登记运行期设置和默认值，敏感项只返回存在性，不回显代理凭据。设置页改用 definitions 渲染标签和说明，保存其他设置时不会回写隐藏代理；配置变更审计事件仅记录脱敏前后值。
- **FR2-025 媒体类型与扫描后缀可配置**：新增 `media_type_rules` 规则真源、内置媒体类型 registry、`/api/media-types` 与规则增删改 API；扫描、列表 `type=` 筛选、统计、缩略图、上传校验和图片 raw 入口统一使用同一类型规则。旧 `/api/library/extensions` 保持兼容并映射到新规则服务，迁移会把旧自定义后缀回填为每库规则；前端存储库管理页展示中文类型说明、能力标签，支持新增自定义规则、删除自定义规则和禁用内置规则。
- **FR2-027 定期扫描、增量更新与目录事件监听**：新增扫描变更模型与 `file_state=missing` 状态；watcher、SMB 轮询、手动扫描和定时扫描统一进入扫描任务队列，变更任务 payload 可持久化恢复。全量对账改为按游标分批检查源文件是否存在，缺失文件从常规列表隐藏但不进入回收站；旧扫描 API、扫描任务列表和通用任务镜像保持兼容。
- **FR2-028 分档缩略图生成**：新增 `thumbnail.generate` 与 `thumbnail.backfill` 通用任务处理器，按需请求高优先级幂等入队，批量任务按 checkpoint 分批处理并支持重试/进度。160/320/640 三档产物按 `thumbnails/{spaceID}/{mediaID}/{size}.jpg` 隔离并登记 `cache_assets(kind=thumbnail)`，缓存清理后可重新生成；盘点不会跨 Space 或清空既有关联。缩略图 ffmpeg 路径消费启动发现、持久化设置和工具下载结果，前端按浏览器实际 `srcSet` 档位执行 `probe=1` 的 202 轮询。FR2-059 接入后，同一路由会优先返回当前智能封面，封面缓存缺失时保持原有分档缩略图回退行为。新增 backfill API、Go 单元/集成测试、真实图片/视频 Playwright 截图验收和 Go 单二进制清理重建验收。
- **FR2-037 通用异步任务队列中心**：新增 `tasks` 通用任务真源、状态机、重试退避、优先级公平领取、worker registry、Space/System scope、审计事件和 `/api/tasks` 列表/统计/详情/取消/重试 API；旧扫描队列与预生成转码队列同步到通用任务表，旧状态 `completed/error` 在边界层映射为 `succeeded/failed`。前端新增「任务」页，支持状态/类型/资源过滤、统计卡、取消和重试；`packages/media-client` 与 MSW mock 对齐通用任务状态。新增 FR2-037 SQLite benchmark harness，报告输出到 `.tmp/benchmark/fr2-037/`。
- **FR2-040 全操作审计日志**：新增 `internal/audit` 审计事件真源、脱敏工具、cursor 分页查询 API 与系统控制台审计 tab；配置、媒体库、媒体删除/还原/改名/移动、元数据回写、扫描/转码任务、缓存清理和迁移事件写入 `audit_events`。关键业务变更与审计事件同 SQLite 事务提交，审计失败时回滚业务变更，Space scoped 查询默认不返回系统级事件。
- **FR2-052 多媒体库分型**：`library_paths` 新增 `library_kind` 与 `library_profile_json`，旧库迁移默认 `mixed`；`GET /api/library/kinds` 返回 `movie/series/home_video/mixed` 的中文说明、命名提示与推荐扫描策略，库路径创建/更新支持分型校验与回读。扫描上下文携带 `library_kind`，媒体库管理页新增分型选择并区分来源类型与内容分型，MSW mock 与前端类型同步。
- **FR2-056 硬件转码加速管理面板**：新增 `transcode_hwaccel_mode` 与 `transcode_hwaccel_fallback` 运行期设置，支持自动、软件、NVENC、QSV、AMF、VAAPI、VideoToolbox 策略与硬件失败软件回退；转码预生成、播放协商与预切片执行器按策略选择 ffmpeg encoder，指定硬件不可用且关闭回退时返回明确中文错误。系统控制台硬件加速 tab 新增策略分段控件、回退开关、保存与强制重测按钮，强制重测写入系统级 `codec_probe.retested` 审计事件。能力缓存键纳入 FFmpeg 版本、实际路径、文件元数据与内容摘要，避免工具热切换后复用同版本其他构建的旧探测结果。
- **FR2-061 文件级/哈希去重**：`media_files` 新增 SHA-256 内容哈希字段、计算时间、stale 标记与 `(space_id, file_size, content_hash)` 组合索引；`media_hash_groups` 保存 Space 内重复组快照，精确查询会回连 `media_files` 复验软删、missing 与 stale 状态。扫描增量变更会在大小或 mtime 变化后置 stale，回填通过 FR2-037 任务 `library.file_hash_backfill` 流式计算源文件内容 hash。新增 `POST /api/library/file-hashes/backfill` 与 `GET /api/library/duplicates/exact`，旧 dHash 去重保留为 Space scoped 的「相似重复」；重复项页区分「精确重复 / 相似重复」，两类处理都复用批量软删并写审计，不物理删除源文件。Benchmark 报告输出到 `.tmp/benchmark/fr2-061/`。
- **FR2-031 本地离线影视信息推断**：新增 `media_inferences` 推断真源、movie/series/mixed/home_video 本地规则解析、全局与按库关闭配置、扫描入库与 backfill 接入、查询/人工纠正 API 以及播放页详情抽屉编辑入口。人工纠正优先并写审计事件，backfill 不覆盖人工值；展示名按「人工纠正 > 库内显示名 > 高置信自动推断 > 原始文件名」决策，低置信候选不自动替换显示名。设置变更后会自动为已有媒体补齐缺失推断；时间轴新增全局已推断/未推断筛选并展示推断标题。

### 修复
- **FR2-031 离线影视推断复核阻断**：媒体列表现已接受 `inference=inferred` 并返回自动推断与人工纠正的并集、排除未推断媒体；媒体落库后的即时推断若失败，会持久化单媒体补偿任务并唤醒通用 worker，后续自动补齐且不改变人工纠正、全局关闭或按库禁用语义。
- **FR2-022 可信工具镜像发布**：修复原子发布阶段的资产列表解析语法错误，以及回下载请求混用 JSON/二进制 `Accept` 头导致 `SHA256SUMS` 下载为错误响应的问题；六平台工具包已通过来源证明与回下载校验并正式发布为 `tools-v1.0.0`。
- **服务退出清理与扫描职责边界**：主入口改为在 `run` 返回后再退出进程，确保播放服务、指标采样、任务 worker、扫描调度与目录监听的 defer 清理完整执行；移除扫描完成后自动全库预切片死链，保持扫描只负责入库、高成本 HLS 由显式任务触发。

### 工程 / 质量
- **静态检查与覆盖率门禁收口**：清零 32 个 golangci-lint 问题，补齐资源关闭、导出注释和精确 gosec 信任边界；Go 覆盖率默认门槛为 60%，组合根与 SMB 的显式例外集中声明并由策略测试锁定。
- **Playwright E2E 发布门禁**：真实服务端到端测试固定单 worker 串行执行，并在每次启动前重建专用数据根，避免共享 SQLite、任务队列和运行期设置造成跨场景互扰；`pnpm run quality` 现已包含完整 Playwright E2E。
- **控制台异步测试稳定性**：设置区异步加载断言使用 5 秒等待窗口，连续复验与全量前端测试通过，消除正式门禁中的时序抖动。

## 0.22.0（2026-07-08）

### 新增
- **FR2-006 API client 与多端基础**：`packages/media-client` 新增 Space 感知 API client、timeout/retry、统一 query key、媒体分页/详情查询、任务详情查询、旧任务状态 `completed/error` 兼容映射与端能力检测；`packages/mock` 新增最小 v2 mock fetch/handlers，覆盖媒体列表、详情、任务轮询与 Space 切换；`packages/theme` 消费端能力结果决定密度，`apps/wiki` 增加 client demo 展示列表、详情、分页、任务轮询和 Space 切换。
- **Wiki UI 博物馆与 mockup 先行（FR2-005）**：`apps/wiki` 增加可交互的 UI 博物馆骨架，支持搜索、分组、场景切换、状态切换和 `@jianvideo/*` 代码片段展示；`packages/ui` 暴露首批控件预览描述，`packages/theme` 暴露主题 / 密度配置，`packages/mock` 补齐 HLS、缩略图、转码任务、AI 任务与 Space 权限 mock 场景，并增加敏感信息扫描测试。
- **FR2-063 PixiJS 与 Benchmark 最小原型切片**：`packages/mock` 新增 100 万 / 500 万 / 1000 万确定性 mock 索引数据源与窗口查询；`packages/render-pixi` 新增网格窗口、纹理池 LRU、真实 `pixi.js` 预览挂载、HLS 预览触发判定与渲染指标快照；`packages/benchmark` 新增 FR2-003 前端 / 后端阈值判定、Go/SQLite 真实索引查询 harness 与 `.tmp/benchmark/fr2-063/` Markdown summary 输出；`apps/mock-studio` 增加 FR2-063 Benchmark 页面、真实 PixiJS 预览画布、Canvas 非空 E2E 验证入口与 hover/选中触发的 HLS 预览请求计数。

## 0.21.0（2026-07-08）

### 文档 / 工程
- **v2 规格冻结与技术基线（P0/P0.5）**：从 v0.21.0 起进入 v2「自托管视频素材与 AI 媒体中心」路线。归档旧 PRD，重建 v2 PRD/ROADMAP 与功能登记册（FR2-001~063）；冻结 apps/packages 工作区、前端技术栈与最严静态检查门（ADR-0052~0054）；补齐通用任务队列、Space 权限、player-core、数据层可移植、AI 管线与迁移方案等架构决策（ADR-0055~0060）；给出大库性能预算、旧能力兼容矩阵、UI 博物馆与 P1 原型规格。本版为文档与规格基线，不改变当前运行时行为。

## 0.20.3（2026-07-07）

### 修复
- **浏览器标签页 favicon 不显示 / 发虚**：原 `favicon.svg` 含 16 个高斯模糊滤镜 + 蒙版且 viewBox 非正方形，在标签页 16×16/32×32 极小尺寸下糊成一团或被部分浏览器丢弃。改为去滤镜的扁平折角播放标识（正方形画布），并补 `favicon-32x32.png` / `favicon-16x16.png` 多尺寸 PNG 回退、在 `index.html` 增加对应 `<link rel="icon" sizes>` 引用，确保主流浏览器标签页清晰显示品牌图标。（待真机浏览器验证标签页图标）
### 新增
- **目录浏览多标签（FR-150，参考 Windows 资源管理器）**：目录浏览页顶部新增标签栏，每个标签是一个独立浏览会话——自带目录位置与排序 / 筛选态，可新建（「+」）、关闭（始终保留至少一个）、点击切换；标签标题取当前目录末段（根标签显示「全部」）。标签状态由前端 store 持有，切换标签以会话整体重挂实现，互不串扰；单标签时与原资源管理器布局行为等价。（待真机走查多标签开 / 关 / 切换与各标签独立性）
- **浏览标签与位置持久化（FR-151）**：打开的标签列表与上次浏览位置经后端 settings 持久化（复用 `open_tabs` / `last_opened_path` 键，经 GET/PUT `/api/settings` 读写，零后端改动）——目录浏览页挂载时恢复上次打开的标签并定位上次激活标签，标签结构或位置变化时防抖写回；恢复完成前不写回，避免空写覆盖。（待真机验证重启 / 刷新后恢复）
- **Web 上传媒体入库（FR-149，[ADR-0051](docs/adr/0051-upload-ingestion.md)）**：页眉新增上传按钮，打开上传弹窗（选文件 / 拖拽 + 逐个进度 + 目标位置与命名规则选择）；新增 `POST /api/library/upload` multipart 端点，文件流式落盘到「上传目标位置」后自动入队该库增量扫描入库（保持「磁盘文件为入库真源」，`media_files` 仍由扫描填充）。目标位置可在设置「上传」分区配置默认值（须为已注册本地库目录）、上传时可临时指定；命名规则两档：「保留原样」直接落目标目录、「按日期整齐归档」分 `YYYY/MM` 子目录。校验目标须在已注册本地库目录内（防越权写库外，拒 `..` 逃逸与 SMB 库）、仅接受图片 / 视频、重名按 `名(N).ext` 避让。（需真机验上传落盘与扫描入库链路）
- **时间轴分组头聚合信息与组级全选（FR-146，扩 FR-138/FR-31）**：时间轴日期分组头由「日期 + 数量」升级为聚合信息——数量（组内条数）+ 主要设备（组内最常见相机型号）+ 地点（组内最常见逆地理编码「省·市」地名），设备 / 地点无数据时对应维度不显示；选择启用且为日 / 月粒度时，分组头附「选当天全部 / 选当月全部」组级全选入口，一键选中该日 / 该月全部已加载媒体（复用现有多选基建，新增 `selectIds` 并入语义）。地点数据由 GPS 逆地理编码接入富化链——扫描 / 富化媒体时若有 GPS，就近解析「省·市」写入 `media_files.location`（纯增量、不覆盖其它字段），无 GPS 则空。聚合（数量 / 最常见设备 / 最常见地点）与地名解析均为纯函数、含穷举单测。（待真机走查）
- **按文件名启发式识别媒体来源 App（FR-148）**：新增 `internal/library` 来源识别纯函数，按文件名命名模式推断媒体来源（截图 / 微信 / QQ / 相机 等），命中规则返回来源标识、无命中返回空（前端不显示）。规则以有序表组织、大小写不敏感、仅按基名判定，便于后续扩展；本期仅交付后端能力 + 单测，聚合展示见后续 FR-146。
### 新增
- **GPS 逆地理编码后端能力（FR-147，[ADR-0050](docs/adr/0050-reverse-geocoding.md)）**：在 `internal/library` 新增离线逆地理编码服务（`Geocoder` 接口 + `NewOfflineGeocoder` 实现），由 GPS 经纬度就近匹配内置粗粒度城市锚点表（中国主要省会 / 直辖市），解析为「省·市」可读地名（如「浙江·杭州」），供后续聚合 / 筛选展示（前端展示见 FR-146）。离线方案不联网、不外泄坐标；无 GPS、坐标越界或超出就近半径（150km）时不解析、不臆造地名。`models.MediaFile` 预留 `location` 字段（AutoMigrate 自动建列、向后兼容）。坐标→地名映射与 Haversine 距离均为纯函数，含穷举单测。
### 新增
- **高级搜索与可搜字段扩展（FR-136）**：基础裸词搜索由仅匹配文件名扩展为同时匹配 文件名 / 显示名 / 相机 / 镜头（多列 OR，全参数化无注入面）；表达式新增 `camera:` / `lens:` token 精确约束对应 EXIF 列；搜索框补充语法帮助浮层与更新占位提示，说明可搜字段与全部表达式 token。
- **媒体备注与备注搜索（FR-137）**：`media_files` 新增 `notes` 备注字段（GORM AutoMigrate 自动建列）；播放页「详情」抽屉新增备注编辑入口，保存后持久化（去首尾空白、留空即清除），新增 `PUT /api/library/media/:id/notes` 端点；备注内容纳入基础裸词搜索（与文件名/显示名/相机/镜头同列 OR，全参数化无注入面）。
### 新增
- **动态文档标题（FR-129）**：新增统一 `useDocumentTitle` hook，由 AppLayout 按当前路由解析页名（复用导航 navItems 作为页名真源，播放 / 开源协议页补映射），设浏览器标签页标题为「<页面名> - JianVideo」；未知路由回退「JianVideo」。
- **统一 PageHeader 组件并隐藏各页冗余标题（FR-134，扩 FR-95）**：新增统一 `PageHeader` 组件收敛各页冗余的 `<Title order={2}>页名</Title>` 大标题，默认视觉隐藏标题（保留无障碍可读文本）、右侧操作区常驻；时间轴 / 目录浏览 / 相册等 12 个页面统一接入，当前所处页面由左侧导航高亮 + 浏览器标签页标题（FR-129）表明，少数确需视觉标题的页面可经 `showTitle` 显式开启。
- **引入 framer-motion 动效库（FR-135，[ADR-0048](docs/adr/0048-framer-motion.md)）**：引入 `framer-motion` 承担纯 CSS 难做的复杂动效（列表进入 / 共享元素过渡，复杂编排留后续期）；本版仅做引入 + 最小封装 `MotionFade`（淡入 + 轻微上移），并强制遵守 `prefers-reduced-motion`——开启「减少动态」时完全不下发任何动画（无障碍守护测试覆盖）。常规 hover / 过渡仍用既有 CSS。

### 变更
- **回忆轮播与点击跳转那天（FR-145，扩 FR-72/FR-120/FR-44）**：时间轴「最近查看」「那年今日」回忆区由原生溢出滚动升级为横向轮播——统一 `MemoryCarousel` 容器提供左右滚动按钮（到边界即禁用）+ CSS `scroll-snap` 卡片对齐停靠；卡片交互重构为「主体点击跳到那天」——点击卡片主体导航到 `/timeline?date=YYYY-MM-DD`，时间轴页读取该 URL query 初始化：首屏即按当天区间筛选（下界当天零点、上界次日零点含整天）并滚动到该日分组；卡片右上角新增独立播放入口，点击直达播放页（不触发跳转）。无有效媒体日期的卡片退化为直接播放。（待真机走查）
- **时间轴移动端交互优化（FR-143，扩 FR-86/FR-45）**：窄屏（≤48em）下时间轴右侧标尺由 12px 可拖动细轨（屏幕边缘易误触）切换为防误触形态——隐藏细轨、改竖排可点击年份标签快速导航（点年份跳到该年最新分组）；新增窄屏下拉刷新——页面顶部向下拖动超阈值松手即重载首屏（带橡皮筋阻尼与「下拉 / 松手刷新」指示，非顶部 / 拖动不足 / 桌面均不触发）；移动端筛选抽屉底部新增「重置筛选」一键入口（无筛选生效时禁用，重置后关闭抽屉回到全部结果）。响应式经 Mantine `visibleFrom` / `hiddenFrom` 与 `useMediaQuery` 区分窄屏 / 桌面，桌面形态不变。（待真机移动端走查）
- **时间轴日期跳转与粒度切换视口锁定（FR-142，扩 FR-32/FR-120）**：时间轴工具栏「视图」组新增「跳转到日期」输入框，输入 `YYYY` / `YYYY-MM` / `YYYY-MM-DD`（接受 `/` 或 `-` 分隔）回车即定位到对应分组——按年 / 月查询落到该区间内最新一组，按日查询落到覆盖该日的分组；非法输入提示并不跳转。切换年 / 月 / 日 / 所有缩放粒度时视口锁定不漂移：切换前记录视口顶部分组日期、切换后据其重新定位（「所有」为单组不锁定）。日期规整与「分组下标解析」均为纯函数并含穷举单测。（待真机走查）
- **时间轴卡片级虚拟滚动与缩略图尺寸自适应（FR-141，扩 FR-120/FR-E）**：时间轴虚拟化由「按日期组」细化到「卡片级」——把日期分组展平为「分组头行 + 按列切片的网格行」一维行列表，对网格行做窗口虚拟化，单日上千张卡片也只渲染视口附近的若干网格行（不再整组全量渲染）；网格列数随容器真实宽度自适应（ResizeObserver 观测），缩略图请求尺寸按「单卡列宽 × 设备像素比」从 160/320/640 白名单就近选档，高 DPR 屏自动取更大档、低分屏不浪费带宽。右侧时间轴 scrubber 仍按分组跳转（映射到该组头行下标）。（待真机走查）
- **缩略图左下角常驻收藏按钮（FR-140，扩 FR-41/FR-99）**：时间轴缩略图卡片新增左下角常驻可点收藏按钮（复用 `PUT /api/library/media/:id/favorite`），无需 hover 即可一键收藏 / 取消；与左上选择框、右下时长角标、右上 hover 操作浮层四角避让互不遮挡；点击只切换收藏不打开卡片。原 hover 浮层内的收藏按钮移除（避免重复入口）。（待真机走查）
- **时间轴工具栏冻结吸顶与分类筛选（FR-139，扩 FR-100/FR-41）**：时间轴顶部把搜索 + 内容筛选 + 视图控件聚合为单一工具栏，并在页面滚动时 `sticky` 冻结吸顶（随全局固定高度布局），下方继续观看 / 最近查看 / 那年今日等回忆区改置于工具栏之下随内容滚动；内容筛选以新增「分类」入口承载——复用现有标签（FR-41，API 已支持 `tag_id`）每个标签即一个分类，并内置映射 `favorite` 的「收藏夹」分类、「全部」清空，三态互斥。（待真机走查）
- **时间轴导航形态重构（FR-138，[ADR-0049](docs/adr/0049-timeline-navigation-redesign.md)）**：移除时间轴左侧竖向日期轴，日期分组改为顶部横向轻量分组头、媒体网格占满主区；右侧标尺升级为唯一完整时间轴导航条——常驻年份刻度，hover / 拖动弹出「日期 + 数量 + 缩略图九宫格」预览浮层，点击 / 松手跳转到对应分组。取代 FR-120 / FR-B「左竖轴 + 右标尺」并存形态。（待真机走查）
- **全局统一固定高度布局（FR-133，扩 FR-95）**：页面滚动容器（html/window）始终预留纵向滚动条宽度（`scrollbar-gutter: stable`），消除内容多（出滚动条）与内容少（无滚动条）页面间的横向位移——概览↔时间轴↔目录切页不再横向跳动；播放页全屏沉浸态（已禁滚动）复位为 `scrollbar-gutter: auto` 避免右侧留出无用空槽。
- **页眉居中全局搜索框（FR-132，扩 FR-74/FR-35）**：页眉中部新增居中全局搜索入口（窄屏隐藏，移动端仍走时间轴页内搜索框），输入关键词回车即跳转时间轴页并经 URL `?search=` 承接，搜全部媒体（名称 / 路径 / 显示名，复用 FR-35 媒体搜索真源）；空白关键词不跳转避免空搜索；新增「/」快捷键聚焦搜索框（在其它输入框内打「/」不误触），与命令面板 Ctrl/Cmd+K 协同。
- **导航紧凑化与项进入动画（FR-131，扩 FR-54/FR-115/FR-96）**：导航项密度收紧——内边距 `p="xs"` 改为 `.nav-link` 显式 `padding:4px 8px`、项间距 `gap` 8→6、组内 `gap` xs→4、标签字号 sm→xs；新增导航项进入淡入动画（`jianvideo-nav-item-in`），与既有展开/收缩宽度过渡协同；`prefers-reduced-motion` 下进入动画与过渡均关闭（无障碍守护测试覆盖）。
- **左侧导航分组重构（FR-130，扩 FR-83/FR-55）**：导航由「浏览 / 管理 / 系统」三组重构为「浏览 / 洞察 / 管理」三组——新增「洞察」组承载统计（自浏览移出）+ 监控（自系统移出），原独立「系统」组并入「管理」组（系统为末项）；库管理导航项标签由「管理」改为「库管理」以避免与组标题歧义。各项路径 / 语义与命令面板（FR-74）扁平真源不变。
- **时间轴媒体库筛选下拉（FR-144，扩 FR-06/FR-100）**：时间轴工具栏筛选区新增按媒体库（图库）筛选下拉——「全部图库」+ 各启用图库，选某库即按后端 `library_id` 过滤仅展示该库媒体，「全部图库」恢复全量；并提供「多选模式」入口（资源管理器式图库切换）以复选框列出各库，恰勾选一个库按该库过滤、未选 / 多选回退全量（后端当前仅支持单 `library_id`）。图库列表复用 `GET /api/library/paths` 真源，不另起数据源。（待真机走查）

### 工程 / 质量
- **清理未使用的 Tailwind 依赖（P14）**：移除 `frontend` devDependencies 中从未实际使用的 `tailwindcss` 与 `@tailwindcss/vite`（vite 配置早已无 tailwind 插件、源码无相关 import），纯技术债清理，build 通过、无视觉回归。

## 0.20.2（2026-06-29）

### 修复
- **CI 质量门工作流在 Linux runner 的多项失败（FR-128，[ADR-0047](docs/adr/0047-ci-quality-gates.md)）**：`ci.yml` 自落地起在 GitHub Actions（Ubuntu / Node 20）从未跑绿，本版逐项修复至 go-quality / web-quality / e2e 三 job 全绿：
  - **依赖安装**：`npm ci` 因 package-lock 在 Windows 生成、缺 Linux 平台可选依赖（`@emnapi/*` 等 napi WASM 垫片）在 Linux runner 严格校验失败，统一改 `npm install` 按目标平台解析（与 build.yml 既定决策一致）。
  - **Go 静态检查**：`golangci-lint-action` 升 v6→v7 适配 golangci-lint v2；并去掉 `args` 中重复的 `run` 子命令（v7 已自动注入，致 `run run ./...` 把多余 `run` 当路径报错退出码 7）。
  - **Go 测试**：go-quality job 安装 ffmpeg（transcoder 版本键控缓存测试依赖 `FFmpegVersion`，缺 ffmpeg 时版本为空、缓存判定按设计失效）；库 `BrowseDirectory` 的 Windows 路径用例改用规范化正斜杠存储（`filepath.ToSlash` 在 Linux 不转换反斜杠，致 `D:\` 路径前缀查询在 Linux 落空），跨平台可在 Linux 运行。
  - **前端覆盖率门**：vitest（environment=node）测试 setup 显式挂载 `navigator`（react-dom 求值期读取，该全局 Node 21+ 才有，CI 的 Node 20 缺失致 70 个组件 / 页面测试套件加载即抛 `navigator is not defined`）。
  - **E2E**：Playwright webServer 启动前创建 `.tmp` 隔离库目录（`.tmp` 被 gitignore、CI 全新检出不存在，SQLite 不自建父目录致 go 服务启动报 `unable to open database file`）。
  - 均为测试基建 / CI 配置修复，无业务代码变更、无新依赖、无新 ADR。

## 0.20.1（2026-06-29）

### 工程 / 质量
- **Go 静态检查门禁（FR-122，[ADR-0047](docs/adr/0047-ci-quality-gates.md)）**：新增 `.golangci.yml`（golangci-lint v2，启用 govet/staticcheck/errcheck/ineffassign/revive/bodyclose/sqlclosecheck + gofmt/goimports formatters）与 `make lint` 目标（一次跑齐格式 + 静态检查、只管 Go），并清零全部存量告警（errcheck/revive/staticcheck/unused/bodyclose/ineffassign 共 106 处）。落地 static-analysis.md 既定 Go 工具链。
- **前端 Prettier 落地与 ESLint 告警清零（FR-123）**：引入 prettier + eslint-config-prettier，新增 `format`/`format:check`/`lint:fix` 脚本，eslint 配置接入 eslint-config-prettier 关闭冲突格式规则；全量 `prettier --write` 格式化前端代码（纯格式、行为不变）并修复全部 eslint 告警（含将组件内非组件导出拆到 `*.helpers`）。
- **前端 Mock API 端点补全（FR-124）**：审计 `frontend/src/api` 全部后端端点，补齐 MSW handlers 至 100% 覆盖（补 21 个端点 + HLS master 探测回退），测试 `onUnhandledRequest` 由 `warn` 改 `error`，强制零未处理请求。
- **前端关键浅测补强（FR-125）**：审计 44 个无交互测试，确认绝大多数已是行为/逻辑测试，补 MetricCard「末两点持平」条件渲染断言（精准、无镀金）。
- **前端测试覆盖率阈值门禁（FR-126，[ADR-0047](docs/adr/0047-ci-quality-gates.md)）**：引入 `@vitest/coverage-v8`，vitest 配 `thresholds`（lines/statements 75、functions 60、branches 75，按实测务实定档），覆盖率不达标即失败；`coverage` 报告产物加入忽略。
- **前端 E2E 扩关键场景（FR-127）**：复用 Playwright，新增目录浏览 / 设置 / 播放页加载关键流程 spec 与 `ensureSetup`/`login` 公共前置（适配 FR-109 初始化引导），并修正既有 E2E 因 FR-109/FR-117/FR-95 漂移的失效断言。
- **CI 质量门工作流（FR-128，[ADR-0047](docs/adr/0047-ci-quality-gates.md)）**：新增 `.github/workflows/ci.yml`（PR + push main 触发），统一执行 Go（golangci-lint + go test）、前端（lint + format:check + 覆盖率门禁）、E2E 三道质量门，任一失败挡合并；`build.yml`/`release.yml`/`prerelease.yml` 发版职责不变。

## 0.20.0（2026-06-28）

### 新增
- **首页概览数据看板（FR-117，[ADR-0043](docs/adr/0043-homepage-overview-dashboard.md)）**：根路由 `/` 由时间轴改为「概览」系统总览看板，时间轴迁至 `/timeline`（导航「浏览」组并列「概览」「时间轴」两入口）。看板含 KPI（媒体总数 + 视频/图片拆分、视频总时长、占用空间、媒体库数、相册数）、媒体构成与各库分布、观看概览（跳 `/stats`）、系统状态（FFmpeg/硬件加速/运行时长/内存 + 巡检问题数）、任务队列（扫描进度 + 转码计数）、继续观看，各数据源独立降级、空库零值不崩。新增后端聚合端点 `GET /api/library/summary`（一次 `GROUP BY` 取总量/视频图片拆分/`SUM(file_size)`/`SUM(duration)`/各库明细，视频图片口径复用 `builtInImageExtensionList()`、全程 `deleted_at IS NULL`、避免 N+1）；前端新增 `OverviewPage` 与 `api/summary.ts`，并把 `formatBytes`/`formatUptime` 抽到 `utils/format.ts` 去重。修订 AC-13 + 新增 AC-21。后端 6 单测、前端 OverviewPage 8 组件测试，受影响前端全量测试与生产构建通过。机制见 [docs/specs/overview-dashboard.md](docs/specs/overview-dashboard.md)。
- **统计页内容趋势扩展（FR-118，扩 FR-75，[ADR-0045](docs/adr/0045-recharts-charting-library.md)）**：`/stats` 顶部分「观看 / 媒体」两 tab（`?tab=` 经 `useSearchParams` 记忆），每 tab = 当前值卡（含 sparkline + 涨跌）+ 时序折线图。观看 tab：已看/未看/追看中当前值 + 观看活跃趋势折线（复用 `stats.recent_timeline`）+ 保留既有续播热力/各库各格式/Top 榜；媒体 tab：媒体总数/视频/图片/总时长/占用当前值（复用 `/api/library/summary`）+ 媒体增长与累计容量/时长折线 + 媒体构成。新增后端端点 `GET /api/library/trends`（按天新增媒体 count/size/duration，一次 `GROUP BY`、升序、`deleted_at IS NULL`）；前端引入 **Recharts** 图表库（新增 `TrendChart`/`MetricCard` 复用组件、`api/trends.ts`），StatsPage 改 `lazy()` 使 recharts 落入独立 chunk、主包不超 PWA 预缓存上限。新增 AC-22。后端 4 单测、前端 TrendChart/MetricCard/StatsPage 共 22 用例，受影响前端全量测试与生产构建通过。机制见 [docs/specs/stats-content-trends.md](docs/specs/stats-content-trends.md)。
- **系统监控页（FR-119，[ADR-0044](docs/adr/0044-metrics-sampling-persistence.md)）**：新增 `/monitor` 系统监控页（导航「系统」组），展示 CPU / 内存 / 磁盘 / 转码并发的当前值卡 + 时序折线（range 1h/24h/7d 可切、复用 FR-118 的 Recharts `TrendChart`），15s 轮询、无样本显占位。后端新增指标采样机制：引入 `gopsutil/v4` 采集系统 CPU% 与数据盘用量（进程内存/goroutine 用 `runtime`、转码并发用播放服务 `ActiveSessions()`），`internal/metrics` 采样器每 15s 写一行入 SQLite `metric_samples`、按 7 天保留期裁剪、随服务启停；`GET /api/system/metrics?range=` 按 range 下采样（`GROUP BY` 时间桶 + AVG/MAX）返回有界 points + current 快照。新增 AC-23。高风险区测试覆盖 SQLite WAL 并发读写（`-race`）、保留期裁剪、采样器生命周期无 goroutine 泄漏、下采样聚合；前端监控页 6 用例。机制见 [docs/specs/system-monitor.md](docs/specs/system-monitor.md)。
- **时间轴苹果风重设计（FR-120，扩 FR-14/FR-32/FR-68/FR-72）**：`/timeline` 右侧时间标尺由「仅拖动显单张」升级为「鼠标 hover 即在指针处弹出该时段日期 + 数量 + 缩略图九宫格预览」（rAF 节流、hover/drag 共用 `positionToGroupIndex`、保留拖动跳转与键盘可达）；缩放由日/月/年扩为年/月/日/所有（「所有」= 不分组方形密铺）；网格苹果风收紧（方形密铺、列数加密、分组头更轻，不动虚拟滚动/多选/批量）；顶部新增「最近查看」回忆区块（与继续观看/那年今日并列，空则不渲染）。后端新增 `media_files.last_viewed_at` 列 + `PUT /api/library/media/:id/viewed`（打开媒体时记录）+ `GET /api/library/recently-viewed`（倒序、排除软删、limit 夹紧）。新增 AC-24。后端 11 单测、前端 RecentlyViewed/TimelineScrubber/timeline/TimelinePage/PlayPage 共 10 例。机制见 [docs/specs/timeline-apple-redesign.md](docs/specs/timeline-apple-redesign.md)。
- **目录浏览重做为资源管理器 + 真实路径树（FR-121，[ADR-0046](docs/adr/0046-realpath-tree-directory-browse.md) 取代 ADR-0037，扩 FR-33/FR-66）**：`/browse` 重做为 Windows 资源管理器布局——左导航树（懒展开、自动展开当前路径祖先链）+ 可点地址栏（路径段）+ 工具栏（下载/加相册/打标签/删除批量动作 + 详情/列表/大中小视图 + 排序）+ 名称/修改日期/类型/大小详情列 + 状态栏（项目数/选中体量），复用既有多选/详情/批量/筛选。后端聚合由「虚拟库根逐库下钻」改为「真实磁盘路径跨库树」：`GET /api/library/browse` 顶层返回各盘符/共享根（卷根推导去重），浏览任意路径按 `file_path` 前缀**跨所有库**聚合（不再按 `library_id` 收窄），有包含关系的库在公共上级合并（`D:\1` 与 `D:\1\2` → `D:→1→2`、库 `D:\` 整盘可浏览），新增 `sort`（name/size/type/time）服务端排序，`library_id` 参数弃用（向后兼容被忽略）。移除全局页级面包屑 `PageBreadcrumbs`（目录/相册/播放三页，组件与测试一并删除）。新增 AC-25。后端 browse 用例（卷根/跨库树/整盘/sort/软删/大小写）、前端 Explorer 与移除面包屑回归全绿。机制见 [docs/specs/directory-realpath-explorer.md](docs/specs/directory-realpath-explorer.md)。

### 修复
- **统计页在「有媒体但无观看记录」时白屏（FR-75/FR-118 修复）**：`GetWatchStats` 的各 fill 用 `var rows []T` 承接查询，零行时 nil 覆盖了非空初始化、序列化成 JSON `null`，稀疏态下前端 `.map` 整页白屏。后端在 `GetWatchStats` 末尾把 `recent_timeline`/`by_library`/`by_format`/`top_viewed` 的 nil 收敛为空切片（契约恒为数组）；前端 `StatsPage` 另构 `safeStats` 把数组字段 null 归一为 `[]`、`MediaTab` 守 `by_library`（双保险）。加后端稀疏态非 nil 回归与前端「null 不白屏」组件回归。纯修复、无新依赖、无新 ADR。
- **播放器音量滑块拖动致崩溃（FR-104 修复）**：音量 `Slider` 未设 min/max、用了 Mantine 默认域 `[0,100]`，但音量为 `[0,1]`——拖动时 `onChange` 给出 0-100 直接赋给 `video.volume` 触发 `IndexSizeError: volume outside [0,1]`。音量 Slider 显式 `min=0 max=1 step=0.05`（onChange 输出 `[0,1]`）+ `aria-label「音量」`；`handleVolume` 用新增纯函数 `clampVolume` 夹取 `[0,1]` 兜底任何越界来源。加 `clampVolume` 纯函数测试与「音量滑块域为 [0,1]」渲染回归（既有测试只覆盖纯函数、漏了滑块路径）。纯修复、无新依赖、无新 ADR。
- **控制台内容标题与一级 tab 不一致（FR-113 后续修复）**：`SystemPage` 的内容区 order-2 标题对所有四个系统 tab（运行环境/硬件加速/编解码/应用更新）都恒显「系统诊断」——点「应用更新」tab 内容标题却是「系统诊断」，与上方 tab 名不符。改为按当前 `section` 显示对应一级 tab 名称（env→运行环境、hwaccel→硬件加速、codec→编解码、update→应用更新）；抽出 `SECTION_TITLES` 映射并由 `ConsolePage` 的 `Tabs.Tab` 文案与各区块内容标题共用（单一真源、不再各写一份硬编码）。设置 tab 标题仍为「设置」。纯前端、无新依赖、无新 ADR。`SystemPage.test.tsx`/`ConsolePage.test.tsx` 加各 section 内容区标题断言（含「应用更新」section 标题为「应用更新」而非「系统诊断」）。机制见 [docs/specs/console-flat-tabs-anchor-nav.md](docs/specs/console-flat-tabs-anchor-nav.md)。
- **控制台锚点高亮不准、点击定位偏移、滚动后 tab 条被页眉遮住（吸顶布局根因，FR-113 后续修复）**：深层根因在布局——控制台固定页眉（`.mantine-AppShell-header`，position:fixed，占视口 y=0..56）与 sticky 一级 tab 条（`.console-tabs > .mantine-Tabs-list`，原 `top:0`）叠放：tab 条 stuck 在 y=0 正好**被固定页眉盖住**（滚动后整条 tab 看不见），且吸顶偏移测量在「点击时未 stuck」与「滚动后已 stuck」取值不一致，导致点击落点与高亮判定线对不上（点「网络」却高亮「扫描」、区块落点被遮）。本次三处同修：① **sticky tab 条让开页眉**——`top` 由 `0` 改为 `var(--app-shell-header-height, 56px)`（取 AppShell 暴露的页眉高度变量、随配置/安全区自适应，不写死），使 tab 条 stuck 在页眉正下方、滚动时可见；② **左侧锚点列 `.anchor-nav-sticky` 让开页眉 + tab 条**——`top` 改为 `calc(var(--app-shell-header-height,56px) + 2.375rem)`（tab 条高度无变量、取实测近似常量），原内联 `top:56` 移除（仅保留 `position:sticky` 内联供单测断言）；③ **`measureStickyOffset` 改为返回稳定 stuck 偏移**——= 固定页眉 `offsetHeight` + tab 条 `offsetHeight`（与当前滚动无关），使点击设的 `scroll-margin-top` 与 scroll-spy 判定线（`scrollPos + 吸顶偏移 + 小提前量`）用同一值、点哪个锚点就高亮哪个且区块落在 tab 条正下方完整可见。死区/触底/空集保留语义不回归（空 offsets 保留 active、触底钳末项、绝不回退首项）。纯前端、无新依赖、无新 ADR。`AnchorNav.test.tsx` 的 `measureStickyOffset` 用例改为桩定页眉 + tab 条 `offsetHeight` 验稳定偏移，组件级吸顶高亮/点击 `scroll-margin-top` 用例随之调整，保留死区/触底用例。机制见 [docs/specs/console-flat-tabs-anchor-nav.md](docs/specs/console-flat-tabs-anchor-nav.md)。

## 0.19.0（2026-06-27）

### 新增
- **运行环境区块「复制」按钮（FR-113 后续增强）**：系统页「运行环境」tab 的「运行环境」卡片头部新增复制按钮（与编解码「复制结果」同风格，`useClipboard`、复制后绿勾 + 「已复制」文案）。点击把当前展示的运行环境字段（应用版本/操作系统/架构/CPU/主机名/Go 版本/运行时信息/FFmpeg 等）拼成纯文本报告（`buildEnvReport`）写入剪贴板，便于反馈环境信息。纯前端、无新依赖、无新 ADR。`SystemPage.test.tsx` 加按钮存在、点击写入剪贴板且报告含关键字段断言。机制见 [docs/specs/console-flat-tabs-anchor-nav.md](docs/specs/console-flat-tabs-anchor-nav.md)。
- **左侧导航可拖拽调宽（FR-115 扩展，扩 FR-54）**：navbar 右边缘新增拖拽手柄（窄竖条，hover 出品牌紫提示条、`col-resize` 光标），拖动调整**展开态**宽度并夹紧 **160–360px**，宽度持久化到 `localStorage`（键 `jianvideo.nav.width`）、刷新后保留。新增 `useNavWidth` hook（与 `useNavCollapsed` 同风格，含纯函数 `clampNavWidth` 夹紧/取整/非有限数兜底）；展开态宽度此前固定 180px，改为读 `useNavWidth`（默认值仍取 180）。仅展开态生效——收缩态固定 64px 图标态、移动端抽屉均不受拖拽影响；拖拽过程关闭 navbar 宽度过渡动画避免卡顿，`prefers-reduced-motion` 下手柄提示条过渡关闭。纯前端、无新依赖、无新 ADR。新增 `useNavWidth.test.ts`（夹紧/持久化/越界），`AppLayout.test.tsx` 加拖拽手柄存在、模拟拖拽更新宽度与持久化、收缩态无手柄断言。机制见 [docs/specs/nav-interaction.md](docs/specs/nav-interaction.md)。

### 变更
- **导航收缩态隐藏开源协议、切换加宽度过渡、按钮配色统一（FR-115 后续修复）**：v0.18.0 第二批真机走查反馈三处导航打磨。① **收缩态（64px）完全不显示「开源协议」入口**（原图标 + Tooltip 取消），底部仅留收缩/展开按钮，避免图标态拥挤；展开态左下「开源协议」文字链接保留。② **收缩↔展开切换加宽度过渡动画**（`.mantine-AppShell-navbar` `transition: width 180ms ease`，图标↔文字标签随之轻微淡入），切换时宽度渐变而非突变；拖拽调宽仍由 `.nav-resizing` 关闭过渡（拖拽不动画），`prefers-reduced-motion` 下新增过渡关闭。③ **导航相关按钮配色统一到设计 token**：主导航项 hover（`--mantine-color-default-hover`）/ 激活（`--mantine-color-purple-light`）/ focus（品牌紫 outline）均已 token 化，**AnchorNav 的 `NavLink` 显式 `color="purple"`** 锁定激活态品牌紫（不依赖隐式 `primaryColor`，避免串成蓝色），跨亮/暗主题一致。纯前端、无新依赖、无新 ADR。`AppLayout.test.tsx` 收缩态协议入口断言改为「不再展示、仅留收缩/展开按钮」。机制见 [docs/specs/nav-interaction.md](docs/specs/nav-interaction.md)。
- **导航底部重排：版本号移至页眉、底部单行不换行（FR-61/FR-115 后续修复）**：v0.18.0 真机走查发现展开态左侧导航底部同时塞「版本号 + 开源协议 + 收缩按钮」挤成多行。本次重排——当前版本号（取自系统信息）移到页眉品牌「JianVideo」**右侧**展示（`v0.x.y` 小号、dimmed、与 logo 同一行）；导航左下角只保留「开源协议」文字链接、与右下角收缩按钮**同处底部一行**（左协议、右收缩），不再换行、不再平铺版本号长文本（收缩态后续修复为完全隐藏协议入口，见上条）。移动端抽屉底部不受导航宽度/收缩影响，保留「版本号 + 开源协议」原样。纯前端、无新依赖、无新 ADR。`AppLayout.test.tsx` 相应调整：版本号断言改为命中页眉品牌处（`app-version`）、navbar 内不再出现版本号、底部协议入口断言限定在 navbar 内。机制见 [docs/specs/nav-interaction.md](docs/specs/nav-interaction.md)。

### 修复
- **通知堆积致页面白屏（严重，FR-115 后续修复）**：轮询接口（页眉扫描任务 2s 轮询、自更新进度轮询、页眉更新检查等）持续超时/网络失败时，`api/client.ts` 响应拦截器对每次无响应错误都弹一条全局网络 toast，而 `App.tsx` 的 `<Notifications>` 未设上限——toast 无上限堆积撑爆 DOM 致白屏。两端同修：① `<Notifications>` 增 `limit={4}` 从根本限制同时渲染数量（位置仍 top-right）；② 给 axios 请求配置增加 `silent` 标记（模块增强注入类型），拦截器对带 `silent` 的请求网络失败**不弹**全局 toast，并把后台轮询请求（`getScanTasks`/`getUpdateProgress`/`checkUpdate`）标为静默（轮询失败本就静默重试；显式「检查更新」按钮有自身内联错误提示）。纯前端、无新依赖、无新 ADR。新增 `client.test.ts`（普通请求弹一次、`silent` 请求不弹）与 `App.notifications.test.ts`（`<Notifications>` 设 `limit`、位置 top-right）。机制见 [docs/specs/nav-interaction.md](docs/specs/nav-interaction.md)。
- **设置锚点点击高亮被平滑滚动抢回（死区，FR-113 后续修复）**：点击左侧锚点「工具路径」却高亮回最上面的「账户安全」。深层根因是 scroll-spy **死区**——原用 `IntersectionObserver`（`rootMargin` 有效观测带仅视口顶部约 34%）+「可见集为空回退首个」，滚到某些位置（如 `scrollY≈973`）恰无区块落在窄带内 → 可见集空 → 回退首项，高亮 snap 回去（700ms 点击锁仅治标，锁过期必复现）。改为**健壮的位置式 scroll-spy**：纯函数 `pickActiveByScroll(offsets, scrollPos, currentActive)` 取「顶部已越过 `scrollPos+阈值` 的最后一个区块」（视口顶部所在区块），最顶取首、最底取末，`offsets` 为空时保留当前 active——任何位置都有确定结果、绝不回退到无关首项；组件在 `scroll`（rAF 节流）+ `resize` 时用 `getBoundingClientRect + window.scrollY` 实测偏移重算。点击仍即时高亮被点项并加 700ms 锁屏蔽滚动途中跳变，落定后 scroll-spy 接管且结果正好等于被点项。纯前端、无新依赖、无新 ADR。`AnchorNav.test.tsx` 改为覆盖死区场景（`scrollY=973` 取工具路径、中部空隙归属上一区块、空 offsets 保留 active）与组件级滚动高亮/点击锁定/落定一致。机制见 [docs/specs/console-flat-tabs-anchor-nav.md](docs/specs/console-flat-tabs-anchor-nav.md)。
- **设置/系统页锚点列随内容滚走（FR-113 后续修复）**：v0.18.0 真机走查发现在某 tab 内点左侧锚点滚到下方后，锚点列本身随内容一起滚走、滚到底即不可见。本次把设置 tab 与运行环境 tab 的左侧锚点列容器改为 `position: sticky`（`top` 留出顶部一级 tab 高度偏移、`max-height` + 自滚动防锚点过多溢出），并把控制台一级 tab 头部（`Tabs.List`）一并改为吸顶（`.console-tabs` sticky），使滚动时锚点列与一级 tab 常驻可见、点击定位与当前区块高亮正常。纯前端、无新依赖、无新 ADR。`SettingsPage.test.tsx` 加锚点列容器带 sticky 定位断言。机制见 [docs/specs/console-flat-tabs-anchor-nav.md](docs/specs/console-flat-tabs-anchor-nav.md)。

## 0.18.0（2026-06-27）

### 新增
- **页眉刷新按钮（FR-114）**：全局页眉新增刷新按钮（`IconRefresh`，无障碍名「刷新当前页面」），点击仅重载当前路由页面的内容数据——不整页 `reload`、不重载导航/页眉、不重置登录态。机制：`AppLayout` 持 `refreshNonce` 状态，刷新按钮自增之；`AppShell.Main` 内主内容区包裹元素 key 由 `${pathname}:${refreshNonce}` 组成，序号变化使当前路由内容区重挂载、重跑各页数据拉取副作用（导航/页眉在 Main 之外不受影响，登录态由 store/Cookie 持有不触动）。与 FR-67 时间轴页内刷新并存。纯前端、无新依赖、无新 ADR。`AppLayout.test.tsx` 加断言：刷新按钮存在、点击使内容区重挂载且登录态不变、不触发路由跳转。机制见 [docs/specs/header-refresh.md](docs/specs/header-refresh.md)。

### 变更
- **系统/设置页改一级 tab + 每 tab 左侧锚点导航（FR-113）**：控制台页（`ConsolePage`）去掉「系统信息 / 设置」两级 tab，拍平为一级 tab——运行环境 / 硬件加速 / 编解码 / 应用更新 / 设置（「设置」并入为同级 tab）。`SystemPage` 改为按 `section` 渲染单个区块（不再有内层 tab），`ConsolePage` 以新式 `?tab=env|hwaccel|codec|update|settings` 控制选中并**向后兼容旧深链**（`?tab=system&sys=update`、`?tab=settings` 仍能正确定位）；页眉 `UpdateIndicator` 跳转改 `?tab=update`。新增通用组件 `AnchorNav`（`IntersectionObserver` 观测区块、滚动高亮当前，点击 `scrollIntoView` 平滑定位），运行环境 tab（运行环境/FFmpeg）与设置 tab（账户安全/扫描/网络/工具路径/回收站/诊断/环境变量）各配左侧锚点列。纯前端、无新依赖、无新 ADR。新增 `AnchorNav.test.tsx`，重写 `ConsolePage.test.tsx`/`SystemPage.test.tsx` 覆盖一级 tab 与旧深链兼容。
- **应用更新面板单按钮化 + 缓存优先不自动联网（FR-112）**：「应用更新」区块把「检查更新（走缓存）」「获取更新（强制）」两个按钮合并为**单个「检查更新」按钮**，点击 = `force` 强制直连重查 GitHub。进入「应用更新」tab 时**仅展示上次本地缓存**——有缓存直接显示缓存版本与发布说明、无缓存则不显示更新区，**进入不自动联网**（去掉原「进入即后台 force=false 刷新」逻辑，缓解直连 GitHub 慢拖累页面）；执行更新成功后清理该频道本地缓存（新增 `update-cache.ts` 的 `clearCachedUpdate`），下次进入需重新点检查更新强制重拉。页眉 `UpdateIndicator` 提示逻辑不变、缓存契约兼容。纯前端、无新依赖、无新 ADR。`update-cache.test.ts` 加 `clearCachedUpdate` 用例，`SystemPage.test.tsx` 更新区断言改为单按钮 force / 无缓存不显示 / 有缓存不联网 / 更新成功清缓存。
- **导航交互完善（FR-115）**：左侧导航的「展开/收缩」按钮由 navbar 顶部移到底部右下角；点击顶栏 logo 改为切换导航展开/收缩并持久化（`localStorage`），**移除 logo「点击回首页」**——回首页改由导航「时间轴」项（`/`）进入；补齐所有导航项的 hover 背景与过渡态（非激活项也有可见浅底 + 平滑过渡，复用 `--mantine-color-default-hover` 设计 token，激活项保持品牌紫浅底不被覆盖，`prefers-reduced-motion` 下过渡关闭）。纯前端、无新依赖、无新 ADR。`AppLayout.test.tsx` 加断言：收缩按钮居底、点 logo 切换收展且持久化、「时间轴」指向首页、导航项带 hover 类。机制见 [docs/specs/nav-interaction.md](docs/specs/nav-interaction.md)。

### 修复
- **测试版频道「一直有更新」（FR-46/FR-48）**：dev 版本号策略此前取 `max(最新 tag, VERSION)` 的**下一修订号**，使发完正式版（如 0.17.1）后 dev 恒为 `0.17.2-dev.<SHA>`——主干无新提交也凭空高一位；后端 `hasUpdate` 同基线判定按短 SHA 比较（`pre != pre`），每次 push main 重建 dev 即误报「有更新」。本次两端同修：① dev 版本号改为 `<最新正式版 tag 基线>-dev.<提交距离>.g<短SHA>`（基线不再 +1，提交距离 = `git rev-list --count <tag>..HEAD` 反映真实主干），提交距离为 0（发完正式版无新提交）时 `scripts/dev-version.sh` 退出码非 0、`prerelease.yml` 据此跳过发布；② 后端同基线更新判定改按「提交距离序号」比较——仅序号增大（主干真有新提交）才提示，短 SHA churn 不误报，旧格式 dev 走保守兜底。新增 `TestHasUpdate_PrereleaseSameBaselineCompareBySeq`、`TestCheck_PrereleaseSeqNoChurn`、`TestCheck_PrereleaseAfterStableReleaseNoNewCommit` 及更新后的 `scripts/dev-version_test.sh` 守护。决策见 ADR-0042（扩展 ADR-0032）。真实发布链路端到端「发版后频道不再误报」待真机验。
- **路由切换整页淡入（FIX-2）**：此前 `App.tsx` 用 `<RouteTransition>` 包住整个 `<Routes>`，路由切换时左侧导航与页眉随主内容一起淡入/闪动。将淡入容器（`.route-fade`）下移到 `AppLayout` 的 `AppShell.Main` 内，仅以路径为 key 重挂载主内容区——导航栏与页眉位于 Main 之外，不再跟随淡入；`prefers-reduced-motion` 兜底保留。纯前端、无新依赖、无新 ADR。`AppLayout.test.tsx` 加断言：淡入容器仅含主内容、不含导航栏与页眉入口。

## 0.17.1（2026-06-27）

### 修复
- **无可回滚版本时仍显示回滚按钮（FR-46）**：系统页「应用更新」的「回滚到上一版」按钮此前恒显示，但仅当存在上一版备份（`.old`）时回滚才有效——无备份时点按钮会失败报「无可回滚的旧版本」。后端 `update.Service` 新增 `RollbackAvailable()`（探测 `os.Executable()+".old"` 是否存在，纯函数 `rollbackAvailableAt` 便于单测），更新检查响应（`CheckResult`）新增实时计算的 `rollback_available` 字段（不入缓存，避免 `.old` 状态过期）；前端据此条件渲染回滚按钮（无备份则不显示）。新增 `TestRollbackAvailableAt` 与 SystemPage「无回滚版本隐藏按钮」断言。
- **更新下载失败回显裸技术错误、未引导配代理（FR-46/FR-80）**：自更新下载/应用失败时前端直接回显底层串（如 `wsarecv: A connection attempt failed...`），用户无从下手。新增前端 `friendlyUpdateError`：将网络类失败（超时/连接失败/DNS/重置等）映射为友好文案并引导「可在 设置 → 网络 配置出站代理后重试」（国内直连 GitHub 下载 CDN 常超时；全局代理 FR-80 本就覆盖更新下载，配上即可）。新增 SystemPage「下载网络失败展示友好提示并引导配代理」断言。
- **预发布 dev 版本号偏低（FR-48）**：`prerelease.yml` 此前仅按远端 `git describe` 最新 tag 自增推导 dev 号，版本 tag 未推全时偏低（如已发 0.17.0 仍显示 `0.15.1-dev`，且在线更新页「最新版本」反比当前版本旧、令人困惑）。改为取 `max(最新正式版 tag, VERSION 文件)` 的下一修订号作 dev 基线——dev 跟随 VERSION 文件、解耦 tag 推送时机，且仍领先于上个正式版（不倒挂）。推导逻辑收敛到 `scripts/dev-version.sh`（带 `scripts/dev-version_test.sh` 本地测试，CI 与本地一致）。注：在线更新「误把更旧版本报为可更新」已于 v0.17.0（FR-46 基线比较）修复。

## 0.17.0（2026-06-27）

### 新增
- **登录用户修改密码（FR-108）**：已登录用户可在「系统 → 设置 → 账户安全」修改密码——校验当前密码后设置新密码、bcrypt 存储、即时生效（旧密码立即失效、新密码立即可用）。后端新增受保护端点 `POST /api/me/password`（用户取自认证上下文，当前密码不符返回 401，成功 204）；`auth.Service` 增 `ChangePassword` 与 `ErrCurrentPasswordWrong`，`models` 增 `UpdateUserPassword`。前端设置页新增「账户安全」卡（当前/新/确认密码，前端校验新密码 ≥6 位且两次一致）。附带修复 `auth` 包 `TestParseToken_Tampered` 概率性误判（改篡改签名段首字符，避免末位 base64 冗余位等价）。
- **首次初始化引导（FR-109）**：取消「启动自动创建 admin/admin 默认账户」（消除默认弱口令隐患），改为首次初始化引导——系统无任何用户时，首次访问自动进入 `/setup` 引导页，由用户设置管理员用户名+密码创建首个账户并自动登录；已有用户的实例行为不变（直接进登录页）。后端新增免登端点 `GET /api/auth/setup-status`（查是否需初始化）与 `POST /api/auth/setup`（无用户时创建首个账户并签发 Cookie，已初始化返回 409）；`auth.Service` 增 `NeedsSetup`/`Setup`，`CreateDefaultUser` 保留仅供测试播种。前端新增 `/setup` 路由、`SetupPage`、`RequireSetup` 守卫，`auth` store 增 `needsSetup`/`setup`，`ProtectedRoute`/`RequireAnon` 在需初始化时导向 `/setup`。决策见 ADR-0040（部分取代 ADR-0016 的默认账户创建）。**行为变更**：全新部署不再有 admin/admin 默认账户，须在引导页设置。
- **运行时调试日志开关与默认安静日志（FR-110）**：默认降噪——`main.go` 把 GORM 日志器换为新增 `internal/dblog`（默认安静：Error 级 + 忽略 record-not-found，不再刷「查无记录」与每条 SQL 噪音）；设置页「诊断」分区新增运行时「调试日志」开关，开启切 Info 级输出 SQL 与慢查询、关闭恢复安静，**保存即生效、无需重启**，开关持久化到 `settings` 表 `debug_log` 键、启动读取决定初始级别（重启保持）。区别于启动期只读的 `JIANVIDEO_DEBUG`（gin 模式，保持原样）。`dblog.Logger` 以 `atomic.Bool` 持原子开关、实现 `gorm/logger.Interface`；`PUT /api/settings` 含 `debug_log` 时经 `WithDebugLogApply` 回调即时切级别。新增 `internal/dblog` 包、`settings.KeyDebugLog`/`DebugLog`，前端 `SETTING_KEY_DEBUG_LOG` 与设置页 `Switch`。无新依赖、无新 ADR。

### 修复
- **在线更新误把更旧的预发布版报为可更新（FR-46）**：测试版（prerelease）频道的 `hasUpdate`（`internal/update/service.go`）此前只判「最新版与当前不同即提示」，未比较新旧——当前为正式版 `0.16.x` 时，频道里更旧的滚动 dev（如 `0.14.1-dev`）也被误报为「可更新」（顶栏常驻提示、更新页显示有可用更新）。改为按 `MAJOR.MINOR.PATCH` 基线比较：基线更高才有更新、**基线更低绝不降级**、同基线下预发布标识不同才提示（仍支持正式版↔同基线 dev 切换与 dev→新 dev 滚动，同一 dev 不提示）。新增纯函数 `baselineCmp` 与 `TestHasUpdate_PrereleaseNoBaselineDowngrade` 守护，既有预发布选版/同 dev 不更新等用例不回归。真机实测：当前 `0.16.1`、测试版 latest `0.14.1-dev` → `has_update=false`、更新页显示「已是最新」。
- **应用更新页需手动点检查、未缓存更新日志（FR-46）**：系统页「应用更新」子 tab 此前进入后停在「点击检查更新」占位、必须手动点按钮才显示版本与发布说明。改为进入即展示：新增本地缓存工具 `frontend/src/utils/update-cache.ts`（按频道持久化上次检查结果含发布说明到 `localStorage`，损坏/缺失安全兜底），`SystemPage` 在进入/切换频道时先用缓存即时回填版本与更新日志、再后台非强制刷新（命中后端 TTL 缓存即廉价返回，失败保留缓存静默不打扰），切换频道改由 `[channel]` 副作用按频道回填缓存。新增 `update-cache.test.ts` 与 SystemPage「进入即自动展示」「命中缓存即时展示发布说明」断言。纯前端、无新依赖、无新 ADR。
- **侧边导航栏无法滚动**：桌面 `AppShell.Navbar`（`AppLayout.tsx`）为定高 flex 列，导航项较多（11 项 + 三组标题 + 分隔线 + 顶部收起按钮 + 底部版本/协议）叠加矮视口时内容超出可用高度，但中间导航列表 `Stack` 仅 `flex:1`、未设溢出滚动，navbar 自身 `overflow-y: visible`，导致溢出内容（转码/系统/开源协议）溢出视口被截断且无法滚动到达（实测视口高 500 时内容 741px、「系统」「开源协议」落在视口外）。给导航列表 `Stack` 补 `minHeight:0 + overflowY:auto`（flex 子项须 `minHeight:0` 方能收缩到内容以下并触发内部滚动），使**顶部收起按钮与底部版本/协议常驻、中间导航列表可纵向滚动**，矮视口下全部导航项可达。纯前端、无新依赖、无新架构决策、无新 ADR。`AppLayout.test.tsx` 加滚动容器样式与底部入口常驻断言。
- **发布说明源自 CHANGELOG（FR-111）**：正式发布（`.github/workflows/release.yml`）此前用 `generate_release_notes: true`（从提交自动汇总），与 `CHANGELOG.md` 变更真源脱节；预发布（`.github/workflows/prerelease.yml`）内联 awk 抽取的标题为 `## 未发布版本`，而 CHANGELOG 实际标题是 `## 未发布`（无「版本」二字），导致预发布正文「自上个正式版以来的变更」段长期抽空、显示「（暂无记录）」。本次将两条工作流统一改为从 CHANGELOG 抽取对应版本段作为 Release notes——正式发布抽「## X.Y.Z（日期）」段（X.Y.Z 取自 VERSION）经 `body_path` 传入、去掉 `generate_release_notes`；预发布修正标题为 `## 未发布`。抽取逻辑收敛到共享脚本 `scripts/changelog-extract.sh`（带 fence 状态机，代码块围栏内 `## ` 不误判为段落结束），两工作流共用、并由 `scripts/changelog-extract_test.sh` 单测覆盖（抽正式版/未发布段、段边界、代码块内 `## ` 不误判、真实 CHANGELOG 非空），保证「本地测试绿=CI 真能抽对」。决策见 ADR-0041（扩展 ADR-0032 发布体系）。CI 真实发布正文核验留作真机。
- **在线更新重启后失效（FR-46）**：在线自更新运行时虽生效（新进程跑新代码），但停服重启后又回退旧版。根因在 `internal/update/apply.go` 的 `moveFile` 跨卷退化复制路径——下载临时目录（`%TEMP%`，常在 C:）与可执行文件（常在 D:）跨卷时 `os.Rename` 失败转为复制 + 删除，但删除源前用 `defer in.Close()` 关闭源句柄，致 `os.Remove(src)` 在句柄仍打开时执行，Windows 下「文件被占用」删除失败，使整段落地返回错误并触发上层 `os.Rename(old, exe)` 回退到旧二进制（新版未持久化）。修复：删除源前显式按序关闭 `out`/`in` 句柄，并在关闭前 `out.Sync()` 刷盘确保新二进制完整落地。将文件落地逻辑抽为纯函数 `landBinary` 与 `copyAndRemove` 便于单测；新增 `TestLandBinary_PersistsNewContent`、`TestCopyAndRemove_FlushesAndRemovesSource` 守护「落地后磁盘内容=新二进制且源被删除」。无新依赖、无新 ADR。端到端「停服→重启 exe→版本仍为新版」真机核验待主代理合并后做。

## 0.16.1（2026-06-26）

### 修复
- **无障碍对比度与可访问名（FR-97 真机收尾）**：v0.16.0 发布后经真实浏览器 Lighthouse 桌面无障碍审计（统计页）发现四类 WCAG 失分，本次修复后该页无障碍分由 87 升至 **100**（收起/展开导航两态均 100）。① **亮色 dimmed 文字对比度**——`theme.ts` 的 `cssVariablesResolver` 此前仅覆盖暗色 dimmed、亮色沿用 Mantine 默认 `gray.6`(#868e96)，落白底仅 3.32:1、落浅灰底 3.02:1，任意字号均不达 AA 4.5:1（全站 `c="dimmed"` 文本受影响，单一 token 根因）；亮色 dimmed 下调为更深的语义变量 `gray.7`(#495057，落白底 8.18:1、浅灰底 7.42:1），一处改全站达标、保持语义变量不写死 hex。② **进度条可访问名**——统计页 4 个 `Progress`（观看进度概览/各库/各格式/Top 榜）补 `aria-label`，消除 `progressbar` 无名。③ **图表 div 禁用属性**——续播热力方块与时间线条的 `<div aria-label>` 补 `role="img"`，修正「无 role 的 div 不可用 aria-label」。④ **收起态导航链接无名**——侧栏收起（仅图标）时导航 `<Link>` 无可访问文本，给每个导航链接补 `aria-label`（取导航名），两态均有可访问名。⑤ **未看徽标对比度**——统计页 `color="gray"` light 徽标文字默认取 `gray.6` 落浅灰底仅 2.85:1，强制 label 文字为更深 `gray.7`。纯前端、无新依赖、无新架构决策、无新 ADR。新增 `theme.test.ts`（WCAG 对比度纯函数 + 亮/暗 dimmed 映射断言），`StatsPage.test.tsx`/`AppLayout.test.tsx`/`theme-transition-contrast.test.ts` 加 FR-97 守护（进度条可访问名、图表 role、收起态导航名、未看徽标深色、亮色 dimmed 映射归真）。

## 0.16.0（2026-06-25）

### 变更
- **组件规范统一（FR-94，扩 FR-G，含 ref）**：① **原生日期控件换 Mantine DatePickerInput**——时间轴/目录筛选的两个 `<input type="date">`（原生 `年/月/日` spinner，与 Mantine 风格割裂）换为 `@mantine/dates` 的 `DatePickerInput`，统一边框/圆角/占位；用本地年月日做 YYYY-MM-DD ↔ Date 转换（避免 UTC 解析/格式化差一天），**请求侧 `time_from`/`time_to` 字符串契约不变**，转换纯函数 `strToDate`/`dateToStr` 附穷举单测。② **卡片 elevation 基线**——`theme.components.Card` 设默认 `withBorder + shadow:xs + radius:md`（用 FR-92 阴影 token），给卡片表面统一层次（消解卡片扁平）。**新增依赖 `@mantine/dates`（经用户批准，锁 ^7.17.0 与 core 同版本、无版本冲突）**。`design-tokens.test.ts` 加 FR-94 守护。按钮四档层级与徽标语义规范的逐处套用随相关页 FR 落地（守精准修改）。机制见 [docs/specs/design-system-tokens.md](docs/specs/design-system-tokens.md)。
- **色彩体系与语义色（FR-93，扩 FR-G）**：根因修复+主色收敛。此前全站引用 `--mantine-color-purple-*` / `color="purple"`，却从未在主题定义 `purple` 色板，靠未定义 CSS 变量 / CSS 命名色兜底（不一致）；且 `appTheme` 未设 `primaryColor`，默认走 Mantine 蓝——导航激活态/链接/默认按钮/滑块/进度条等强调色全是蓝、与品牌紫冲突。本次在 `theme.ts` 正式定义 10 阶品牌紫色板（锚定 `#7e14ff` 于 shade 6）并设 `primaryColor:'purple'` + `primaryShade:{light:6,dark:5}`，**一改全站默认主色由蓝收敛回品牌紫**（链接由蓝变紫、进度/徽标/激活态统一）；新增语义色 token `other.semantic`（success/danger/warning/info/neutral → Mantine 命名色）约束绿蓝红只用于语义场景。分散在播放器/后缀/系统页的显式 `color="blue"` 留给将重写这些区域的页 FR（FR-100/101/104）一并收敛，避免散改与冲突（守精准修改）。`design-tokens.test.ts` 加 FR-93 守护。无新依赖、无新 ADR。机制见 [docs/specs/design-system-tokens.md](docs/specs/design-system-tokens.md)。
- **空态/加载/反馈统一（FR-98，扩 FR-88）**：在 FR-88 已有 `EmptyState` 上把仍是「单行灰字」的空态、空白闪现的加载态与各页不一的通知统一到同一设计语言，纯前端、无新依赖、无新架构决策、无新 ADR。①**EmptyState 扩覆盖**——「照片地图」页（`MapPage`）无带 GPS 照片时、「相册」页（`AlbumsPage`）无相册时与相册详情无媒体时，均由单行灰字换为居中插画 + 文案 + CTA 的 `EmptyState`（空相册 CTA「新建第一个相册」、空相册详情 CTA「从媒体库加入」，与各自页头按钮区分避免重名歧义）。②**搜索/筛选无结果态**——时间轴（`TimelineView`）与目录浏览（`DirectoryBrowser`）新增可选 `filtered` + `onClearFilter`，0 结果时按是否有筛选条件区分「没有匹配的媒体（带『清除筛选』CTA）」与「空库 / 空目录」两种空态；`TimelinePage`/`BrowsePage` 据自身筛选状态传入并提供清除全部筛选回调（仅清前端筛选态，不改请求契约）。③**列表骨架屏**——`MapPage`/`AlbumsPage`（含相册详情、媒体选择弹窗）首屏加载用 Mantine `Skeleton` 占位替代空白闪现；时间轴/目录既有骨架保持。④**通知/错误态一致**——新增 `frontend/src/utils/notify.ts`（`notifySuccess`/`notifyError`：绿/红 + 勾选/警示图标 + 成功 2500ms/失败 4000ms 自动关闭），`AlbumsPage` 各处通知改走该助手统一约定；`MapPage`/`AlbumsPage` 加载失败改用 `EmptyState`（插画 + 「重试」CTA）。**复用 FR-88 的 EmptyState、不引第三方插画库、不重排布局、不动业务请求**；通知助手仅在本 FR 触碰页面接入确立约定，全仓其余 `notifications.show` 留后续（守精准修改与 YAGNI）。机制见 [docs/specs/empty-states-unify.md](docs/specs/empty-states-unify.md)。各空态/加载态在亮暗主题下居中、无大片留白、无结果态有「清除筛选」引导待真机验。
- **应用框架重构（FR-95，扩 FR-54/FR-55/FR-61）**：基于设计走查重构应用框架壳（`frontend/src/components/AppLayout.tsx`），纯前端、无新依赖、无新架构决策、无新 ADR。①**顶栏用户头像下拉菜单**——把原本平铺在顶栏的「用户名 + 退出登录」收进一个 Mantine `Menu`（以用户名首字符的 `Avatar` 为触发、带 `用户菜单：<用户名>` 无障碍标签），下拉含用户名 `Menu.Label` 与「退出登录」项，减少顶栏拥挤；命令面板 / 更新提示 / 扫描指示 / 主题切换仍外露。②**侧栏激活态 pill**——`renderNavLink` 接入 `useLocation` 判定当前路由对应项，激活项加品牌紫浅底 `var(--mantine-color-purple-light)` + 紫字 `var(--mantine-color-purple-light-color)` + token 圆角 + 半粗字（复用 FR-93 品牌紫），并打 `data-active` 钩子；根路由 `/` 用精确匹配避免前缀误命中所有路径、其余命中自身或子路径；展开态与收缩态（64px）激活底均可辨。③**放大 logo**——顶栏品牌标志由裸 `IconVideo size=22` 改为紫浅底圆角框（36×36）托底的 `size=26` 图标 + `size="xl"` 品牌字，提对比、增品牌存在感。④**深层页面包屑**——新增 `frontend/src/components/PageBreadcrumbs.tsx`（Mantine `Breadcrumbs` + react-router `Link`，末项当前页为纯文本、支持页内 `onClick` 回调项），接入目录浏览（首页 › 目录浏览）、播放页（首页 › 媒体名，影院态隐藏让位视频）、相册（列表「首页 › 相册」、详情「首页 › 相册 › 相册名」，相册 crumb 经 `onClick` 返回列表），指明路由层级；与既有 `DirectoryBreadcrumb`（页内目录树）区分。⑤**收起导航入口前移**——把「收起导航」按钮从 navbar 底部移到顶部（logo 区下、导航组上），更易发现，收缩 / 展开行为与 FR-54 持久化不回归。**约束守住**：扁平 `navItems` 真源不动、命令面板（FR-74）flat map 复用不破坏、FR-83 分组渲染沿用、复用品牌紫与 token 不写死颜色 / 圆角、路由表（App.tsx）不动。新增 `PageBreadcrumbs.test.tsx`、`AppLayout.test.tsx` 加 FR-95 断言（用户菜单含退出登录、激活 pill 对当前路由出现且收缩态可辨、收起入口居顶）。机制见 [docs/specs/app-frame.md](docs/specs/app-frame.md)。桌面 + 窄屏顶栏分组 / 用户菜单 / 激活 pill / 面包屑待真机验。

- **全局微交互与动效（FR-96，扩 FR-G）**：第九期界面打磨，纯 CSS + Mantine 既有能力，无新依赖、无新 ADR。① **可点表面 hover 过渡与抬升**——`index.css` 给 `.mantine-Card-root`/`.mantine-Button-root`/`.mantine-NavLink-root` 加 ~140ms 平滑过渡（仅 background-color/border-color/box-shadow/transform，不过渡全部属性以免大面积重绘），卡片 hover 上移 2px 并加深一档阴影（复用 FR-92 阴影 token）。② **路由切换动画**——新增 `RouteTransition` 组件以路径为 key 包裹 `<Routes>`，触发 `.route-fade` 轻量 fade + 上移渐入。③ **全局顶部加载条**——新增自实现 `TopProgressBar` 组件（nprogress 风格、**不引第三方库**），路由切换触发「启动→爬升至 80%→补满→淡出」状态机，固定视口顶部、复用品牌紫，组件卸载清理全部定时器。④ **prefers-reduced-motion 兜底**——沿用 FR-84 写法，在 `index.css` 的 reduce 媒体查询块禁用 hover 抬升 / 可点表面过渡 / 路由渐入动画，顶部进度条在减少动态效果下瞬时变化。**约束守住**：仅在 `App.tsx` 包装 `<Routes>` 并挂进度条，**不改 `AppLayout.tsx`** 与各页业务逻辑、不重排布局。新增 `motion.test.ts`（index.css 规则与 reduced-motion 兜底断言）、`RouteTransition.test.tsx`、`TopProgressBar.test.tsx`。机制见 [docs/specs/motion.md](docs/specs/motion.md)。切页过渡 / hover 反馈 / 顶部进度条 / 开启「减少动态效果」后禁用，待真机验。

- **无障碍与可读性（FR-97，扩 FR-G）**：在 FR-92（`focusRing:'auto'`）/FR-93（品牌紫）/FR-84（dimmed 对比度）之上做全站无障碍与可读性收尾，纯 CSS + 少量可访问属性，无新依赖、无新架构决策、无新 ADR。① **键盘焦点可见环**——`index.css` 追加 `:focus-visible` 规则，给按钮/链接/导航/输入/`[role=button]`/`[tabindex]` 统一品牌紫 `outline`（复用 FR-93 `--mantine-color-purple-6` token，不写死 hex）+ `outline-offset`，并对 `:focus:not(:focus-visible)` 置 `outline:none`——**键盘 Tab 才显焦点环、鼠标点击不残留**；用 outline 兜底（不占布局、不被祖先 overflow 裁切）与 Mantine 的 box-shadow 焦点环并行不冲突。② **移动端触控目标 ≥44px**——在既有 `@media (pointer: coarse)` 块内给 `.mantine-ActionIcon-root` 设 `min-width/min-height:44px`，放大图标按钮（收藏/删除/导航等）点击区避免误触，仅设最小尺寸不改图标视觉与桌面布局。③ **长文本可读**——系统诊断页 `InfoRow` 对字符串值（数据库/FFmpeg 路径等）补 `title` 全文悬停提示（值为非字符串节点时不挂）。对比度延续 FR-84/FR-93（dimmed 已达标），本 FR 抽查确认。**约束守住**：在 `index.css` **追加** focus/touch 规则、**不改 FR-96 已有动效块**，仅少量组件补可访问属性、不改结构与业务逻辑、不重排布局。新增 `a11y.test.ts`（focus-visible 焦点环 + pointer:coarse 触控目标规则断言）。机制见 [docs/specs/a11y.md](docs/specs/a11y.md)。键盘 Tab 焦点可见 / 鼠标点击不显 / 移动端点击区 ≥44px / 对比度工具抽查 WCAG AA，待真机验。

- **浏览页信息架构 B（FR-101，扩 FR-60/FR-75）**：信息架构与可视化打磨，复用 FR-92 token、FR-93 品牌紫，纯前端、不动后端、无新依赖、无新架构决策、无新 ADR。① **系统诊断键值定宽两列**——`SystemPage.tsx` 的 `InfoRow` 此前用 `Group justify="space-between"` 把 label/value 左右拉满、中间留巨大空隙、长路径换行难读；改为定宽 label 列（120px、`flex-shrink:0`）+ 紧贴其后的 value 列的紧凑两列，键值成对易读，并新增 `mono` prop——路径/版本号/PID 等值用等宽字体（`--mantine-font-family-monospace`）并截断 + `title` 挂全文（保留 FR-97 悬停提示）。② **统计页可视化精修**——`StatsPage.tsx` 自建 CSS 图表（仍不引图表库）配色统一为品牌紫阶：续播热力方块由写死 `rgba(151,117,250,…)` 魔法值改为 `color-mix` 叠 `--mantine-color-purple-6` token（随主题色板统一）、时间线条由 `purple-5` 收敛到品牌锚点 `purple-6`（加顶圆角与高度过渡）、各格式分布条由 `grape` 统一为 `purple`，刻度/间距/对齐微调。③ **媒体行统一组件**——新增 `components/MediaRow.tsx`（缩略图 + 信息 + 勾选/操作，**直接复用现有 `MediaThumbnail` 不动其 API/实现**），回收站（`RecyclePage`）/巡检（`InspectPage`）/重复项（`DuplicatesPage`）三页媒体列表行由各自手写的卡片/网格统一接入该行组件，保留各页既有还原/删除/勾选回调；问题项无缩略图（源文件可能丢失）以无缩略图形态渲染。根节点保留 `.mantine-Card-root` 类与无障碍标签兼容既有定位。新增 `MediaRow.test.tsx`，并在 SystemPage/StatsPage/Recycle/Inspect/Duplicates 测试加 FR-101 断言（键值定宽两列 + monospace、图表品牌紫、三页用统一 MediaRow）。机制见 [docs/specs/page-ia-b.md](docs/specs/page-ia-b.md)。系统页键值不再左右拉满、统计精致、三页媒体行一致待真机验。
- **浏览页信息架构 A（FR-100，扩 FR-14/FR-40/FR-64）**：三个浏览/管理页的信息组织打磨，复用 FR-92 token、FR-93 品牌紫、FR-99 媒体卡组件（`MediaThumbnail`），纯前端、不动后端、无新依赖、无新架构决策、无新 ADR。① **时间轴筛选分组 + 日期轴视觉**——`TimelinePage.tsx` 的筛选区控件（收藏/标签/类型/大小/日期/缩放）原先平铺偏挤，改为按「内容筛选 / 视图」两个带 `dimmed` 小标题的 `role=group` 容器分组（搜索框仍在分组块之外常驻，FR-35/FR-86 不变）；`TimelineView.tsx` 的 `DateGroupRow` 左侧日期轴时间锚视觉增强——圆点加大并用品牌紫实心 + 浅紫光晕环、主标签（月-日/年）醒目，竖线沿用。**仅重组控件排列/分组容器与视觉，受控 state、请求参数、桌面 `visibleFrom` 内联与移动 `Drawer` 承载、FR-99 sticky 批量条、`splitDate` 分组逻辑均不动。** ② **相册卡封面 + 自适应网格**——`AlbumsPage.tsx` 相册卡此前只有图标占位，新增封面：非空相册按需经现有 `GET /api/albums/:id/items` 取首个成员、用 `MediaThumbnail`（`objectFit:'cover'`）渲染封面，空相册（`item_count===0`）显占位插画且**不发成员请求**；列表网格由固定 `cols` 改 CSS Grid `minmax(220px,1fr)` 自适应铺满。**不新增后端封面端点。** ③ **管理页后缀折叠分组**——`LibraryPathManager.tsx` 每库 33 个后缀的徽标墙原先常显是信息噪声，改为默认折叠的「N 个后缀」摘要行（`UnstyledButton` + `Collapse`，`aria-expanded` 钩子），展开后按 `is_builtin` 分「内置 / 自定义」两组渲染；FR-64 的全部/视频/图片筛选档位作用于展开内容、增删后缀逻辑不变；「添加自定义后缀」入口常显以突出。抽出 `ExtensionBadge` 子组件消除内置/自定义徽标的重复渲染。既有 FR-64 测试改为先展开摘要行再断言（行为不回归）。新增 `TimelinePage.fr100.test.tsx`（筛选分组容器 + 受控不回归）、`AlbumsPage.test.tsx`/`LibraryManagerPage.test.tsx` 加 FR-100 断言（相册封面/占位、后缀默认折叠可展开分组）。机制见 [docs/specs/page-ia-a.md](docs/specs/page-ia-a.md)。时间轴筛选分组清晰、日期轴时间锚明显、相册有封面/空相册占位、管理页后缀折叠展开见内置/自定义分组待真机验。
- **直接播放（FR-102，扩 FR-16/FR-34）**：点开视频「直接播放」，去掉中间一步点击，纯前端、不动后端、无新依赖、无新 ADR。① **播放器静音自动播**——`VideoPlayer.tsx` 此前带声 autoplay 常被浏览器自动播放策略拦截、落到「点击播放」遮罩还要再点一次；改为统一先 `video.muted=true` 再触发播放（浏览器允许 muted autoplay），进页即出画面，并在视频右上角提供显著「🔇 点击取消静音」入口，点击恢复有声（`muted=false`）；用户主动调音量/切静音也撤下该入口。原「点击播放」遮罩仅保留为 muted 仍被拦时的极端兜底。三处自动播放点（mpegts `loadeddata`、hls `MANIFEST_PARSED`、mp4 原生）收敛为单一 `attemptAutoPlay` 辅助，消除重复。**续播 seek（FR-44）、追播缓冲（FR-18）、字幕、编码协商（FR-52）等既有行为不变。** ② **灯箱内视频直接播**——`MediaDetailPanel.tsx` 遇视频时由「缩略图 + 打开播放按钮」改为内嵌 `<VideoPlayer autoPlay 静音>` 直接播放（用既有 `/api/play/:id/stream` 流地址，不引入协商逻辑），去掉中间跳转一步；播放器经 `React.lazy` 懒加载，仅在实际查看视频时才拉入 mpegts.js 重内核，图片预览场景不受影响；图片预览逻辑不动。新增 `VideoPlayer.directplay.test.tsx`（静音自动播不显遮罩、取消静音后 `muted=false`），`MediaDetailPanel.test.tsx` 改断言视频内嵌播放器且无「打开播放」按钮。机制见 [docs/specs/direct-play.md](docs/specs/direct-play.md)。浏览器点视频直接出画面播放（静音）、点取消静音有声、灯箱内视频直接播待真机验。
- **播放页全屏沉浸布局（FR-103，扩 FR-85）**：让播放页正好铺满视口、不可纵向滚动，纯前端、不动后端、无新依赖、无新架构决策、无新 ADR。此前 `PlayPage.tsx` 用 `<Stack>` 竖排「头部 + 固定 16:9 视频 + 下方媒体信息 `Paper`」，视频高度被锁在 16:9、下方信息卡又把内容撑出视口，导致播放页可向下滚动、视频未铺满。①**播放路由专属全屏布局**——根容器由 `<Stack>` 改为 `<Box>` 列向 flex（`height:100dvh` 用 `dvh` 避开移动端地址栏高度抖动 + `display:flex; flexDirection:column; overflow:hidden` 锁纵向滚动），面包屑/头部 `flex-shrink` 不撑高，视频区包一层 `flex:1; minHeight:0` 容器吃满头部/控件之外的全部高度。②**视频填满剩余高度**——`VideoPlayer.tsx` 新增「填充模式」prop `fill`：为真时视频区放弃固定 `aspectRatio:16/9`、改为 `flex:1; minHeight:0` 填充父容器，`<video>` 用 `object-fit:contain`（letterbox 黑边）铺满；播放页传 `fill`，**灯箱（`MediaDetailPanel`）/分享等默认用法（无 `fill`）维持 16:9 不变**（FR-102 灯箱内嵌播放器不受影响）。③**媒体信息移出文档流**——原视频下方的「媒体信息」`Paper` 搬进右侧 Mantine `Drawer`，经头部「更多 ⋯」菜单新增的「详情」项打开，内容（真实文件名/路径/格式/编码/分辨率）不变、不再撑高页面。④**与外层滚动协调**——`PlayPage` 挂载时给 `body` 加 `play-immersive` 类、`index.css` 据此把 `AppShell.Main` 去 padding、锁高到 `100dvh`、禁外层滚动，卸载时移除类，**不改 `AppLayout.tsx`**、其它页布局不受影响。**约束守住**：保留 FR-85 影院模式与「更多」菜单全部操作、FR-102 静音自动播、续播（FR-44）/字幕/编码协商（FR-52/53）等既有行为；复用 FR-92 间距 token、不引新依赖。新增 `VideoPlayer.fill.test.tsx`（默认 16:9 / fill 时 flex 填充 + `object-fit:contain`）、`play-immersive.test.ts`（index.css 的 `body.play-immersive` 协调规则），`PlayPage.test.tsx` 加 FR-103 断言（根容器列向 flex + overflow hidden + 100dvh、视频区 fill、媒体信息收进抽屉、body 类挂载/卸载）。机制见 [docs/specs/play-immersive.md](docs/specs/play-immersive.md)。桌面 + 移动端播放页正好铺满、无纵向滚动条、视频吃满高度、信息收纳可查看、影院/自动播不回退待真机验。
- **播放器内核与控件增强（FR-104，扩 FR-16）**：在 FR-102 静音自动播、FR-103 fill 模式之上给 `VideoPlayer.tsx` 补齐现代播放器常用控件，纯前端、不动后端、不引新依赖、无新架构决策、无新 ADR。① **全屏**——控件栏全屏按钮调浏览器原生 `requestFullscreen`/`exitFullscreen`（作用最外层容器）、双击视频区切全屏，监听 `fullscreenchange` 同步按钮态。② **画中画（PiP）**——按 `document.pictureInPictureEnabled` 探测能力，支持时显 PiP 按钮调 `requestPictureInPicture`/`exitPictureInPicture`，不支持则隐藏（优雅降级）。③ **键盘快捷键**——容器 `tabIndex=0` + `onKeyDown`：空格播放/暂停、←/→ 退/进 10s、↑/↓ 音量、F 全屏、M 静音、数字 0-9 跳百分比；命中前判断焦点是否在 `input`/`textarea`/`select`/contentEditable，**不干扰页面其它输入**。④ **倍速**——0.5×–2× 菜单置 `playbackRate`，媒体重建后复位当前倍速。⑤ **±10s 快退/快进**——按钮与方向键复用 `seekBy`，夹取到 `[0,duration]`。⑥ **进度条 hover 时间预览**——进度条容器加大热区（16px）+ 显 thumb，`onMouseMove` 算位置百分比显时间气泡。⑦ **控件自动隐藏**——播放中鼠标静止 3s 控件淡出、移动/暂停时重现（暂停常驻）。⑧ **记忆音量/静音**——音量与静音偏好持久化 `localStorage`（纯函数 `loadVolumePref`/`saveVolumePref`，损坏内容/越界值安全兜底），用户主动调音量/切静音时保存、挂载时恢复；**与 FR-102 协调**：首次仍静音自动播，取消静音后按记忆音量出声。⑨ **字幕字号档**——小/中/大三档映射字幕 `fontSize`（仅在有字幕时展示入口）。⑩ **配色修偏（FR-93）**——进度条/缓冲条/音量条由蓝/青收敛为品牌紫（`color="purple"`），音量图标由亮色主题不可见的 `color="white"` 改语义灰 `color="gray"`。**约束守住**：复用既有 video ref 与状态，保留续播（FR-44）/追播（FR-18）/字幕/编码协商（FR-52/53）/静音自动播（FR-102）/fill（FR-103）一切既有行为；全屏/PiP 用浏览器原生 API、不引依赖。新增 `VideoPlayer.controls.test.tsx`（倍速改 `playbackRate`、±10s 改 `currentTime`、空格/M/方向键快捷键 + 输入框避让、记忆音量纯函数、进度条品牌紫）。机制见 [docs/specs/player-controls.md](docs/specs/player-controls.md)。全屏/画中画/键盘快捷键整体/倍速实效/进度条 hover 气泡/控件静止自动隐藏/音量跨会话记忆待真机验。
- **图片灯箱交互增强（FR-105，扩 FR-34）**：在 FR-102 视频内嵌直接播放之上，仅增强 `MediaDetailPanel.tsx` 的图片预览交互，**不动视频分支**、不动后端、不引新依赖、无新架构决策、无新 ADR，复用 FR-92 token、FR-93 品牌紫。① **缩放后拖拽平移**——放大（zoom>1）后可鼠标按住拖动查看图片别处，平移过程绑定到 `window` 以便拖出元素仍跟手、拖拽中关过渡跟手，缩回 1× 平移归零。② **双击放大/复位**——双击在 1×↔2× 间切换，复位时平移归零。③ **移动端捏合缩放**——触摸双指捏合按两指间距比例缩放、单指拖动平移（原生 `touch` 事件 + `touchAction:'none'`）。④ **上下一项环绕**——`←/→` 与上一项/下一项按钮到头时首↔尾循环（`goPrev/goNext` 对 `files.length` 取模），不再端点禁用。⑤ **相邻原图预加载**——`useEffect` 监听当前项，预取前后各 1 张原图（`createElement('img')` 触发浏览器缓存），切换不闪白。⑥ **幻灯片**——标题区播放/暂停开关，开启时按固定 3 秒间隔自动切下一张，卸载/重开清理定时器。⑦ **位置计数**——标题区显示「当前 / 总数」（如 3 / 60）。⑧ **旋转**——标题区左/右旋转 90° 按钮（图片专属，视频分支不显），CSS `rotate` 实现。⑨ **键盘补全**——在既有 `←/→/Esc` 上补 `+/-` 缩放、`F` 全屏、`Space` 下一张、`R` 右旋。**约束守住**：仅改图片分支，FR-102 视频内嵌播放器分支不动；换项/重开复位缩放/平移/旋转/幻灯。`MediaDetailPanel.test.tsx` 既有端点禁用断言随环绕行为调整为环绕断言，新增双击缩放切换、放大后拖拽改 translate、首项上一项环绕到末项、位置计数渲染、右旋改 transform 含 rotate、切项触发相邻预取断言。机制见 [docs/specs/image-lightbox.md](docs/specs/image-lightbox.md)。缩放拖拽看全图、双击/移动端捏合、上下一项环绕、相邻预加载切换不闪白、幻灯片自动轮播、左右旋转待真机验。
- **图片查看器 UI 重做（FR-106，扩 FR-34/FR-38/FR-39）**：在 FR-105 图片交互之上重做 `MediaDetailPanel.tsx` 的信息呈现与工具栏，**不动 FR-105 缩放/平移/导航交互、不动 FR-102 视频内嵌分支**、不新增后端端点、不引新依赖、无新架构决策、无新 ADR，复用 FR-92 token、FR-93 品牌紫、FR-91 批量编排、FR-101 定宽两列范式。① **EXIF 图标化 + 单位格式化**——右侧 EXIF 区由 `space-between` 键值（两端拉满 + `break-all`）改为定宽 label 列（64px）的定义列表两列（`dl/dt/dd`），相机/镜头/光圈/快门/ISO 各加 tabler 图标；新增 `formatAperture`/`formatShutter`/`formatIso` 纯函数把后端裸值归一化为标准摄影写法（`f/1.8`、`1/200s`、`ISO 100`），对已带/未带标准标记的输入幂等。② **GPS 改站内地图**——GPS 区由原「在外部地图打开」改为「在站内地图打开」主按钮（`useNavigate` 跳 `/map?lat=&lon=`），`MapPage` 读 `useSearchParams`，合法经纬度时把地图初始视图定位到该坐标并放大（zoom 14），非法/缺省走默认全局视图；外部 OSM 链接保留为次要 `dimmed` 入口。③ **工具栏齐全**——标题区工具补齐收藏（`onToggleFavorite` 父层驱动、空心/实心心形随收藏态切换，未传回调不显）、打标签（复用 `useBatchActions.openAddTag` + `BatchActionsModals`，单 id 列表，零新后端）、分享（复用既有 `ShareDialog`，`resourceType='media'`）、下载（既有 `/download` 链接），旋转复用 FR-105 已有按钮，与网格操作一致；`TimelinePage` 把既有 `handleToggleFavorite` 接入面板，目录浏览侧不支持收藏故按钮自动隐藏（守精准修改不下沉收藏到目录流）。④ **沉浸可折叠信息栏**——标题区「折叠/展开信息栏」按钮切换右侧详情，折叠后右侧不渲染、左侧预览吃满宽度更沉浸。⑤ **一键复制**——`useClipboard` 给文件路径、GPS 坐标加复制按钮（成功 2 秒勾选反馈）。**约束守住**：仅重做信息/工具区，FR-105 图片交互与 FR-102 视频分支零改动；`MapPage` 仅顺带读参定位、不扩展地图其它能力（YAGNI）。新增 `format.test.ts` 的光圈/快门/ISO 格式化穷举单测，`MediaDetailPanel.test.tsx` 加 EXIF 定宽两列/单位格式化/GPS 站内地图入口/工具栏齐全/收藏回调/信息栏折叠/复制按钮断言，`MapPage.test.tsx` 加带参定位与默认视图断言；既有 FR-102/105 断言全绿。机制见 [docs/specs/image-viewer-ui.md](docs/specs/image-viewer-ui.md)。EXIF 排版精致 + 图标/单位、GPS 进站内地图定位、工具齐全、信息栏折叠沉浸待真机验。
- **统一媒体查看器框架（FR-107，ref，扩 FR-34/FR-85）**：第九期收敛重构。图片灯箱（`MediaDetailPanel`）与播放页（`PlayPage`）此前各自内联 raw/stream/HLS-master 媒体地址绝对化逻辑，抽出共享纯函数 `frontend/src/utils/media-url.ts`（`mediaRawUrl`/`mediaStreamUrl`/`mediaHlsMasterUrl`），两处复用、消除重复。两查看器为异构（单视频沉浸路由 vs 多项模态灯箱），强行合并为单一巨型查看器核改动大、收益小且有回归风险，故按简单优先做最小安全收敛——导航/全屏/工具栏因上下文差异未强合。**纯重构、对外行为不变**：FR-102~106 全部保持原样，既有 37 个 MediaDetailPanel/PlayPage 测试一字不改全绿 + 新增 `media-url.test.ts`。无新依赖、无新 ADR。机制见 [docs/specs/unified-viewer.md](docs/specs/unified-viewer.md)。

### 新增
- **设计系统 token 基础（FR-92，第九期地基）**：在 `frontend/src/theme.ts` 的 `appTheme` 集中定义/调优设计 token 并设全局默认，经 Mantine 级联让全站统一——是第九期其余界面 FR 的依赖地基。设 `defaultRadius:'md'`（卡片/按钮/输入统一继承圆角）、`focusRing:'auto'`（可见焦点环，无障碍地基）、`cursorType:'pointer'`；显式圆角刻度、精炼的 elevation 阴影刻度（消解卡片扁平无层次）、略放宽的行高刻度、平衡收小的标题刻度（消解 H1 过大）、自定义语义 token `other.contentMaxWidth`（供后续布局限宽）。**不发明新 token 体系**（Mantine 已有 spacing/radius/shadows/fontSizes/lineHeights）、**不做全站 find-replace**（守精准修改）；`spacing`/`fontSizes` 沿用 Mantine 默认以免布局回归；保留 FR-84 的 dimmed 对比度覆盖。新增 `design-tokens.test.ts` 守护。无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/design-system-tokens.md](docs/specs/design-system-tokens.md)。
- **媒体卡与网格重设计（FR-99，扩 FR-D/FR-E/FR-69）**：重设计时间轴与目录浏览的媒体卡及网格，纯前端、无新依赖、无新架构决策、无新 ADR，复用 FR-69 多选基建、FR-91 批量能力、FR-92 阴影/圆角 token、FR-93 品牌紫。①**信息密度优化**——媒体卡信息由「文件名/类型徽标/大小/时长各占多行」精简为缩略图底部渐变叠层内的单行紧凑信息（文件名 + 类型徽标 + 大小），缩略图改 `objectFit:'cover'` 铺满卡面，卡片更矮、网格更密；新增纯展示组件 `MediaCardOverlay.tsx` 承载叠层。②**hover 操作浮层**——卡片悬停浮出快捷操作（播放/打开 + 收藏 + 更多），默认隐藏、`media-card:hover`/`:focus-within` 时淡入（沿用 FR-96 过渡），各按钮 `stopPropagation` 不误触卡片打开；触控设备（`pointer:coarse`）无 hover 故浮层常显、`prefers-reduced-motion` 下去过渡。③**视频时长角标 + 播放叠层**——视频卡缩略图右下角叠半透明时长角标、中心叠淡播放三角强化「视频感」，图片卡均不渲染。④**选中态强反馈**——多选选中项叠品牌紫半透明罩 + 紫粗描边 + 右上勾选徽标（`MediaCardOverlay` 渲染，`data-selected` 钩子保留）。⑤**sticky 批量操作条**——新增 `SelectionBatchBar.tsx`，选中 ≥1 项时页面底部浮现 sticky 条（「已选 N 项」+ 加入相册/打标签/打包下载/删除/清除选择），消费 `TimelineView`/`DirectoryBrowser` 已持有的多选 state（`useMultiSelect`）与 FR-91 批量回调，批量操作不再只靠右键菜单。⑥**响应式列数**——时间轴网格与目录图标档（大/中/小）改用 `SimpleGrid type="container"` + 容器断点列数，超宽屏增列、窄屏减列。`MediaThumbnail.tsx` 新增可选 `objectFit` 与 `overlay`（不破坏既有 202 轮询/降级逻辑）。**约束守住**：不动后端、不引新依赖、不重排页面整体布局（仅重设计卡片/网格 + 批量条）、不新造多选逻辑（sticky 条消费已有 state）、列表档行内布局不动。文件名移入 pointer-events:none 叠层后，`TimelinePage.test.tsx` 两处「点图片打开预览」改点缩略图（行为断言不变）。新增 `MediaCardOverlay.test.tsx`、`SelectionBatchBar.test.tsx`、`TimelineView.fr99.test.tsx`、`DirectoryBrowser.fr99.test.tsx`，`MediaThumbnail.test.tsx` 补 `objectFit`/`overlay` 断言。机制见 [docs/specs/media-card.md](docs/specs/media-card.md)。卡片 hover 浮层、视频角标、选中批量条、超宽/窄屏列数自适应待真机验。

## 0.15.0（2026-06-24）

### 安全
- **JWT 随机密钥不再明文写入日志**：未设置 `JWT_SECRET` 时启动会生成随机密钥，原先把密钥明文打进 WARN 日志（有泄露风险）；改为仅提示「已生成随机密钥（重启后需重新登录），建议设置 JWT_SECRET 环境变量以持久化会话」，不再输出密钥本身。

### 新增
- **登录页打磨（FR-82，扩 FR-13）**：纯前端打磨登录首屏，不碰后端、无新依赖。①**禁用态明确化**——空表单时登录按钮仍禁用（防无效提交），但按钮下方常驻一行辅助提示「填写用户名和密码后即可登录」，并对按钮包裹 tooltip「请先填写用户名和密码」，让用户判断这是「未填完」而非「按钮坏了」；表单填妥后提示消失、按钮恢复常态（tooltip 为增强，因 Mantine 禁用按钮不派发悬停事件，可辨性以常驻辅助文案为准）。②**品牌图形**——新增原创内联 SVG 组件 `frontend/src/components/BrandLogo.tsx`（视频/媒体主题：圆角媒体帧轮廓 + 中心播放三角 + 两侧胶片齿孔，配色取品牌紫 `--mantine-color-purple-4/6` CSS 变量，`role="img"`+`aria-label="JianVideo 标志"` 兼顾无障碍与可断言），置于登录卡片标题上方，消除整页空洞感；不引第三方插画/图标库素材。窄屏沿用 `maw="90vw"` 不溢出。机制见 [docs/specs/login-polish.md](docs/specs/login-polish.md)。浏览器实机的 logo 显示、禁用态提示清晰度与窄屏布局待真机验。
- **左侧导航分组（FR-83，扩 FR-54）**：左侧导航由单层平铺 11 项重排为「浏览 / 管理 / 系统」三组并加视觉分隔，降低查找成本。分组为——浏览：时间轴 / 目录 / 相册 / 地图 / 统计；管理：管理（存储库）/ 回收站 / 巡检 / 重复项 / 转码；系统：系统。展开态每组前置 `dimmed` 小标题，收缩态（64px）不显标题文字改用 `Divider` 分隔（首组前不加）以免破版、图标仍居中可点且沿用 Tooltip。桌面 Navbar 与移动 Drawer 均按组渲染。实现仅改 `frontend/src/components/AppLayout.tsx`：保留扁平 `navItems`（11 项，路径/顺序/语义不变）作为命令面板（FR-74）flat map 的唯一来源，新增只读 `navGroups` 按 `path` 引用其中同一对象做视觉分组（不复制项），抽出 `renderNavGroups` 供桌面与抽屉共用；顺带清理因改用 Link 跳转而孤立的 `handleNavigate`。**不做**可折叠分组 / 拖拽排序 / 分组持久化（YAGNI）。纯前端，无后端改动、无新数据模型、无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/nav-grouping.md](docs/specs/nav-grouping.md)；浏览器实机分隔观感与收缩态点击/Tooltip 手感待真机验。
- **主题切换过渡与 dimmed 对比度达标（FR-84，扩 FR-G）**：两处纯前端体验打磨。其一，亮暗主题切换由瞬时硬切改为平滑过渡——`frontend/src/index.css` 给 `body` 加 `background-color`/`color` 的 150ms ease 过渡（只列这两个属性、不对全部属性过渡，防大面积重绘的性能开销与闪烁），并带 `@media (prefers-reduced-motion: reduce)` 无障碍兜底（用户开启「减少动态效果」时置 `transition: none` 禁用）。其二，全局上调暗色次要文案对比度至 WCAG AA——Mantine 暗色默认把 `--mantine-color-dimmed` 映射到 `dark.2`（#828282），叠在 `dark.7`/`dark.6` 表面上对比度仅约 4.0/3.5:1 处于 AA 边缘；`frontend/src/App.tsx` 经 `createTheme` + `cssVariablesResolver`（抽到 `frontend/src/theme.ts`）在暗色变体把 dimmed 覆盖为更亮的语义变量 `var(--mantine-color-dark-1)`（#b8b8b8），叠 `dark.7`=7.83:1、叠 `dark.6`=6.85:1，正常文本均 ≥4.5:1。**一处改、全局生效**，所有 `c="dimmed"` 站点随之达标，**保持语义变量不写死颜色**；亮色保持 Mantine 默认（gray.6 已达标）。不逐处替换 dimmed 站点。无后端改动、无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/theme-transition-contrast.md](docs/specs/theme-transition-contrast.md)。实机切主题观感平滑、开启「减少动态效果」后过渡禁用、暗色下次要文案肉眼可辨待真机验。
- **移动端筛选折叠（FR-86，扩 FR-36/FR-45）**：时间轴页（`TimelinePage`）与目录浏览页（`BrowsePage`）的筛选区原先纵向堆叠、窄屏控件换行溢出，把媒体列表推到很下、移动端首屏看不到缩略图。本次在移动端（< `sm` 断点）把筛选控件收进右侧「筛选」抽屉（Mantine `Drawer` + `@mantine/hooks` 的 `useDisclosure`，复用 `AppLayout` 既有移动端抽屉惯例），搜索框常驻在抽屉之外，使首屏直达媒体；桌面端（≥ `sm`）筛选区仍内联铺开、布局与行为不变。抽屉内筛选项与桌面内联项共用同一套受控状态（收藏/标签/类型/大小/日期/缩放的 state 与请求拼装均未改），展开调整即时带进请求、收起保留已选。搜索防抖（各 400ms）与请求逻辑未触动。桌面以 `Box visibleFrom="sm"` 内联渲染、移动以「筛选」按钮（`hiddenFrom="sm"`）打开 `Drawer`（`hiddenFrom="sm"`）承载控件，抽屉关闭按钮加中文无障碍标签「关闭筛选」。纯前端，无后端改动、无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/mobile-filter.md](docs/specs/mobile-filter.md)。375px 窄屏首屏可见 ≥1 行缩略图、抽屉可展开收起待真机验。
- **播放页操作收纳与影院模式（FR-85，扩 FR-30，新增播放页影院态）**：播放页（`PlayPage`）头部原把全部操作平铺一行，窄屏换行铺满多行、长标题挤出按钮。两点纯前端打磨。①**操作收纳**——头部只外露「返回 + 标题 + 影院模式 + 更多 ⋯」，把改显示名 / 改文件名 / 下载 / 分享 / 外部播放器 / 加入预生成六个次要操作收进 Mantine `Menu`「更多」下拉（各 `Menu.Item` 仍打开既有弹窗、回调不变，FR-30/42/43/77/79 行为零改动），头部 `wrap="nowrap"` + 标题 `lineClamp={1}` 截断，窄屏不换行铺满、无横滚、长标题不挤出按钮，右侧字幕菜单保留在外。②**影院模式（方案 A：仅临时收起左导航扩大视频区）**——新增 `frontend/src/hooks/useCinemaMode.tsx`（`CinemaContext` + `CinemaProvider` + `useCinemaMode`），`AppLayout` 自持影院本地 `useState` 经 `CinemaContext.Provider` 下发给 `AppShell.Main` 内的页面，导航有效收缩态 = `navCollapsed || cinema`。播放页头部「影院模式 / 退出影院」按钮切换该本地态，`useEffect` 卸载清理在离开播放页时自动 `setCinema(false)` 恢复。**影院态为播放页本地会话态——不写 localStorage、不改全局 `useNavCollapsed` 持久语义、不污染其它页面**；全局持久收缩态优先生效（`navCollapsed=true` 时无论影院态导航均收缩、持久值不被影院切换改写）；兼容 FR-83 分组渲染（`renderNavGroups` 仍按有效收缩态切换展开/收缩）。无后端改动、无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/play-cinema.md](docs/specs/play-cinema.md)。桌面切入影院态视频区明显变宽、退出/离开页面恢复待真机验。
### 变更
- **设置页结构化编辑与分区（FR-87，扩 FR-24/FR-55/FR-56）**：设置页（`frontend/src/pages/SettingsPage.tsx`）三处可用性改进，**纯前端、后端契约与后端测试零改动**。①「每盘符回收站路径」由裸 JSON `Textarea` 改为结构化「盘符→路径」行列表编辑器（可增/删行，每行 盘符 + 路径 两个输入）——加载既有 `{"D":"D:/.recycle"}` 回填为对应行，空盘符或重复盘符行内标红校验且阻止提交；提交时前端把行序列化回 JSON 串，后端 `recycle_bin_paths` 存 JSON 串的契约不变。行↔JSON 的解析/校验/序列化抽为纯函数 `frontend/src/utils/recycle-bin.ts` 并附穷举单测。② 设置项按「扫描 / 网络 / 工具路径 / 回收站」四个带标题的分区卡片归位，环境变量保持只读区；仍只有一个「保存设置」按钮统一 PUT 全部键（后端 `SetMany` 单事务语义不变，不拆多次提交）。③ 环境变量区消费 `GET /api/system/env` 的 `JWT_SECRET`，当 `set=false`（未设置）时内联展示警示「未设置会导致每次重启需重新登录、建议设置 JWT_SECRET 环境变量持久化会话」，`set=true` 时不显；不新增任何写入/持久化 JWT_SECRET 的入口（敏感/启动期项只读）。无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/settings-improve.md](docs/specs/settings-improve.md)。浏览器实测增删行手感、保存往返与 JWT_SECRET 警示展示待真机验。
- **空态体验统一（FR-88，扩 FR-75/FR-77，覆盖 FR-70/FR-73）**：纯前端体验打磨，统一各页「无数据 / 少数据」时的观感。新增共享组件 `EmptyState`（`frontend/src/components/EmptyState.tsx`）：居中插画/图标 + 标题 + 说明 + 可选一键 CTA 按钮，全部 props 驱动；内置一幅**原创线性 SVG「空盒」插画**（`currentColor` + 半透明描边，深浅主题皆可读，不依赖第三方插画库），调用方也可传 tabler 图标作居中图标。接入五页：①「观看统计」页——媒体库为空（`total=0`）时改显整页引导态（去添加媒体库 CTA），不再渲染任何满 0 的空图表卡；「有媒体但毫无观看活动」（未看 + 无续播 + 无时间线）的稀疏态隐藏满 0 的续播热力网格与孤立的时间线细条，避免杂乱无意义的图表。②「转码预设」页——无预设 / 无预生成任务时改用 `EmptyState`（前者带「新建预设」CTA、后者纯引导文案）。③「问题媒体」页——空态带「开始巡检」CTA，文案区分「已巡检无问题」与「尚未巡检」。④「重复项」页——空态带「扫描重复项」CTA。⑤「回收站」页——空态用 `EmptyState`（无 CTA，仅插画 + 文案）。各页空态 CTA 复用页面已有 handler，不碰后端（统计接口对空/稀疏的返回本就正确）、不引新依赖、不重排页面布局。机制见 [docs/specs/empty-states.md](docs/specs/empty-states.md)。各页空态在亮/暗主题下的居中观感与无大片留白待真机验。
- **自更新下载进度与失败重试（FR-90，扩 FR-46）**：原先点「立即更新」后下载几十 MB 产物全程无反馈，下载/校验失败也只弹一句错误、无显式重试入口。后端 `downloadToTemp` 用计数 `io.Writer`（`countingWriter`）包装 `io.Copy`，按字节累进上报「已下载 / 总字节」（总字节取响应 `Content-Length`，缺失时报 0 即不确定态）；`update.Service` 新增互斥量保护的进程内进度单例（状态机 `idle/downloading/verifying/done/failed`，与 FR-46 的 TTL 缓存单例同构、不落库），`Apply` 链路在下载/校验/失败/完成各阶段切状态。新增轮询端点 `GET /api/system/update/progress`（复用 `/api/*` 鉴权，无外部依赖恒可用），返回 `{state, downloaded, total, percent}`。前端「应用更新」子 tab 在更新进行中每 ~800ms 轮询进度、以 Mantine `<Progress>` 展示百分比（总字节未知时退化为展示已下载字节 + 不确定态动画），下载/校验失败后展示显式「重试」按钮直接重新触发 apply（无需再确认）。**只用 Go 标准库 + 内存状态轮询，不引 SSE/WebSocket/消息队列**，不动 `replaceAndRestart`（Windows 覆盖运行中 exe / spawn 失败回退）正常逻辑，无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/update-progress.md](docs/specs/update-progress.md)。完整「下载→替换→重启」端到端真机受限于本机直连 GitHub 下载 CDN 不可达（需配 FR-80 代理在可达环境复验），待真机验。
- **缩略图显示增强（FR-81，扩 FR-D）**：四点打磨缩略图观感与流量。①**透明源中性灰底（后端 P1）**——带 alpha 的源（透明 PNG/部分 WEBP/HEIC 等）直接压 JPEG 时透明区被默认黑底合成纯黑，深色卡片上突兀；ffmpeg 路径改用 `color=0x808080` 经 `scale2ref`+`overlay` 把图像合成到中性灰底，magick（HEIC/RAW）路径加 `-background #808080 -flatten`，无 alpha 的源结果不变。灰底色值由常量 `thumbnailMatteColor` 统一定义，缩略图产物仍恒为 `.jpg`。②**比例自适应（前端 P3）**——`MediaThumbnail` 由固定 `objectFit:cover` 改为 `object-fit:contain` + 中性背景容器，竖图/正方图完整可见不裁，不破坏 `SimpleGrid` 等高网格；容器纵横比可经新增 `aspectRatio` prop 配置（默认 `16/9` 与历史一致）。③**骨架占位与兜底（前端 P14）**——缺图先显 `Skeleton` 覆盖占位（图片始终在 DOM、可被角色查询命中），`onError` 时探测服务端：命中 `202` 生成中则短间隔（1.5s，至多 8 次）轮询重载、生成完成自动替换，其余错误显 `IconPhotoOff` 降级占位。④**多尺寸 srcset（后端+前端 P12）**——缩略图端点 `GET /api/library/thumbnail/:id` 新增 `size` 查询参数（白名单 `160/320/640`，非白名单回落默认 `320`），`thumbnailPathForSize` 按尺寸映射缓存路径，**默认 320 命名保持不变（不破坏 FR-70 dHash 去重与 FR-73 健康巡检读取路径），非默认尺寸用 `<hash>_<size>.jpg` 并存**；前端 `<img>` 加 `srcset`/`sizes` 按列宽请求更小图降流量与解码成本。分享版缩略图端点经共用 `serveThumbnail` 一并支持 size。无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/thumb-display.md](docs/specs/thumb-display.md)。带透明区 PNG 灰底非纯黑、竖图完整不裁、多列按列宽请求更小尺寸单图字节下降、缺图先骨架后自动替换/加载失败显占位待真机验。
- **代理连通性测试（FR-89，扩 FR-80）**：FR-80 的 `network_proxy` 此前仅做 URL 语法校验、不验真实连通性，配错代理只能等自更新等出站请求失败才发现。新增 `POST /api/system/proxy/test`（body `{proxy}`，可空=测直连）：`internal/netproxy` 新增纯函数 `TestProxy(ctx, rawURL, target)`，构造**临时 `http.Client`**（`Transport.Proxy` 指向待测代理、整体超时约 10s）对默认目标 `https://api.github.com`（与 FR-46 自更新出站目标一致）发一次轻量 GET，只要 HTTP 层拿到任意响应（含 4xx）即判可达、网络层错误判不可达，返回 `{reachable, detail, latency_ms, target}`。**全程用临时 client、绝不读写运行期全局代理真源 `netproxy.current`**（与 FR-80「非法不覆盖」「保存即生效」语义一致，探测坏值不污染出站）；URL 校验逻辑与 `SetProxy` 复用同一 `parseProxy`。**代理 URL 含 userinfo 凭据时返回与日志一律脱敏，绝不回显明文**（沿用 `Redacted` 思路，安全红线）。前端设置页「网络」分区代理输入旁加「测试」按钮（loading 态）+ 可达/不可达 Badge，仿 FR-56 ffmpeg「检测」交互；`system.ts` 加 `testProxy`（real/mock 双实现）。socks5 等仍由 Go 标准库 Transport 原生支持，**无新依赖、无新架构决策、无新 ADR**。机制见 [docs/specs/proxy-test.md](docs/specs/proxy-test.md)。配可用/错误代理分别显示可达/不可达待真机验。
- **批量操作扩展：加相册 / 打标签 / 打包下载（FR-91，扩 FR-69，复用 FR-40/41/42）**：在 FR-69 多选基建与右键菜单上扩三类常用批量操作。①**批量加相册**——右键菜单「加入相册」弹选相册框，对选中集每个 id 循环调 FR-40 单项端点 `addAlbumItem`（去重幂等由后端保证），完成弹 toast 显示成功 / 失败计数。②**批量打标签**——右键「打标签」弹选 / 建标签框，对每个 id 循环调 FR-41 单项端点 `addMediaTag`，完成弹 toast 计数。①②**均为纯前端循环复用已交付单项端点，零新后端端点**。③**批量打包下载（净新后端端点）**——新增 `GET /api/library/media/batch-download?ids=`，后端用 Go 标准库 `archive/zip` 把选中媒体原文件**流式 chunked 输出、边写边 flush**（不一次性读入内存防 OOM），用请求 context 控制取消、**不设整体超时**（规避 FR-46 自更新被 30s client 超时掐断的教训）；`smb://` 与磁盘不可访问项跳过（不报错）；附件名按 RFC 5987 编码防中文乱码；设选中数量 ≤ 500、总大小 ≤ 5 GiB 上限超限拒绝。`library.Service` 新增 `GetMediaFilesByIDs` 单次 IN 查询批量取记录（排除软删、无 N+1）。前端右键菜单 `MediaContextMenu` 加三项与可选回调，两宿主（`TimelineView` / `DirectoryBrowser`）透传到两页（`TimelinePage` / `BrowsePage`），编排封装为可复用 hook `useBatchActions` + 弹窗组件 `BatchActionsModals`（两页共用、不重复）；打包下载经 `fetch` 带 Bearer token 拉取后端 zip 流、blob 触发浏览器附件下载（不用 axios，规避其整体超时）。无选中时三项退化为对右键项操作。**无新依赖（archive/zip 为标准库）、无新架构决策、无新 ADR**。机制见 [docs/specs/batch-ops.md](docs/specs/batch-ops.md)。大文件 / 多文件 zip 边写边 flush 不 OOM、不被超时掐断待真机验。

## 0.14.0（2026-06-24）

### 新增
- **后端出站网络代理设置（FR-80）**：新增 settings 键 `network_proxy`，让后端所有外部 HTTP 出站运行期可配置走代理（空=直连），解决 FR-46 自更新真机暴露的「本机直连 GitHub 下载 CDN 不可达」。新增独立无业务依赖的 `internal/netproxy` 包持有全局代理（`atomic.Pointer[url.URL]` 无锁并发安全）：`SetProxy(rawURL)` 校验 scheme ∈ {http,https,socks5,socks5h} 后原子更新（空串清空走直连、非法 URL 拒绝且不覆盖既有值），`ProxyFunc` 供 `http.Transport.Proxy` 使用（无代理返回 nil 直连）。`update.Service`（首个也是当前唯一的后端出站消费者）的检测 client 与下载 client 各接 `Transport:&http.Transport{Proxy:netproxy.ProxyFunc}`，**各自 Timeout 语义保持不变**（检测 30s、下载无整体超时靠 context）。`main.go` 启动期读 `network_proxy` 非空则注入；`PUT /api/settings` 含 `network_proxy` 时落库后即时 `SetProxy`，非法值仅记 WARN 不阻断保存（保留既有代理）。前端「设置」页新增「网络代理」输入（占位 `http://host:port` 或 `socks5://host:port`，空=直连，附说明「用于自更新等后端外部网络访问」），复用 FR-63 热更新保存模式。socks5 由 Go 标准库 `net/http` Transport 原生支持，**无新依赖**。守架构不变量（依赖单向：`api`/`update`/`main` → `netproxy`，`netproxy` 无业务依赖）。机制见 [docs/specs/fr-80-network-proxy.md](docs/specs/fr-80-network-proxy.md)。实际代理穿透 GitHub 检测/下载待真机验（本机未必有可用代理）。

### 变更
- **归真 FR-47/48 发布 CI 文档漂移**（仅文档）：`.github/workflows/release.yml`（FR-47 正式发布：tag 推送触发多平台原生构建 + 创建正式 Release）与 `prerelease.yml`（FR-48 预发布：push main 触发滚动 dev 预发布）实际早已实现并自 v0.7.0 起持续产出各平台二进制 + checksums（自更新检测真机时确认产物存在），PRD 却仍标「计划」。据实归真为「已交付@v0.7.0」，消除漂移（不改 CI 文件本身）。

### 修复
- **自更新下载大产物被 30s 整体超时掐断（FR-46）**：检测与下载共用 `Timeout=30s` 的 `http.Client`，下载几十 MB 二进制产物时被 client 的整体超时掐断（报 `context deadline exceeded (Client.Timeout)`，handler 给的 5min context 被架空）。`update.Service` 新增下载专用 client（**不设整体 Timeout、靠调用方 context 控制**），检测仍用 30s client。真机验证：旧版 0.12.0 端到端正确检测到正式频道 v0.13.0 有更新（识别平台产物 `jianvideo-windows-amd64.exe`）；下载改靠 context 后不再被 30s 掐断（本机网络无法访问 GitHub releases 下载 CDN、下载速率为 0，完整「下载→替换→重启」待可访问该 CDN 的环境真机复验）。

## 0.13.0（2026-06-24）

### 新增
- **转码预设与预生成队列（FR-77，复用 FR-49~53 / FR-29）**：把「首次播放才同步切片」前移为「手动预热」——用户定义可复用的转码预设（目标编码 + 分辨率），把媒体加入预生成队列后台串行预转码，产出切片缓存到 `hls/{mediaID}/`，消除首播冷启动等待。新增两表（`main.go` AutoMigrate 追加）：`transcode_presets`（id/name/codec/width/height，`codec` 取 h264/h265/av1/vp9、`width|height=0` 表示沿用源分辨率，MVP 不含 bitrate/多码率档位）与 `transcode_tasks`（媒体 + 预设入队，含入队时刻预设 codec/width/height 的快照，使任务执行不依赖预设此后被改/删）。预生成队列 `transcoder.PregenQueue`（`pregen_queue.go`）**完整复用 FR-29 扫描任务队列范式**：以 `transcode_tasks` 为持久化真源、单 worker goroutine 串行执行、条件信号唤醒不空转、`RecoverRunning()` 重启把残留 `running` 重置 `pending` 重新入队；落 `transcoder` 包以保持依赖单向（exec 直接调本包 `PreSliceWithCodec`，无需反向依赖 `library`），exec 经函数注入便于单测替身，`main.go` 生产闭包按 `media_id` 反查媒体路径后调 `PreSliceWithCodec(ctx, id, path, w, h, codec, hlsMgr, hlsDir)` 同步切片（已存在切片则复用）。预设存储 `transcoder.PresetStore`（`preset_store.go`）做 CRUD + 编码白名单校验（复用 `CodecOutputParams`，`hevc` 归一化为 `h265`，空名/不支持编码/负分辨率拒绝）。新增端点 `GET/POST/PUT/DELETE /api/transcode/presets`（预设 CRUD）、`POST /api/transcode/tasks{media_id,preset_id}`（入队，校验媒体与预设存在）、`GET /api/transcode/tasks?status=`（列任务）。前端新增「转码」页（`/transcode` + 左侧导航 `IconMovie`）列/建/改/删预设并轮询展示预生成队列任务状态，播放页头部新增「加入预生成」入口（`PregenDialog` 选预设入队）；新增 `@/api/transcode` 客户端（real/mock 双实现）与 `TranscodePreset`/`TranscodeTask` 类型。**砍掉的镀金**：预设的 description/enabled/bitrate/多码率档位字段、批量笛卡尔积入队、任务 retry/auto-generate/settings 全局开关均不做（YAGNI）。**已知局限**：现有 `PreSliceWithCodec` 不支持任意分辨率缩放（TS 路径按源分辨率选档、fMP4 路径不缩放），故本期预设 `width/height` 仅落库为元数据、不进 ffmpeg 缩放参数，预生成只按 `codec` 预热（真正缩放需扩 `PreSliceWithCodec`，另立 FR）。无新依赖。机制见 [ADR-0039](docs/adr/0039-transcode-pregeneration-queue.md) 与 [docs/specs/fr-77-transcode-presets.md](docs/specs/fr-77-transcode-presets.md)；实际 ffmpeg 预转码产出可播切片、预热后首播无冷启动等待待真机验。
- **分享增强：密码 / 限次（FR-78，扩 FR-43）**：分享链接新增两项可选保护——访问密码与访问次数上限（二维码本期不做，无 QR 库）。`shares` 表新增 `password_hash`/`max_uses`/`used_count` 三列（AutoMigrate 自动建列、`main.go` 无需改）。`share.Service.Create` 增 `password`/`maxUses` 参数：密码非空时复用 auth 既有 `golang.org/x/crypto/bcrypt` 哈希存 `password_hash`（**绝不明文落库、`json:"-"` 不回显前端**），`max_uses=0` 表示无限。新增 `VerifyPassword`（bcrypt 比对）与 `ConsumeUse`（GORM 事务内「先检查 `used_count<max_uses` 后 `used_count+1`」原子自增，SQLite 单写者 + 事务防并发超发，无限次不计数）。`POST /api/shares` 请求体加 `password`/`max_uses`。**校验口径**：密码门禁在 `GET /api/share/:token`（进入页）强制——需密码且未带/带错 `X-Share-Password` 头时只回 `{requires_password:true}`、不含任何 media/album 元信息（不泄露内容、不区分过期/撤销，供前端弹密码框）、且**不消费**额度；限次原子自增与密码二次校验发生在**实际访问资源**（raw/thumbnail/download/stream）时，每次成功访问计一次。密码错 / 限次尽统一映射 `404`「分享不存在或已过期」。前端 `ShareDialog` 加访问密码输入（占位「不设密码」）+ 访问次数上限 `NumberInput`（0=无限）；`SharePage` 对需密码分享先渲染密码门、输入正确密码后带头重拉才展示内容、密码错提示「分享不存在或已过期」；`types`/`api/share.ts` 同步（密码经 `X-Share-Password` 头发送，`password_hash` 不回显）。与 FR-79 外链向后兼容（新增参数默认空密码/0 次，FR-79 调用无需改）。无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/share-enhance.md](docs/specs/share-enhance.md)。浏览器实测密码弹框与限次到达后失效待真机验。

## 0.12.0（2026-06-24）

### 新增
- **外部播放器深链（FR-79）**：视频播放页头部新增「外部播放器」入口，一键生成 VLC / IINA 等外部播放器可直接消费的免登网络串流地址。站内三路播放（HLS / fMP4 / 直链 `/api/play/:id/stream`）全部需 JWT 鉴权、外部播放器不可用；本功能复用 FR-43 已有的免登分享流端点 `GET /api/share/:token/media/:mediaId/stream`（原文件直链 + Range，非 HLS，不触发匿名转码），故**后端零改动**。点击入口弹窗选有效期（同 FR-43 分享对话框）→ 复用 `createShare('media', id, hours)` 生成 token → 新增纯函数 `buildExternalStreamURL(origin, token, mediaID, lastPosition?)`（`frontend/src/utils/external-player.ts`）拼出**绝对**地址 `{origin}/api/share/{token}/media/{id}/stream`，并在 `last_position` 为有限正数时追加媒体片段续播点 `#t={取整秒}`（FR-44；该 fragment 不发往服务端，由支持 media fragment 的播放器客户端处理）。新增 `ExternalPlayerDialog` 组件（`useClipboard` 复制地址带「已复制」反馈、`window.open` 在浏览器打开、文案指引在 VLC/IINA「打开网络串流」粘贴）。外链免登暴露面与 FR-43 同等（任何持有地址者可只读访问该媒体原文件，有效期由用户创建时选择）。**二维码扫码本期不做**（无 QR 库），留待 FR-78 引入 QR 依赖时合并。纯前端，无后端改动、无新依赖、无新架构决策、无新 ADR；真机用 VLC/IINA 打开外链播放、从续播点起播待真机验。
- **命令面板（Ctrl+K）（FR-74）**：新增全局命令面板——在任意页面按 `Ctrl+K`（macOS `Cmd+K`）弹出，输入关键字按命令中文名模糊过滤，键盘 `↑`/`↓` 移动高亮、`Enter` 执行选中、`Esc` 关闭，鼠标点击命令项亦可执行；header 增一个命令图标按钮供无物理键盘（移动端）触发。命令清单含：跳转类——左侧导航 9 项（管理/时间轴/目录/相册/地图/回收站/巡检/重复项/系统）+「开源协议」(`/licenses`) +「扫描媒体库」(跳 `/library-manager`) +「搜索」(跳时间轴 `/`)；直接执行类——切换主题、收起/展开导航、退出登录（退出后跳 `/login`）。新增纯展示组件 `CommandPalette`（`@mantine/core` 的 `Modal`+`TextInput`+`UnstyledButton` 列表自建，过滤/键盘导航/高亮回绕逻辑内聚于组件），命令清单由 `AppLayout` 构造（持有 `navigate`/`toggleColorScheme`/`toggleNavCollapsed`/`logout` 闭包）后以 props 注入，便于独立单测；全局快捷键由 `@mantine/hooks` 的 `useHotkeys([['mod+K', open]])` 注册（与既有 `useDisclosure` 同包、`mod+K` 全仓无冲突）。面板内**只跳转、不直接执行扫描/加库/搜索**（这些依赖页面级上下文，由目标页承接），**不引入** `@mantine/spotlight`/`cmdk` 等第三方命令面板库。纯前端交互层增强，无后端改动、无新数据模型、无新架构决策、无新 ADR、无新依赖。实机任意页面 Ctrl+K 打开与键盘选中跳转的手感待真机验。
- **观看热力与统计（FR-75，复用 FR-44）**：新增「观看统计」页（`/stats` + 左侧导航「统计」`IconChartBar`），把散落在 `media_files` 的观看状态汇总成全局视图。后端 `media_files` 新增 `view_count` 列（默认 0，AutoMigrate 自动建列、`main.go` 无需改），由 `library.MarkWatched` 在置 `watched`/清零续播位置的**同一次 UPDATE** 内用 `view_count + 1` 原子自增——**看完计一次**，约 10s 一次的位置上报（`UpdateWatchPosition`）不计数，避免重复累加。新增 `library.GetWatchStats()`（`internal/library/stats.go`）纯查询/聚合现有列（全程 `deleted_at IS NULL`），产出六维度：已看/未看/总数计数、最近观看时间线（`last_watched_at` 按本地时区 `strftime` 天分桶、最近 30 天）、续播位置热力（`last_position/duration` 比例分 10 档，`duration>0`，比例收敛到 `[0,1]`）、各存储库已看分布（join `library_paths` 带 label）、各格式已看分布、观看次数 Top 10；位置→档纯函数 `ratioToBucket` 可穷举单测。新增端点 `GET /api/library/stats` 返回聚合 JSON。前端新增 `WatchStats` 类型、`getWatchStats` api（real/mock 双实现）与 `StatsPage`——**自建无图表库**：已看占比用 Mantine `<Progress>`，续播位置热力与时间线用 CSS Grid / 内联 div 按桶值映射紫色深浅，各库/格式用 `<Progress>` 条，Top 榜复用 `MediaThumbnail` + 跳播放页。**不新建观看明细表**（YAGNI、守真源不变量），统计是 `media_files` 现有列的只读派生，无新依赖、无新架构决策、无新 ADR。机制见 [docs/specs/watch-stats.md](docs/specs/watch-stats.md)。

## 0.11.0（2026-06-24）

### 新增
- **GPS 旅程轨迹视图（FR-76，扩 FR-39）**：照片地图（`/map`）在原带 GPS 照片散点基础上叠加按「天」聚合的旅程轨迹折线，让用户看出「这一天去了哪、怎么走的」。地图数据拉取改带 `sort:'media_time_asc'`（后端按 `COALESCE(media_time, added_at) ASC` 升序返回，后端无改动）；新增纯函数 `buildDayTracks(files)`（`frontend/src/utils/gpsTrack.ts`）——过滤含有效 GPS 坐标（经纬度均存在且非 0,0 空岛）的点，复用 `groupMediaByDate(files,'day')` 按天分组，丢弃当天点数 < 2 的天（无法连线），按日期升序输出 `{date, positions, color}[]`，颜色按下标循环取自固定调色板 `TRACK_COLORS`。`MapPage` 据此渲染若干 `<Polyline>` 折线层（react-leaflet 5.0.0 已含 Polyline，**不引新依赖**），不同天不同颜色以便区分；新增「轨迹模式」`Switch`（默认开）控制折线显隐，散点 `Marker` 与弹窗保持不变。空数据 / 单点的天不画线、不报错。复用 [ADR-0031](docs/adr/0031-photo-map-leaflet.md) 既定技术栈（leaflet + react-leaflet + OSM），纯前端展示层增强，无后端改动、无新依赖、无新 ADR。OSM 瓦片联网与真实 EXIF GPS 沿用 FR-39 既有真机维度限制。
- **「那年今日」回忆流（FR-72）**：时间轴首页在「继续观看」旁新增平级「那年今日」回忆区块，自动挑出往年同一天拍摄的媒体并以「X 年前的今天」横向卡片流展示，点击进入播放页。后端新增 `GET /api/library/on-this-day?limit=N`（默认 12、上限 50），`library.ListOnThisDay` 以服务器本地时间取「今天」的月-日与年份，用 SQLite `strftime('%m-%d', media_time)` 与 `strftime('%Y', media_time)` 命中「往年同月日」——仅 `media_time` 非空（不回退入库时间，避免噪声）、未软删，按 `media_time` 倒序（最近年份在前）。前端新增 `OnThisDay` 组件（复用 `MediaThumbnail` + `mediaDisplayName`，每项按 `media_time` 年份与今年差标注「X 年前的今天」，回忆为空时整块不渲染）与 `getOnThisDay` api（real/mock 双实现）。无新数据模型、无新架构决策、无新 ADR、无新依赖。
- **媒体健康巡检（FR-73）**：新增「问题媒体」页（`/inspect` + 导航「巡检」），后台只读巡检媒体库找出问题媒体。新增独立问题表 `media_health_issues`（不在 `media_files` 加列、与软删等其它机制解耦），每轮巡检先清空再写入当轮快照。巡检由独立后台 goroutine + 状态单例 `HealthScanStatus` 驱动（单飞，已在跑时再次触发不并发；不复用 FR-29 任务队列——巡检是单次全局操作、语义与按库排队的扫描不同），遍历**未软删**媒体逐项判四类问题：0 字节（`file_size==0`）、源文件丢失（`os.Stat` 失败，排除 `smb://`）、视频损坏（新增 `library.ProbeVideoHealth` 跑 ffprobe 判可解析性，**不改动** `probeVideoMetadata` 的元数据静默降级行为）、缩略图无法生成（新增同步入口 `library.TryGenerateThumbnail` 捕获生成错误，不动既有异步 `GenerateThumbnail`）。判定核心抽为纯函数 `classifyMediaIssues`（无 IO 副作用、可穷举单测）。新增端点 `POST /api/library/health/scan`（触发后台巡检）/ `GET /api/library/health/status`（进度）/ `GET /api/library/health/issues`（问题清单，附媒体基本信息）。前端页按问题类型分组列出问题媒体，可勾选后批量删除进回收站（复用 FR-69 `batch-delete`）。**巡检全程只读、绝不改 `deleted_at`**（软删真源归 FR-25/27），不自动修复 / 不自动删除。机制见 [ADR-0038](docs/adr/0038-media-health-inspection.md)。`main.go` AutoMigrate 追加 `MediaHealthIssue`、注入健康巡检服务，无新依赖。
- **感知哈希去重「重复项」页（FR-70）**：检出近似重复的照片/视频并提供批量清理候选。后端 `media_files` 新增 `dhash` 列（64 位差分哈希，0 表示未计算），由 `library` 模块纯 Go 标准库实现——读缩略图（320 宽 JPEG）缩放为 9×8 灰度后逐行相邻像素差分得 64 位（`computeDHash`），汉明距离 `bits.OnesCount64`（`hammingDistance`），并查集按距离 ≤ 阈值（默认 10）聚类（`clusterByHamming`），全部抽成纯函数穷举单测，**不引第三方图像哈希库**。服务层 `ComputeMissingDHashes` 为「未软删且 `dhash=0`」的媒体有界并发补算（缩略图缺失先同步生成一次再算、单条失败仅记 WARN 跳过、幂等），`FindDuplicateGroups` 查「未软删且 `dhash!=0`」的媒体聚类成组（排除软删、稳定有序）。新增端点 `POST /api/library/duplicates/scan`（返回本次新算条数）与 `GET /api/library/duplicates`（返回重复组）。前端新增「重复项」页（`DuplicatesPage`）与左侧导航入口（`IconCopy`）——按组列出成员缩略图（复用 `MediaThumbnail`），勾选每组多余项后「删除选中项」复用 FR-69 批量软删端点进回收站（可还原），「扫描重复项」按钮触发后端补算哈希后刷新；空态/加载态提示。图片与视频都参与（视频用其缩略图单帧，代表性弱属已知局限）。**不新建去重持久表、不持久化已忽略重复对**（YAGNI、守真源不变量），无新依赖、无新架构决策、无新 ADR。

## 0.10.0（2026-06-23）

### 新增
- **时间轴可拖动 scrubber + 浮层预览（FR-68，补齐 FR-32 拖动）**：时间轴右侧新增竖向可拖动时间滑块（`TimelineScrubber`），与当前分组列表（按 granularity 分组、顶部最新/底部最旧）一一对应。拖动时按指针在轨道内的纵向比例经纯函数 `positionToGroupIndex(fraction, count)` 映射到目标分组下标，实时浮层预览该分组日期 + 首项缩略图（复用 `MediaThumbnail` 与 `/api/library/thumbnail/:id`）；松手后调虚拟器 `scrollToIndex` 滚动跳转到对应日期分组。指针事件用 `setPointerCapture` 保证拖出轨道仍跟手；`role="slider"` + 上下方向键移动一个分组并跳转保留键盘可达。空 / 加载 / 错误态不渲染。纯前端交互，无后端改动、无新依赖。
- **时间轴刷新按钮（FR-67）**：时间轴页标题右侧新增手动「刷新」按钮（`ActionIcon` + `Tooltip`），点击调用累积分页 hook 的 `reload()` 重载首页数据（`append=false` 回到第一页重新累积）；加载期间按钮显示加载态。便于在不刷新整页的情况下拉取最新入库媒体。纯前端，无后端改动、无新依赖。
- **运行环境信息补齐（FR-60）**：系统诊断「运行环境」子 tab 在原 6 项标量（应用版本 / 操作系统 / CPU 核心数 / 主机名 / Go 版本）基础上补齐运行时诊断字段——`GET /api/system/info` 新增 `runtime` 对象，含进程 PID（`os.Getpid`）、工作目录（`os.Getwd`）、可执行文件路径（`os.Executable`）、数据库路径（由 main 注入 `cfg.DBPath`）、运行时长（由 main 注入启动时刻派生，未注入退化为 0）、运行时内存（`runtime.MemStats` 的 `Alloc`/`Sys` 字节 + `NumGC` 次数）、`GOMAXPROCS`（`runtime.GOMAXPROCS(0)`）。前端 `SystemInfo` 类型同步、运行环境卡片逐项展示（内存格式化为可读单位、运行时长格式化为 `Xd Xh Xm Xs`），复制结果报告一并纳入。全部字段取自 Go 标准库、**不引新依赖**；系统级总内存 / 磁盘可用需第三方库（gopsutil 等），不在本期范围。`Handler` 新增 `WithStartTime`/`WithDBPath` 注入器，未注入时 `runtime` 仍返回（运行时长 0、db_path 空），保证无注入测试可用。
- **系统诊断页拆子 tab + 硬件加速卡片网格 + 页眉精确跳转（FR-59）**：「系统信息」页（`SystemPage`）原把运行环境 / FFmpeg / 硬件加速 / 编解码测试 / 应用更新五块单列平铺、需大量滚动，改为内部拆 Mantine 子 tab——运行环境（含 FFmpeg）/ 硬件加速 / 编解码测试 / 应用更新四个子 tab，消除长滚动。子 tab 状态由 URL query `sys=env|hwaccel|codec|update` 控制（缺省 / 非法落回运行环境），函数式更新保留外层 `tab=system`，便于深链。硬件加速 per-family 卡片由纵向单列 `Stack` 改 `SimpleGrid`（`cols={{ base:1, sm:2, lg:3 }}`）一行 2-3 列网格，提升信息密度。页眉「更新可用」提示（FR-58）的点击跳转归真——`UpdateIndicator` 由 `/system?tab=system#update`（`keepMounted={false}` 下锚点滚动失效）改为 `/system?tab=system&sys=update`，`SystemPage` 读 query 自动选中「应用更新」子 tab 精确定位（`id="update"` 锚点保留兜底）。纯前端重组，无后端改动、无新依赖、无新 ADR；既有内容与交互不变。
- **存储库卡片改一行 2-3 个网格布局（FR-65）**：存储库列表此前用单列 `Stack` 铺满，每卡把信息与「浏览/增量/全量/删除」挤在一排，叠加 FR-64 后缀管理后单列横向更拥挤。改为 `SimpleGrid`（`cols={{base:1,sm:2,lg:3}}`）一行 2-3 个的卡片网格，并重排卡内结构为纵向堆叠——库信息行（可点进浏览）在上、操作按钮组在下并可换行，窄列下不再拥挤；删除按钮补无障碍标签。纯前端布局重组，无数据/接口/架构改动、无新 ADR、无新依赖；卡片背景仍用随主题切换的 `var(--mantine-color-default)`（亮色模式黑底守护不回归）。
- **存储库后缀列管：列出 + 删除自定义后缀 + 视频/图片/全部筛选（FR-64）**：后缀管理此前只能「加」——后端仅有列（内置+自定义）与增端点、无删除，前端有「加后缀」输入但不列出已加后缀、不能删除。补齐：后端新增 `DELETE /api/library/extensions`（query 带 `library_id`+`extension`），`library.DeleteMediaExtension` **只删自定义后缀**、内置后缀（命中 `builtInMediaExtensions`）拒删、删不存在的自定义后缀报错（成功 204 / 失败 400）。前端存储库卡片新增后缀管理区——按「视频/图片/全部」三档 `SegmentedControl` 筛选（全部=同显两类），逐项以徽标列出该库后缀：内置后缀标「（内置）」且无删除入口、自定义后缀带删除按钮（走 DELETE 后即时刷新该库后缀列表）。添加后缀仍 video/image 二选一，**数据模型不变**（`media_extensions` 唯一键 `(library_id, extension)`、内置后缀 `is_builtin=1` 临时合成不落库）。无新架构决策、无新 ADR、无新依赖。
- **运行期 magick 路径可编辑热更新（FR-63）**：「设置」tab 把 ImageMagick magick 路径（HEIC/RAW 转 JPEG 用，FR-37）补成持久化设置——此前 magick 路径只能靠环境变量 `JIANVIDEO_MAGICK_PATH` 注入、Web 端改不了也要重启。settings 增键 `magick_path`，复用 ffmpeg 路径的现成范式：`main.go` 启动时 `resolveTool` 注入后若设置非空则覆盖（持久化设置优先于自动发现）；`PUT /api/settings` 含 `magick_path`（非空）时落库后即时调 `library.SetMagickPath` 应用到运行期，HEIC/RAW 转换即采用新路径、保存即生效、无需重启（空串不覆盖，保留自动发现/捆绑版结果）。前端「设置」的工具路径区新增「Magick 路径」输入框，随既有「保存设置」一并保存。env 只读表保持不动（进程级、不可 Web 改）；敏感项（JWT/SMB）与启动期项（端口/DB 路径/debug 模式）保持只读、不做可编辑。无新架构决策、无新 ADR、无新依赖。
- **目录浏览聚合多库为单一虚拟根（FR-66）**：目录浏览页（`/browse`）此前是单库视图——后端 `GET /api/library/browse` 必须带 `library_id`，前端写死取第一个库根目录（`paths[0]`）且用一次性锁只初始化一次，注册 ≥2 个存储库时只能看第一个库、无法切换、新增库不刷新。改为顶层聚合所有**启用**存储库为单一「虚拟根」、逐库下钻：后端 `BrowseDirectory` 增聚合根分支（`parent_path` 取哨兵 `__root__` 时忽略 `library_id`，列出所有启用库作为顶层目录项，`DirInfo` 新增可选 `library_id` 标识点进去用哪个库，面包屑单段「全部存储库」），其余 `parent_path` 走原单库前缀逻辑字节级不变；handler 对虚拟根放宽 `library_id` 必填校验。前端 `BrowsePage` 默认进虚拟根列全部库、`useDirectoryBrowse` 去掉一次性 `initialized` 锁，根层点库携 `library_id` 下钻、面包屑可回根、新增库即时反映；带 `library_id`+`path` 的库定位深链（存储库管理页点卡片跳转）保持直接进该库（向后兼容）。库列表真源仍是 `library_paths`、媒体真源仍是 `media_files`，聚合根不新增持久状态。浏览模型由单库扩展为聚合虚拟根的决策见 [ADR-0037](docs/adr/0037-aggregate-directory-browse.md)。
- **应用更新交互完善（FR-62，扩 FR-46）**：系统诊断「应用更新」子 tab 的交互打磨，纯前端、不改后端契约。①检查更新由单按钮拆为双钮（仿编解码测试「测试 / 重新测试」模式）——「检查更新」走 `force=false`（命中后端 10 分钟 TTL 缓存即时返回，进入默认行为），新增「获取更新」走 `force=true` 强制绕缓存重查 GitHub（`handleCheckUpdate` 由无参改收 `force` 形参透传 `checkUpdate(channel, force)`）。②更新 / 回滚的二次确认由原生 `window.confirm` 改为 Mantine `<Modal>` 自建确认对话框（`@mantine/modals` 不在依赖中，故用 `@mantine/core` 的 `Modal` + `@mantine/hooks` 的 `useDisclosure`，**不新增依赖**）——「立即更新并重启」/「回滚到上一版」改为打开对应模态框，模态框内「确认更新」/「确认回滚」才执行原 `applyUpdate`/`rollbackUpdate` + 重启轮询，「取消」仅关闭。原有 busy 态、错误兜底、频道持久化、重启轮询逻辑不变；无后端改动、无新 ADR。
- **列表多选 + 右键菜单 + 批量删除（FR-69，扩 FR-33/FR-25）**：时间轴与目录浏览补齐桌面级多选与批量删除。后端新增 `POST /api/library/media/batch-delete`（body `{ids:[...]}`），`library.BatchDeleteMediaFiles(ids)` 在单事务内对 `id IN (?) AND deleted_at IS NULL` 一次 `UPDATE` 置 `deleted_at`，复用 FR-25 软删语义（仅标记、不动磁盘源文件），跳过不存在/已软删 id、空列表为 no-op，返回实际软删条数。前端抽出可复用多选 hook `useMultiSelect`（按有序 id 列表驱动）封装 Ctrl/Cmd 切换、Shift 区间连选、Ctrl+A 全选、反选、复选框模式，列表收缩时自动剔除陈旧选中项；新增单实例右键菜单组件 `MediaContextMenu`（删除选中/全选/反选/复选框模式开关），未选中时「删除」退化为删右键项。目录浏览（`DirectoryBrowser`）迁移既有 Shift/Ctrl 逻辑到该 hook（保留资源管理器式「单击选中、双击打开」），仅文件可选、目录不参与选择；时间轴（`TimelineView`）从零接入同一 hook（保留画廊式「普通单击打开预览/播放」，多选仅在带修饰键或复选框模式下触发），虚拟列表下 Ctrl+A / Shift 区间作用于「全部已加载项」（按分组展平的有序集合，未滚动加载的项不在范围）。`BrowsePage`/`TimelinePage` 持有批量删除流程——右键「删除选中」→ `ConfirmModal` 二次确认 → 调批量软删端点 → 成功刷新列表（浏览重载/无限滚动 reload）+ 通知，删除进回收站、可在回收站还原。复用 Mantine `Menu`/`Checkbox`/`Modal`，**无新依赖**、无新架构决策、无新 ADR。

### 修复
- **时间轴缩放在缺 media_time 的旧数据上看不出变化（FR-32）**：复现确认缩放逻辑本身正确——有 `media_time` 时切日/月/年分组端到端正常生效（已加页面级测试覆盖）；问题根因在数据层：缺 `media_time` 的旧记录此前仅回退 `added_at`（同一次扫描入库的记录 `added_at` 多为同一天），导致日/月/年都塌缩成单组、视觉上「缩放无效」。将分组时间源降级链由 `media_time → added_at` 扩为 `media_time → added_at → modified_at`，取首个有效 `YYYY-MM-DD`；当某源缺失或格式非法时不再过早归入「未知日期」，而是继续尝试后续源，提升常见旧数据下的缩放可见性。纯前端 `utils/timeline.ts` 改动。
- **亮色模式下卡片/面板仍为黑底（主题切换不生效）**：存储库卡片、目录浏览卡片、时间线卡片、登录页与播放页媒体信息面板均用 Mantine `bg="dark.7"`，它解析为写死的 `var(--mantine-color-dark-7)` 固定深色 token、不随 colorScheme 切换，故切到亮色模式后仍显深底。改为随主题切换的语义变量：普通卡片/面板背景统一 `var(--mantine-color-default)`，目录浏览选中态由固定深紫 `purple.9` 改为随主题强调色 `var(--mantine-color-purple-light)`（亮暗皆可读），登录页标题 `c="white"` 同步改为品牌色 `purple.4`（不再依赖深底）。

### 变更
- **版本号与开源协议入口移入导航、移除页脚（FR-61）**：全局布局（`AppLayout`）移除 FR-57 的全局页脚（`AppShell.Footer` 与 `AppShell` 的 `footer` prop），把「版本号（取自 `GET /api/system/info` 的 `app_version`，拉取失败仍静默不显、不阻塞布局）+「开源协议」链接（→ `/licenses`）」改放左侧导航底部、置于 FR-54 收缩/展开按钮上方。桌面展开态平铺「JianVideo v{版本}」文本与协议链接；收缩态（64px）改用 `IconLicense` 图标 + Mantine `Tooltip`（hover 同时展示版本号与「开源协议」）承载入口，避免文字截断。原页脚在移动端可见，移除后于移动端抽屉（`Drawer`）底部同样补回版本号与协议链接（点击关闭抽屉）。纯前端布局重组，取代 FR-57 的页脚展示形态，无后端改动、无新依赖、无新 ADR。

## 0.9.0（2026-06-23）

### 新增
- **可收缩左侧导航（FR-54）**：桌面端左侧导航栏支持「收缩为图标态 ↔ 展开」切换。展开 180px（图标 + 文字）、收缩 64px（仅图标，hover 出 Mantine `Tooltip` 提示导航名以保可用性）；切换按钮置于 navbar 底部、随状态切换图标与无障碍标签（「收起导航」/「展开导航」）。收缩状态持久化到 localStorage（键 `jianvideo-nav-collapsed`），刷新后保持。`AppShell` 的 navbar 宽度受收缩态控制，未引新依赖。移动端（< sm 断点）的汉堡 + 抽屉导航行为完全不变（抽屉内固定展开渲染、与收缩态隔离）。
- **系统信息与设置合并为单页 + 顶部 tab（FR-55）**：参考宝塔面板，把原「系统信息」（`/system`）与「设置」（`/settings`）两个独立页面、两个左侧导航项合并为同一控制台页（`ConsolePage`）的两个顶部 tab。tab 以 Mantine `Tabs` 承载、状态由 URL query `?tab=system|settings` 控制（缺省系统信息），便于深链；`/system` 路由进控制台页、旧 `/settings` 重定向到 `?tab=settings`（不留死链），左侧导航两项合并为一个「系统」入口。纯前端重组，`SystemPage`/`SettingsPage` 原样作 tab 内容、既有内容与交互不变。
- **页脚版本号 + 开源协议页（FR-57）**：全局布局（`AppLayout`）新增页脚（`AppShell.Footer`，桌面与移动端均可见），左侧显示当前版本（取自 `GET /api/system/info` 的 `app_version`，拉取失败仅静默不显版本、不阻塞布局），右侧「开源协议」链接 → 新页面 `/licenses`（`LicensesPage`，受保护路由）。协议页顶部展示项目自身 JianVideo + MIT 协议 + 作者（可展开全文），含按包名过滤的搜索框，「前端依赖（N）」表逐项列包名/版本/协议/作者（点击展开协议全文），「后端依赖（M）」表列包名/版本/协议（有全文可展开、无全文给 `pkg.go.dev?tab=licenses` 安全外链）。依赖清单 `frontend/src/data/licenses.json` 由 `frontend/scripts/gen-licenses.mjs`（`npm run gen:licenses`）**构建期生成**——前端全部生产依赖经 `license-checker` 取协议全文、后端 `go.mod` 直接依赖（剔除 `// indirect`）从本机 Go module cache 尽力读全文、项目自身读根 `LICENSE`；JSON 入库、随 go:embed 内嵌，**页面直接 import、运行时不联网**。新增 devDependency `license-checker`。
- **页眉「更新可用」提示（FR-58）**：全局页眉（`AppShell.Header`）新增 `UpdateIndicator`，消费 FR-46 已有的更新检查 + 后端 TTL 缓存——挂载时按持久化频道（设置键 `update_channel`，读失败回退 `stable`）调一次 `GET /api/system/update/check`（**非 force**，命中后端 10 分钟 TTL 缓存即廉价返回，不新增端点、不绕缓存、不轮询、不直连刷屏），仅当 `has_update=true` 时常驻展示带处理态徽标的提示按钮（文案含目标版本 `latest`/`tag`，如「v0.9.0 可更新」，hover 出 `Tooltip`「有新版本可更新」）。点击经 React Router 导航到「系统信息」tab 的应用更新区（`/system?tab=system#update`，`SystemPage` 应用更新卡片新增 `id="update"` 锚点供尽力滚动定位），不整页 reload。检查失败 / 无更新一律不渲染、**失败静默**（复用 FR-46 优雅降级语义），不报错、不影响页眉其余元素。纯前端，无后端改动、无新依赖、无新 ADR。
- **设置功能补齐：环境变量查看 + FFmpeg 路径配置与检测（FR-56）**：「设置」tab 新增两个区块。①「环境变量」只读表格——新增端点 `GET /api/system/env` 返回项目已知环境变量清单（涵盖根 config 的 `SERVER_PORT`/`DB_PATH`/`JWT_SECRET` 与 `internal/config` 的 `JIANVIDEO_*` 两套来源），每项含 key/用途/是否敏感/是否已设置/展示值；**敏感项（`JWT_SECRET`、`SMB_MASTER_PASSWORD`）绝不回显明文**，`value` 固定掩码（已设置 `****（已设置）`、未设置 `（未设置）`），只暴露 `set` 布尔（安全红线，测试断言响应体不含 secret 明文）。②「FFmpeg 路径」可配置 + 检测——settings 增键 `ffmpeg_path`/`ffprobe_path`，启动时持久化设置非空则覆盖自动发现（`resolveTool` 之后注入）、`PUT /api/settings` 含这两键时落库后即时应用到转码运行期（保存即生效、无需重启）；新增检测端点 `POST /api/system/ffmpeg/detect`（基于纯函数 `transcoder.CheckFFmpegPath`，跑 `path -version`、不改全局状态）返回 `{ffmpeg_available, ffmpeg_version}`，让用户保存前先验路径是否可用。环境变量为进程级只读（不通过 Web 修改）。

### 修复
- **自更新「检查更新」直连 GitHub 慢导致超时、无缓存、发布说明非 markdown 渲染（FR-46）**：①「检查更新」此前前端 axios 15s 全局超时叠加后端 15s context，国内直连 GitHub 常 >15s，前端先报 `timeout of 15000ms exceeded`——改为前端对 update 检查单请求超时放宽到 60s（全局 15s 不动）、后端 context 与服务的 http.Client（30s）对齐到 30s，并把检查失败的用户提示改为友好文案（后端日志记原始错误，不向用户回显裸超时串）。② 检测无缓存每次必请求 GitHub——`update.Service` 增并发安全的按频道 TTL 缓存（10 分钟），命中返回副本；新增 `force` 形参支持手动强制重测；fetch 出错时优雅降级（非 force 且有历史缓存即返回上次结果，避免一次网络抖动即失败）。③ 发布说明（GitHub Release body 的 markdown 原文）此前以纯文本 `pre-wrap` 展示——改用 react-markdown + remark-gfm 渲染（外链强制 `target=_blank`/`rel=noopener noreferrer`）。

### 变更
- **修正两处预存在 ARCHITECTURE 文档漂移**（非代码变更）：§1「FFmpeg 经 CGO（csnewman/ffmpeg-go）调用」改为「FFmpeg/FFprobe 外部进程 `os/exec`、CGO 仅用于 SQLite 驱动与 `-tags ffmpeg` 可选硬件检测」（与 §5.3/§5.6 一致，`go.mod` 实无 `ffmpeg-go`）；§3 移除从未落地的幻影 `transcode_sessions` 表，改为如实描述内存播放会话（`playback.Service`，含 FR-53 的 `target_codec`/`output_path`），并同步修正 `.claude/rules/architecture-invariants.md` §3 同一真源条目（见 [ADR-0036](docs/adr/0036-codec-negotiation.md)）。

## 0.8.0（2026-06-23）

### 新增
- **硬件加速检测统一为实测真源 + 持久化缓存 + 全编码探测（FR-49）**：此前硬件加速列表（`/api/transcode/hwaccel`，CGO 只查编码器是否编入、硬编 NVENC+QSV）与编解码器实测（`/api/system/codec-test`，外部 ffmpeg 真实试编码、含 AMF）是两套独立逻辑，导致 AMD 的 `h264_amf`/`hevc_amf` 实测成功却不显示在硬件加速列表，且实测无缓存每次重跑约 3 分钟、能力模型只认 H.264/H.265。改为以**编码器实测为单一真源**：结果按 ffmpeg 版本持久化于 SQLite（`codec_probe_caches`，版本变更失效重测 + 手动「重新测试」），硬件加速列表/系统信息/转码选码统一读这份缓存；能力模型重构为 **per-codec**（每家族逐编码记录可用性，家族可用=至少一编码试编码成功）；补齐 **AMD AMF（含 `av1_amf`）、VAAPI、VideoToolbox、Vulkan** 全家族与 **AV1/VP9** 探测（`av1_nvenc`/`av1_qsv`/`av1_amf`/`av1_vaapi`/`av1_vulkan`/`libsvtav1`、`vp9_qsv`/`vp9_vaapi`/`libvpx-vp9`）；启动时后台预热缓存（不阻塞、单航道防重复），GET 冷态返回「未测」不触发同步实测。前端硬件加速卡片改为按家族×编码逐项展示、标示缓存来源与「重新测试」。顺带修正 `intel_gpu` 假阳性（仅 qsv 实测可用或 sysfs 确认 Intel 核显才标记，不再因候选恒含 qsv 而误报）。AMD 真机验收：`/api/transcode/hwaccel` 与 `/system` 页正确显示 `h264_amf`/`hevc_amf` 可用、`preferred=h264_amf`、可输出编码含 av1/vp9（软件）、`intel_gpu=false`。取代 [ADR-0015](docs/adr/0015-hw-fallback.md)（见 [ADR-0033](docs/adr/0033-hwaccel-probe-source-cache.md)）。转码输出仍固定 H.264（AV1/VP9 实际用于输出与自适应播放属 FR-50~53）。
- **转码目标编码可配置（FR-50）**：转码「目标输出编码」由固定 H.264 改为可配置。服务端以 `settings` 表持久化「首选目标编码优先级」（键 `transcode_codec_priority`，JSON 数组，如 `["av1","h265","h264"]`），写入时按 FR-49 实测可输出集校验（非法 / 不支持 / 重复编码整体拒绝）。单/多码率管道按所选编码参数化输出——编码器名由 `SelectEncoderForCodec` 选取，像素格式与关键参数由纯函数 `CodecOutputParams`（h264/h265/av1/vp9）映射，替代原先硬编 H.264。**默认仍 H.264**（未配置 / 非法回落 `["h264"]`，`NewPipeline()` 等价 `NewPipelineForCodec("h264")`，参数字节级不变、mpegts.js 可播）。输出容器不随编码改变（playback 仍 TS、ABR 仍 HLS，FR-06 播放路径不动），故非 H.264 端到端可播留 FR-51/52。真机验证：首选 av1 → 选码 `libsvtav1`、生产管道产出非空 TS、ffprobe 断言编码 av1（硬件 AV1 待硬件）。扩展 [ADR-0003](docs/adr/0003-hls-ts-streaming.md)（见 [ADR-0034](docs/adr/0034-configurable-target-codec.md)）。
- **高级编码 fMP4/CMAF 输出与 MSE 播放路径（FR-51）**：H.264 维持 mpegts.js + MPEG-TS + HLS 路径不变（追播/边下边播/Seek 内核不动），非 H.264 编码（H.265/AV1/VP9）新增 **fMP4/CMAF 分片 + 浏览器原生 MSE** 播放路径——后端 `RunFMP4ToDir` 用外部 ffmpeg 产出 HLS-fMP4（`init.mp4` + `seg_NNN.m4s` + `index.m3u8`，含 `EXT-X-MAP`/`EXT-X-ENDLIST`，VOD），强制 8-bit `yuv420p`、固定 GOP、HEVC 打 `hvc1` tag、音频转 AAC；`SelectOutputPath` 纯函数按目标编码分发（h264→TS 路径分支不动、其余→fMP4），编码器经 `SelectFMP4Encoder` 复用 FR-49 实测快照按硬件优先级选取、软件兜底；HLS 端点扩展 `.m4s`（`video/iso.segment`）/init `.mp4`（`video/mp4`）MIME 识别。定义前端契约（容器/codec MIME 串 `hvc1`/`av01`/`vp09`/URL 方案/清单格式）供 FR-52 消费。**本期 fMP4 路径仅 VOD，不实时追播**（实时 fMP4 追播属后续阶段）。软件维度真机验证：经 `RunFMP4ToDir` 用 `libsvtav1`/`libx265`/`libvpx-vp9` 实转，`ffprobe` 断言容器=`mov,mp4` 且编码=`av1`/`hevc`/`vp9`、tag=`av01`/`hvc1`/`vp09`；硬件 AV1/QSV/NVENC 维度验收机无对应硬件待硬件。扩展播放内核决策见 [ADR-0035](docs/adr/0035-fmp4-mse-playback-path.md)。
- **前端客户端能力探测 + 自适应播放器（FR-52）**：消费 FR-51 前端契约，前端新增客户端编码能力探测与自适应播放分发。能力探测（`utils/codec-capability.ts`）以 `MediaSource.isTypeSupported` 探测本浏览器在 fMP4 容器下可解码哪些高级编码——`codecMIME` 给出与后端 `FMP4CodecMIME` 字节级一致的 MIME 串（`hvc1`/`av01`/`vp09`）、`isCodecSupported` 归类单编码、`probeClientCapabilities` 返回 `{h265,av1,vp9}` 能力描述（供 FR-53 协商上报）。自适应播放器（`VideoPlayer`）新增可选「播放描述符」入参（目标编码 + 清单 URL + 播放路径 `ts`/`fmp4`/`mp4` + 可选回退源），按路径分发——`ts`（H.264）走现有 mpegts.js（含 master.m3u8 ABR，**追播路径与实现不动**）、`fmp4`（高级编码）走 hls.js 原生 fMP4+MSE 加载 `index.m3u8`、`mp4` 走原生 video；缺省描述符时行为与现状字节级一致（现有调用方零改动、回归全绿）。**不支持回退**：分发到 fMP4 前先 `isCodecSupported` 校验，不支持且有回退源时回退 H.264/TS（不抛 Network Error），无回退源则展示「当前浏览器不支持该视频编码」提示。复用 [ADR-0035](docs/adr/0035-fmp4-mse-playback-path.md) 与 [ADR-0026](docs/adr/0026-abr-adaptive-bitrate.md)（无新 ADR）。**端到端「按客户端能力选编码 + 触发后端产 fMP4 + PlayPage 接线」属 FR-53**，完整真实媒体端到端真机待 FR-53 接线后整体进行。
- **端到端编码协商（FR-53）**：把 FR-49/50/51/52 串通——播放发起时做一次服务端协商，**按「首选优先级 ∩ 客户端能力 ∩ 实测可产出」选出实际输出编码与播放路径**，含降级兜底、会话记录实际编码与路径。协商纯函数 `ChosenCodec(priority,clientCaps,producible)`（首选里第一个客户端支持且系统可产出的编码，都不满足兜底 `h264`，可穷举单测）；新增端点 `POST /api/play/:id/negotiate`（客户端上报 `{h265,av1,vp9}`，读 FR-50 优先级 + FR-49 可产出并集协商，返回播放描述符 `{codec,path,url,mime,fallback_url}`）；非 H.264 同步调 FR-51 `PreSliceWithCodec` 产 fMP4，**产出失败降级回 H.264/TS**（不报错）；前端 `PlayPage` 探测客户端能力 → 请求协商 → 协商出 fMP4 则交 FR-52 自适应播放器（hls.js 原生 MSE），协商出 `h264`/协商失败则沿用既有 master 探测（H.264 回退）；协商结果记到内存播放会话（`PlaybackSession.target_codec`/`output_path`）。**软件 AV1 端到端真机验证**：设首选 `["av1","h264"]` + Chrome（支持 AV1）→ 协商出 av1 → 后端 `libsvtav1` 产 fMP4（ffprobe：容器 `mov,mp4`、编码 `av1`、tag `av01`）→ hls.js + 原生 MSE 实播（`videoWidth=640`/`videoHeight=360` 解码出帧、`readyState=4`、`buffered=[0,4.04]`、无 error）；H.264 客户端 → 协商出 `h264`/TS 走 mpegts.js。硬件 AV1/QSV/NVENC 端到端待对应硬件。协商点 + 端点契约决策见 [ADR-0036](docs/adr/0036-codec-negotiation.md)。

### 修复
- **自更新「测试版」频道错拉正式版、无法识别开发预览（FR-46）**：检测时按「整体最新 Release」选取，而 GitHub 把正式版排在滚动 `dev` 之前，导致测试版频道仍命中正式版（已发 0.7.0 后测试版仍显示 0.7.0、无更新）；且滚动 `dev` 的 tag 恒为 `dev`（非语义版本）无法参与版本比较。改为**按频道选对应 `prerelease` 标志的最新项**（正式版取 `prerelease=false`、测试版取 `prerelease=true`），测试版从 Release 名提取内嵌版本（如「开发预览（dev · 0.7.1-dev.<sha>）」）并按版本串不等判定更新；频道选择持久化到设置 `update_channel`（检查/更新不带 channel 时取设置）；前端频道标签由「稳定/预发布」改为「正式版/测试版」。对真实仓库验证：正式版→0.7.0 无更新、测试版→开发预览 有更新。
- **开发预览版本号比正式版还旧、发布说明为空（FR-48）**：预发布工作流原以 `$(cat VERSION)-dev.<sha>` 作版本号，而 `VERSION` 发版后不上抬，导致发布 0.7.0 后开发预览仍是 `0.7.0-dev.<sha>`——语义化版本里它**小于**已发布的 0.7.0（比正式版还旧），测试版频道会把它当「更新」推送（实为降级）。改为基于最新正式版 tag 用 `git describe` 推导下一修订号作 dev 基线（`v0.7.0 → 0.7.1-dev.<sha>`，始终领先于上个正式版），checkout 取全部 tag；并补齐开发预览的发布说明（滚动预览提示 + 版本/提交 + 自上个正式版以来的 CHANGELOG 未发布段）。
- **10-bit HEVC 源转码后不可播放**：转码（单/多码率管道）未强制 8-bit `yuv420p`，10-bit 源（如 HEVC Main 10）会编出 `High 10`（`yuv420p10le`）的 10-bit H.264，浏览器与 mpegts.js 均无法解码，表现为 HLS「能生成但不可播放」（如 TS/hevc/aac 1280×720）。多码率 scale 链加 `,format=yuv420p`、单管道加 `-pix_fmt yuv420p`，统一输出 8-bit H.264；以 10-bit HEVC/AAC TS 样片复现并验证修复后产物为 8-bit `High`。
- **release 构建中 gin 开启 debug 模式**：发布二进制默认运行于 gin debug 模式，启动刷屏 `[GIN-debug]` 路由表与警告。改为默认 release 模式（仅 info 级请求日志），仅当环境变量 `JIANVIDEO_DEBUG=1/true` 时启用 debug（在创建 gin 引擎前 `SetMode`）。

## 0.7.0（2026-06-22）

### 新增
- **远程自更新（FR-46）**：系统诊断页新增「应用更新」——频道切换（稳定/预发布）、检查更新（展示当前 vs 最新版本与发布说明）、一键更新并重启（二次确认）、回滚到上一版。后端 `internal/update` 经 GitHub Releases 检测/按平台选资产/下载/校验 sha256/替换运行中二进制（改名旧 exe 绕 Windows 文件锁）/自动重启/回滚；`main.go` 监听带端口重试以完成重启接管；API `GET/POST /api/system/update/{check,apply,rollback}`（复用 FR-13 鉴权）。校验失败/缺产物/非更新版本一律拒绝替换。单测覆盖版本比较/资产匹配/checksums 校验/频道选择/校验失败拒绝。对真实 Release 的端到端替换重启需 push 后真机验证（见 ADR-0032）。
- **自动发布 CI（FR-47/FR-48）**：新增 GitHub Actions 工作流——`build.yml`（可复用：`ubuntu-latest`/`windows-latest` 原生 runner 各自 CGO 原生构建前端 `go:embed` + 注入版本号的单二进制 + sha256 校验和）；`release.yml`（推送版本 tag `vX.Y.Z` 或手动触发 → 多平台构建 + 创建正式 GitHub Release，附产物 + `checksums.txt`）；`prerelease.yml`（普通 push 或手动触发 → 滚动刷新 `dev` 预发布）。不引 Docker、不交叉编译，沿用 ADR-0027 的「各平台原生构建」并扩展到 CI（见 ADR-0032）。CI 全绿/Release 真出现需推送线上仓库后验证。

### 变更
- **真机验收归真**：FR-21（系统诊断 + 编解码实测）、FR-22（跨平台打包·Windows 单二进制）、FR-45（移动端 PWA：SW 注册激活 + 可安装 manifest + 离线壳）经真机验收通过，PRD 标记已交付@v0.6.2——功能代码随 v0.6.2 发布，本条仅记录验收归真、无代码变更。FR-10/11（Intel QSV/VAAPI、NVIDIA NVENC）与 FR-22 的 Linux 维度因验收机无对应硬件/环境，保持开发中待真机。

## 0.6.2（2026-06-22）

### 修复
- **扫描入库未提取视频时长/编码/分辨率（FR-31）**：库扫描入库视频时，`enrichMediaMetadata` 此前仅用 ffprobe 读取容器 `creation_time` 定媒体时间，从未探测时长/编码/分辨率，导致 `media_files` 的 `duration`/`video_codec`/`audio_codec`/`width`/`height`/`bitrate` 恒为零值，FR-34 详情面板的「分辨率/时长/视频编码/音频编码」始终空白。改为入库时一次 `ffprobe -show_format -show_streams` 同时取容器时长/码率/`creation_time` 与首个视频/音频流的编码及分辨率并写库（library 包直接用注入的 ffprobe 路径，不依赖 transcoder 以守模块依赖方向）。新增 ffprobe JSON 解析纯函数单测与「真实 ffmpeg 生成视频→入库→断言字段非零」的集成回归用例。
- **HEIC/RAW 转换在 ImageMagick 7 下失效（FR-37）**：`buildMagickConvertArgs`/`buildMagickThumbnailArgs` 把 `-auto-orient` 等图像算子放在输入图像之前，ImageMagick 7 的 `magick` 按左到右解析时报「no images found for operation `-auto-orient`」，致 HEIC/RAW 的 `/raw` 与缩略图转换在真机 IM7 下全部失败（此前单测只断言命令含输出路径、未校验算子相对输入的位置，故未发现；真机装 IM7 才暴露）。改为输入图像在前、算子在后；强化单测断言 `-auto-orient`/`-thumbnail` 位于输入之后。真机以 ImageMagick 7.1.2（libheif+libraw）端到端验证：真实 HEIC→JPEG、真实 Sony ARW→JPEG + 缩略图 + 缓存命中。
- **SMB 连接缺省端口（FR-02）**：`smb.Client.Connect` 直接以 `creds.Host` 作为拨号地址传给 go-smb2 的 `Dial`，host 未带端口（如 `localhost`、`192.168.x.x`）时报「missing port in address」致连接必失败。新增 `normalizeDialAddr`：host 无端口补默认 SMB 端口 445、已带端口原样保留，附纯函数单测。真机以本地 SMB 共享端到端验证：连接成功、扫描入库 4 个文件（`smb://` 路径），错误凭据时优雅失败不崩溃（FR-02 高风险区「凭据错误处理」）。

## 0.6.1（2026-06-22）

### 修复
- **PWA 资源被 SPA 兜底（FR-45）**：内嵌前端服务此前仅把 `/assets/*` 作静态服务，其余路径一律由 `NoRoute` 回退 `index.html`，导致 vite-plugin-pwa 产在 dist 根目录的 `sw.js`、`manifest.webmanifest`、`workbox-*.js` 在打包二进制中被当作 SPA 路由返回 `text/html`——Service Worker 无法注册、manifest 无效，实际部署中 PWA 安装/离线失效。改为 SPA 兜底前先尝试从内嵌 `frontend/dist` 命中根级真实文件并按真实 MIME 返回（`.webmanifest` → `application/manifest+json`），命中不到才回 `index.html`。新增 web 层回归用例断言这些根资源返回正确 Content-Type（非 `text/html`）。此前 FR-45 的 vitest 仅 mock 测 manifest 字段与 SW 注册逻辑，未覆盖内嵌服务器是否真把文件服出，故 bug 被掩盖、真机 curl 才暴露。
- **EXIF 光圈格式化（FR-31）**：图片 EXIF 光圈此前用 `float64` 精度格式化 imagemeta 的 `float32` 光圈值，对非整光圈（如 f/1.8、f/2.8）会输出 `f/2.799999952316284` 之类的精度噪声串，并经 FR-38 详情面板 / 时间轴对用户可见。改为按 `float32` 精度取最短往返表示，正确显示 `f/2.8`。快门、镜头、GPS 提取本即正确，未改动；以真实带 GPS 样片新增回归用例。

## 0.6.0（2026-06-22）

### 新增
- **照片地图视图（FR-39）**：新增 `/map` 页，基于照片 EXIF GPS（FR-31）在 OpenStreetMap 在线瓦片上展示地理分布，标记弹窗显示缩略图与名称。引入 `leaflet` + `react-leaflet`（+ devDep `@types/leaflet`）+ OSM 在线瓦片（无 token/账号，见 ADR-0031）。后端 `GET /api/library/media` 新增 `has_gps=true` 结构化筛选（`gps_lat != 0 OR gps_lon != 0`，并入 FR-35 `MediaFilter`，参数化）；地图页分页累积拉取地理标记子集。导航新增「地图」入口。瓦片显示依赖联网、真实 GPS 依赖 FR-31 真机提取。
- **目录资源管理器视图（FR-33）**：目录浏览增强为类资源管理器——展示方式切换（列表详情 / 大-中-小图标，缩略图密度随档位）、排序切换（名称 / 大小 / 类型 / 修改时间，目录恒在前）、单选 + `Shift` 区间选 + `Ctrl/Cmd` 切换（选中高亮），**双击**文件打开 FR-34 详情面板（取代原单击打开）。目录页接入 `MediaDetailPanel` 并移除已无引用的 `ImagePreviewModal`。为支持多档位与选择，目录视图改用常规网格/列表（单目录条目有界；时间轴大列表仍保留虚拟化）。
- **时间轴缩放与按媒体时间组织（FR-32）**：时间轴改为按**媒体时间**（FR-31，缺失回退入库时间）组织（`sort=media_time`），新增缩放粒度控件（日/月/年）：日按 `YYYY-MM-DD`、月按 `YYYY-MM`、年按 `YYYY` 分组，日期轴标签随粒度自适应；拖动浏览复用现有虚拟滚动。
- **前端筛选/搜索 UI（FR-36）**：时间轴与目录视图均接入表达式搜索框 + `MediaQueryFilters` 结构化筛选控件（类型「全部/图片/视频」、最小大小预设、拍摄时间范围）。时间轴页经 `useInfiniteMedia` 透传 `type`/`size_min`/`time_from`/`time_to`（筛选变化即重置首屏）；目录页（BrowsePage）有筛选/搜索时按当前目录路径（前缀，递归）查媒体接口展示匹配结果、无筛选时仍浏览文件夹树——两者均消费 FR-35 引擎，搜索框支持 `ext:`/`type:`/`size:` 表达式语法提示。
- **媒体筛选与表达式查询引擎（FR-35）**：`GET /api/library/media` 的 `search` 升级为 everything 式表达式（`library.ParseSearchExpression`）：裸词→文件名包含（多词 AND）、`ext:jpg,png`→扩展名、`type:image|video`→类型、`size:>10mb`/`<=2gb`/`>=500kb`（单位 b/kb/mb/gb/tb）→大小；纯文本向后兼容。另新增结构化查询参数 `type`/`size_min`/`size_max`/`time_from`/`time_to`（按 `COALESCE(media_time, added_at)` 比较）/`path`（目录前缀）。表达式只解析为结构化 `MediaFilter` 字段、全部走参数化查询，无 SQL 注入面；类型按内置图片扩展名集合粗筛。
- **EXIF 详情展示（FR-38）**：文件详情面板右侧新增 EXIF 区块（有数据才显示），展示拍摄时间（含来源标注 EXIF/文件名/创建/修改）、相机、镜头、光圈、快门、ISO、GPS 坐标，并对有 GPS 的媒体提供「在外部地图打开」链接（OpenStreetMap，新标签打开）。消费 FR-31 后端已提取并随 `media` 接口返回的 EXIF 字段；真实 EXIF 端到端依赖 FR-31 真机提取。
- **文件详情面板（FR-34）**：新增 `MediaDetailPanel`（全屏 Modal），时间轴点击媒体（图片与视频统一）打开：左侧预览（图片为可滚轮缩放的原图 1–4 倍、换项复位；视频为缩略图 + 「打开播放」按钮跳 `/play/:id`），右侧展示文件元数据（显示名/真实名/类型/大小/分辨率/时长/编码/加入与修改时间）并提供原文件下载入口。支持全屏切换、`←`/`→` 在已加载列表内切换上/下一项（端点夹紧）、`Esc` 关闭。取代时间轴原「图片弹窗 / 视频直跳」的分裂打开方式；`ImagePreviewModal` 仍由目录页使用（待 FR-33 统一）。

## 0.5.0（2026-06-22）

### 新增
- **分享链接（FR-43）**：为指定媒体 / 相册生成 token 化只读公开链接，免登访客凭链接在线查看图片、在线播放视频、下载原文件，带可配置过期（含永不过期）。作为鉴权的受控例外：`auth.APIGuard` 豁免 `/api/share/` 前缀（带尾斜杠，不误伤受保护的管理端点 `/api/shares`），公开路由经 `shareAuth` 中间件自校验 token + 过期，且每个媒体端点都做范围校验（mediaId 必须 == 被分享媒体或 ∈ 被分享相册成员，越权一律 404）。token 由 `crypto/rand` 生成 32 字节不可枚举。安全边界：免登视频播放只走渐进式 `StreamFile`（原文件 + Range），不向匿名访客开放 ffmpeg 转码 / HLS 管线（防资源滥用）。新增 `internal/share` 服务、`shares` 表、管理端点 `POST/GET /api/shares` 与 `DELETE /api/shares/:token`、公开端点 `GET /api/share/:token`(+`/media/:mediaId/raw|thumbnail|download|stream`)；前端播放页 / 相册页加「分享」入口（选有效期、生成、复制链接），新增免登公开查看页 `/s/:token`（不套登录守卫，按类型展示图片 / 视频 / 相册网格）。复用 FR-13 鉴权、FR-40 相册、FR-42 下载。
- **下载原文件（FR-42）**：新增鉴权后的原文件下载端点 `GET /api/library/media/:id/download`，对图片与视频一视同仁回传磁盘原始字节（不转码/不转换，区别于 raw 端点），`Content-Disposition: attachment`、文件名为真实 `file_name`（RFC 5987 编码兼容中文），经流式回传支持 HTTP Range 断点续传。软删项不可下载（复用 FR-25）、`smb://` 远程文件暂不支持（返回 `400`，FR-02 真机受限）。前端播放页与图片预览弹窗新增「下载原文件」入口。
- **回收站清理（FR-26）**：新增清理端点 `POST /api/library/recycle/cleanup`，把回收站内全部软删项的磁盘源文件移动到其所在盘符对应的回收站目录（取自设置键 `recycle_bin_paths` 的 JSON 映射，盘符大小写不敏感）、按删除日期分子目录 `<回收站目录>/<YYYY-MM-DD>/<文件名>`，移动成功后删除 `media_files` 记录（先移动成功、后删记录保证一致）。校验先行：存在任一软删项所在盘符（含 SMB / 无盘符）未配置回收站路径则整体拒绝（返回 `409`、不移动任何文件）。前端 `/recycle` 回收站页新增「清理回收站」按钮（二次确认），未配路径时提示去设置页配置。复用 FR-24 设置与 FR-25 软删能力，`library` 服务不依赖 `settings`（盘符→目录映射由 API 层解析后传入）。
- **增量/全量扫描 + 已删文件对账（FR-27）**：扫描区分两种模式——「增量更新」只索引新增文件，「全量扫描」遍历后对账数据库记录，库内未软删但源文件已不存在的记录标记软删进回收站（复用 FR-25，不物理删除、不动磁盘）。`POST /api/library/scan/:id` 新增可选查询参数 `mode`（`full`/`incremental`，缺省增量，向后兼容）；对账仅本地扫描启用，SMB 轮询保持增量语义、遍历整体出错时放弃对账以免误删。前端存储库管理页每个库新增「增量更新」「全量扫描」两个入口。
- **扫描任务队列 + 页眉任务展示（FR-29）**：扫描改为持久化任务队列。触发扫描（`POST /api/library/scan/:id`）改为建 `pending` 任务入队、立即返回任务 ID，由单 worker goroutine 串行执行（多次触发按入队顺序排队、不并发抢资源），worker 调用现有 `ScanLibraryWithType` 按任务的 `scan_type`（full/incremental，对接 FR-27）执行扫描。队列以新增 SQLite `scan_tasks` 表为持久化真源，服务重启时把残留 `running` 任务重置为 `pending` 重新入队。新增任务列表端点 `GET /api/library/scan/tasks`（返回任务列表与当前进行中任务，进行中任务进度由实时全局扫描状态覆盖）。前端 `AppLayout` 页眉新增扫描任务指示器：有进行中/排队任务时常驻展示数量徽标，点开展示任务列表与各自进度。
- **定时扫描（FR-28）**：新增后台定时扫描调度器 `library.ScanScheduler`，按设置 `scan_interval`（秒，FR-24 真源）周期性对所有启用媒体库经 FR-29 队列入队**增量**扫描，作为实时文件监听的兜底（捕获网络目录、丢事件、停机期间的增删）。周期 `<=0`/非法即关闭；等满一个周期才首次触发（不在启动/重启时立即扫，避免扫描风暴）；逐库跳过已有 `pending`/`running` 任务的库以防积压。设置页保存 `scan_interval` 后经 `PUT /api/settings` 的设置变更回调 `Reload` 即时重排、无需重启。纯定时组件经函数注入解耦（`-race` 单测覆盖周期触发/关闭/热重载/停止/幂等）；周期复用既有设置页字段，无前端改动。

## 0.4.0（2026-06-22）

### 新增
- **系统诊断页（FR-21）**：新增 `GET /api/system/info`（OS/架构/CPU 数/主机名/Go 版本/应用版本/ffmpeg 版本 + 复用硬件加速检测）与 `POST /api/system/codec-test`（对软件 + QSV/VAAPI/NVENC/AMF/VideoToolbox 的 H.264/H.265 候选编码器用外部 ffmpeg 跑真机试编码，报告是否编入/是否成功/失败尾部）；前端 `/system` 页展示并支持一键复制纯文本报告。编解码器实测走外部 ffmpeg CLI，独立于 CGO 检测，普通构建即可用，专供 FR-10/FR-11 真机验收。
- **跨平台打包（FR-22）**：新增根目录 `Makefile`（`frontend`/`build`/`build-hwaccel`/`package`/`clean`），一键产出单二进制发布包，构建期经 `-ldflags -X main.version` 注入版本号，并把用户自备的 ffmpeg/ffprobe 随包附带；主程序启动按「环境变量 → 可执行文件同目录捆绑版 → PATH」自动发现 ffmpeg/ffprobe，发布包开箱即用。各平台原生构建（CGO）。
- **移动端 PWA（FR-45）**：引入 `vite-plugin-pwa`，构建产物含 `manifest.webmanifest`（中文名称/品牌紫主题色/192·512·maskable 图标/`display: standalone`）与 Service Worker（`autoUpdate`），支持「添加到主屏」并以独立窗口启动、断网时离线加载应用壳；Service Worker 仅预缓存壳静态资源，`/api` 与媒体流运行时走网络不缓存。`index.html` 补 PWA meta（主题色/iOS 主屏/`viewport-fit=cover`），并加移动端安全区与触控优化。可安装/离线属真机维度，自动化覆盖 manifest 字段与 SW 注册逻辑。
- **设置子系统（FR-24）**：新增 `internal/settings` 服务（按 key 读/写、批量 upsert）与 `GET /api/settings`、`PUT /api/settings` 端点，运行期设置以 SQLite `settings` 表为唯一真源、重启后保留；前端新增 `/settings` 设置页（Mantine）与导航项，支持配置「每盘符回收站路径」「扫描周期」等键值，为后续回收站清理（FR-26）、定时扫描（FR-28）提供配置真源。设置持久化决策见 ADR-0029。
- **收藏与标签（FR-41）**：媒体支持标星收藏与自定义标签。新增收藏切换端点 `PUT /api/library/media/:id/favorite`、标签管理端点 `GET/POST /api/library/tags`、媒体打/去/列标签端点 `GET/POST /api/library/media/:id/tags` 与 `DELETE /api/library/media/:id/tags/:tag_id`；`GET /api/library/media` 新增 `favorite`、`tag_id` 筛选参数。前端时间轴媒体卡片加标星按钮，新增「仅收藏」开关、标签筛选下拉与新建标签入口。复用 foundation 已建的 `media_files.favorite` 字段及 `tags`/`tag_mappings` 表。
- **续播与观看状态（FR-44）**：持久化每媒体「上次播放位置」与「已看/未看」标记。新增观看位置上报端点 `PUT /api/play/:id/position`、标记已看端点 `PUT /api/play/:id/watched` 与继续观看列表端点 `GET /api/library/continue-watching`。前端 `VideoPlayer` 载入有进度的视频时定位续播、播放中约每 10s 上报位置、接近片尾自动标记已看；首页时间轴新增「继续观看」区块展示有进度未看完的媒体并可点击续播。记录的是用户观看位置，与 `playback` 模块的转码/缓冲进度独立。复用 foundation 已建的 `media_files.last_position`/`watched`/`last_watched_at` 字段。
- **文件名双模式编辑（FR-30）**：区分「系统内显示名」与「真实文件名」两种修改。新增显示名端点 `PUT /api/library/media/:id/display-name`（仅更新库内 `media_files.display_name`，不动磁盘文件，空串表示清除）；真实文件名修改复用既有 `PUT /api/library/media/:id/rename`（磁盘改名）。列表/卡片/详情展示名统一优先用 `display_name`、为空回退 `file_name`。前端播放页标题区新增「改显示名」「改文件名」入口，两者均经二次确认弹窗后才执行。复用 foundation 已建的 `media_files.display_name` 字段。
- **软删除与回收站（FR-25）**：删除媒体改为软删除——仅置 `media_files.deleted_at`，不物理删除数据库记录、不动磁盘源文件。常规列表与各库已索引计数排除已软删项（`ListMediaFilesFiltered`/`ListLibraryPathViews` 统一 `deleted_at IS NULL` 口径）。新增回收站列表端点 `GET /api/library/recycle` 与还原端点 `POST /api/library/media/:id/restore`；前端新增 `/recycle` 回收站页（展示软删项 + 一键还原）与导航项。复用 foundation 已建的 `media_files.deleted_at` 字段。
- **媒体时间与 EXIF 提取（FR-31）**：扫描入库时为媒体解析「媒体时间」并提取完整 EXIF。图片用 `imagemeta`（纯 Go）读取拍摄时间与相机/镜头/光圈/快门/ISO/GPS，视频用 ffprobe 读 `creation_time`；按 EXIF/拍摄时间 → 文件名日期解析（`IMG_20230101_120000` / `2023-01-01` / `Screenshot_2023...` 等）→ 文件创建时间 → 修改时间的降级链定出 `media_time` 与来源 `media_time_source`，写入 foundation 已建字段。`GET /api/library/media` 新增 `sort=media_time`/`media_time_asc` 排序（按 `COALESCE(media_time, added_at)`），时间轴按媒体时间组织。新增纯 Go 依赖 `github.com/evanoberholster/imagemeta`。
- **HEIC/RAW 图片支持（FR-37）**：识别常见相机 RAW 扩展名（cr2/nef/arw/dng/rw2/raf/orf/srw/pef）并归入图片类。HEIC/RAW 浏览器无法直接渲染，服务端经外部 ImageMagick（`magick`）转成 JPEG：`GET /api/library/media/:id/raw` 对 HEIC/RAW 返回转换后的 JPEG（普通图片仍直出原图），缩略图也改经 magick 生成；转换结果缓存于数据目录下 `image_cache/`（按「源路径 + 源修改时间」hash 命名，二次命中不重转）。`magick` 路径按「`JIANVIDEO_MAGICK_PATH` → 可执行文件同目录捆绑版 → PATH」解析（与 ffmpeg 一致）；未安装时 `raw` 端点返回 `503`、转换失败返回 `500`，均记中文日志、不影响其他功能。外部 ImageMagick 决策见 ADR-0030。真实 HEIC/RAW 显示需目标机安装带 HEIC + RAW delegate 的 ImageMagick（真机维度）。

### 变更
- **用户相册/合集（FR-40）**：新增相册服务与 `GET/POST /api/albums`、`DELETE /api/albums/:id`、`GET/POST /api/albums/:id/items`、`DELETE /api/albums/:id/items/:mediaId` 端点，支持跨目录把任意媒体手动归入相册、浏览相册成员、移出成员；删除相册仅清理 `albums` 与 `album_items`，不删除源文件与 `media_files` 记录。前端新增 `/albums` 相册页（列相册、建相册、删相册、看相册内容、从媒体库加入/移出媒体）与导航入口。
- **文档对齐**：修正 `README.md`、`docs/ARCHITECTURE.md` 中「CGO 绑定 ffmpeg-go 直接调用 libav C API」的过时表述——转码实为外部 ffmpeg 进程调用，CGO 仅用于 SQLite 与可选硬件编码器检测。
- **存储库管理页精简（FR-23）**：`/library-manager` 移除页内媒体文件列表，仅保留存储库卡片（扫描进度 + 已索引媒体数量）；点击卡片携 `library_id` 与起始路径跳转 `/browse` 定位到该库根目录。`GET /api/library/paths` 响应每项新增 `media_count`（该库未软删媒体数量，按 `library_id` 一次 `GROUP BY` 统计，向后兼容）。

### 修复
- **软删项收口访问隔离（FR-25）**：修复已软删（回收站中）的媒体仍可经详情/播放/raw/缩略图等端点访问的隔离差距——此前 `GetMediaFileByID` 不区分软删状态，软删后 `GET /api/library/media/:id`、播放流、`/raw`、缩略图等仍能取到记录。现 `GetMediaFileByID` 增加 `deleted_at IS NULL` 过滤，软删项经上述正常访问路径一律视为不存在（404），仅经回收站列表（`GET /api/library/recycle`）可见；还原（`POST /api/library/media/:id/restore`）走自身查询读取软删记录，不受影响。
- **亮色模式残留深色改用主题感知变量**：修复切换到亮色模式后页面背景、视频播放器控制条、缩略图占位、时间线等位置仍显深色的问题——此前 `index.css` 强制 `color-scheme: dark` 且 body 背景写死 `--mantine-color-dark-8`，播放器控制条与若干组件写死 `var(--mantine-color-dark-N)` 深色兜底，不随主题切换。现 `color-scheme` 改为 `light dark`、body 背景与各表面/前景改用随主题切换的 Mantine 语义变量（`--mantine-color-body`/`--mantine-color-default`/`--mantine-color-default-border`/`--mantine-color-dimmed`），暗色模式保持不变。亮色实际视觉效果属真机/浏览器维度。
- **捕获 ffmpeg stderr 补全缩略图/HLS 失败日志**：修复缩略图与 HLS 预切片失败时日志不足、难以排查的问题——此前图片/视频缩略图 ffmpeg 命令用 `cmd.Run()` 不捕获 stderr，失败仅记一句 `exit status`（无具体原因）；HLS 预切片的分辨率探测丢弃 ffmpeg 输出、m3u8 校验错误不含输出目录、切片/校验失败无统一日志，与成功日志不对称。现缩略图 ffmpeg 命令捕获 stderr，失败按 ERROR 级别记录「文件路径 + stderr 关键尾部（截断 500 字符）」；HLS 探测失败保留 ffmpeg 输出尾部、m3u8 校验错误补全输出目录、切片/校验/保存失败统一记 ERROR 级日志含 mediaID/输出目录/档位上下文，成功失败对称、不再静默吞错。
- **缩略图生成限并发并加 ffmpeg 超时**：修复扫描大目录时缩略图生成卡死整个页面的问题——此前每个文件直接 `go` 启动 ffmpeg/magick，无并发上限、ffmpeg 无超时，扫描大库会瞬间炸出成百上千并发进程导致资源耗尽。改为所有缩略图任务统一经固定容量信号量限并发（`min(4, CPU 核数)`），并为 ffmpeg 图片/视频缩略图命令加 30s 超时（`exec.CommandContext`，超时即终止并记中文 WARN 日志，不挂起）。保持异步生成、不阻塞扫描。
- **扫描期媒体富化改有界并发，提速大库扫描（FR-31）**：修复 FR-31 媒体时间/EXIF 提取在扫描入库时同步逐文件调用 ffprobe、大库扫描串行过慢的问题。扫描对去重后的新文件列表改用固定容量信号量（`min(4, CPU 核数)`，与缩略图一致）有界并发处理，单文件内富化仍同步（`media_time` 入库即写），总耗时约降为 1/cap，又不致瞬间炸出大量 ffprobe 子进程；入库计数用原子操作、写入经 SQLite WAL 串行、进度状态互斥更新，并发安全（`go test -race` 无竞争）。

### 安全
- **全 `/api` 路由强制鉴权（FR-13）**：修复未授权访问漏洞——此前仅 `/api/me` 挂了认证中间件，库、播放、HLS 切片、stream、raw、缩略图、设置、相册、SMB 凭据、系统诊断、转码等路由均注册在裸引擎上，未登录即可访问与修改全部数据和媒体。新增全局 `auth.APIGuard` 中间件，对路径前缀为 `/api/` 但不属于 `/api/auth/` 的请求强制校验 JWT（Cookie `auth_token` 或 `Authorization: Bearer` 任一有效即放行，均无效返回 `401`）；`/api/auth/login`、`/api/auth/logout`、`/health`、静态资源 `/assets/*` 与 SPA 回退保持豁免，保证未登录也能加载前端壳与登录。
- **SMB 主密码强制显式配置（FR-02）**：`smb.MasterPassword()` 不再在 `SMB_MASTER_PASSWORD` 未设置时回退公开弱默认值，改为返回错误；显式空串同样视为未配置。未配置时全部 Save/Load 调用点拒绝以弱密钥加解密——`POST /api/smb/credentials` 返回 `503`，添加 SMB 库路径仅记录 ERROR 不落弱密钥，SMB 扫描/播放返回明确错误。消除「拿到 `smb_credentials.enc` 即可用公开常量离线解出明文密码」的风险。同步更新 `docs/API.md`、`docs/OPERATIONS.md`。

## 0.3.0（2026-06-21）

### 修复
- **SMB 凭据（FR-02）主密码一致性与密码持久化**：修复保存时用 `SMB_MASTER_PASSWORD`/默认主密码、加载时却用空串，导致加密与解密主密码不一致、凭据永远无法读回的缺陷；同时修复 `Credentials.Password` 因 `json:"-"` 未写入加密文件、SMB 连接始终使用空密码的问题。新增 `smb.MasterPassword()` 作为加解密主密码唯一来源，统一全部 Save/Load 调用点（`saveSMBConfig`/`SaveSMBCredentials`/`library`/`playback`）。

### 变更
- **ABR 自适应码率（FR-07）真机验收交付**：多码率 master.m3u8 + hls.js ABR 链路（代码自 v0.1.0 已落地、未变更）经真机验证（`blob:` MSE 播放、多码率清单生效），状态由开发中转为已交付。
- **双进度条（FR-20）真机验收交付**：VideoPlayer 缓冲进度 + 播放进度双滑块（代码自 v0.2.0 已落地、未变更）经真机验证，状态由开发中转为已交付。
- **接口契约（FR-02）**：`POST /api/smb/credentials` 请求体移除 `master_password` 字段，加解密主密码改由服务端 `SMB_MASTER_PASSWORD` 环境变量统一管理；同步更新 `docs/API.md`、`docs/OPERATIONS.md`。

## 0.2.0（2026-06-21）

### 新增
- **播放器（FR-18）末端缓冲等待**：VideoPlayer 监听 mpegts.js `error` 事件 + 原生 `<video>` `waiting`/`stalled` 事件，进入等待态展示「等待新数据…」横幅；mpegts.js 报错后 1s 自动 `unload + load` 重载，新数据可用时 `canplay`/`playing` 自动复位。

### 修复
- **转码（FR-10/11/12）**：挂载硬件加速能力查询端点 `GET /api/transcode/hwaccel`。此前 HWAccelHandler 已实现但未注册路由，请求被前端 SPA 回退（NoRoute）接管返回 HTML 而非硬件能力 JSON，与 ARCHITECTURE §1/§4 不符；现已注册并返回 available/preferred/h264_supported 等字段。
- **监听器（FR-03）**：修复 main.go 未启动文件监听——watcher 包齐备且单测全绿，但程序入口从未实例化或 Start，导致新增/删除文件不自动入库（真机 35s 不入库）。修复后实测加文件 1s 入库（spec ≤30s）、删文件 1s 移除（spec ≤10s）。

### 变更
- **文档对齐**（期验收 P1 发现）：PRD AC-13 由 `/timeline` 改为根路由 `/`（FR-A 已重排）；FR-10/11 状态注「检测/单测就绪；本机无 Intel/NVIDIA，硬件真机待验」；`docs/specs/smart-playback.md`（FR-05）改为前端探测 `/api/play/hls/:id/master` 不可用即降级 `/api/play/:id/stream`，不再单提 `playinfo` 端点；`docs/specs/mpegts-player.md`（FR-16）澄清 mpegts.js 仅约束 TS/HLS 转码流，直出 MP4 经 `/stream` 允许原生 `<video>`。

## 0.1.0（2026-06-20）

### 新增
- 外挂字幕支持（FR-04）：后端 SRT/ASS→WebVTT 转换 API、前端字幕 overlay 渲染与轨道选择
- 实现 SMB/CIFS 网络共享支持（FR-02）：原生 SMB 连接管理、凭据 AES-GCM 加密存储、SMB 目录扫描与视频播放流
- **ABR 自适应码率（FR-07）**：后端 MultiPipeline 单进程多输出 1080p/720p/480p 三档 HLS 切片，码率阶梯根据源分辨率自动裁剪；新增 master.m3u8 生成与路由；前端 VideoPlayer 支持 hls.js ABR 模式（动态 import），自动回退 mpegts.js
- 初始化项目结构与 SDD 治理文档
- 实现 fsnotify 实时路径监控（FR-03）：递归监听媒体库目录，视频文件自动入库/移除，500ms 去抖机制
- 前端全面重构：引入 Mantine UI v9 暗色主题，替换手写 Tailwind 原子类
- 完善登录页（FR-13）：Mantine 表单 + zustand 认证状态管理 + 路由守卫
- 完善媒体库管理页（FR-01/FR-14）：路径增删、扫描、媒体列表/搜索/分页
- 新增文件目录浏览 API（FR-15）：`GET /api/library/browse` 按目录层级浏览媒体文件，支持面包屑导航、子目录列表、媒体文件列表，前端 Tab 切换（时间轴 | 文件目录）
- 完善视频播放页（FR-16）：VideoPlayer 接入 HLS 流
- 路由改造：修复 catch-all 通配符冲突，统一 BrowserRouter 模式
- 后端异步扫描 + SSE 进度推送（FR-C）：扫描改为后台 goroutine 异步执行，新增 `GET /api/library/scan/progress` SSE 端点实时推送已扫描数/总数/状态，前端展示扫描进度条并在完成后自动刷新
- 缩略图系统（FR-D）：后端扫描时通过 ffmpeg 异步生成 320px 缩略图（视频取第 2 秒帧、图片缩放），新增 `GET /api/library/thumbnail/:id` 端点，前端媒体卡片改用缩略图加载（图片预览弹窗仍用原图）
- 页面流程重设计（FR-A）：原综合媒体库页拆分为存储库管理 `/library-manager`、时间轴 `/`、目录浏览 `/browse` 三个独立页面，AppLayout 导航在三者间切换；管理页支持媒体文件删除与重命名（新增 `PUT /api/library/media/:id/rename` 磁盘改名端点）
- 时间轴视图重做（FR-B）：按 `added_at` 日期分组，左侧竖向日期轴 + 右侧缩略图网格，视频与图片均展示缩略图
- 虚拟列表 + 懒加载（FR-E）：时间轴与目录浏览改用 `@tanstack/react-virtual` 窗口虚拟滚动，只渲染可见区 + overscan；时间轴改为滚动到底自动加载更多（替代分页），缩略图 `loading="lazy"`
- 暗色模式 + 路由守卫 + 全局错误处理（FR-G）：MantineProvider 接入 `localStorageColorSchemeManager` 持久化主题，顶栏新增明暗切换按钮；新增 ProtectedRoute/RequireAnon 路由守卫（未认证跳登录、已认证访问登录页跳首页）；新增 `handleApiError` 工具，Axios 拦截器对网络错误统一 toast

### 变更
- 前端代码结构拆分（FR-F）：LibraryPage 拆分为 LibraryPathManager / MediaTimeline / DirectoryBrowser 等子组件与 useLibraryPaths / useMediaList / useDirectoryBrowse hooks，VideoPlayer 改用 Tabler Icons 与 Mantine 样式，删除 App.css 模板代码
- 扫描接口改为异步（FR-C）：`POST /api/library/scan/:id` 不再等待扫描完成，立即返回 `{"status":"scanning"}`，实际进度经扫描进度 SSE 端点获取

### 修复
- **时间轴（FR-E）**：修复无限滚动失效——滚到底不再加载更多、永远停在首页 60 条。改用 IntersectionObserver 底部哨兵触发加载（替代依赖虚拟化 lastIndex 变化的脆弱判定），并在 useInfiniteMedia 用同步标记防止重复拉取。
- **扫描进度（FR-C）**：修复扫描完成后 SSE 重连风暴——浏览器 EventSource 每 ~3 秒重连并重复触发列表重拉。后端 SSE 改为保持连接打开、仅在状态变化时推送；前端按 `completed_at` 变化判定「新完成」才刷新，避免重复刷新且兼容快扫描。
- **媒体库**：修复本地扫描只读取第一层目录的问题，改为递归扫描并按目录类型分发 local/SMB。
- **媒体库**：统一图片/视频后缀识别策略，新增按 `LibraryPath` 绑定的自定义后缀，删除目录时同步清理，避免污染全局。
- **媒体库**：新增图片 raw 预览接口，前端时间轴与目录视图点击图片打开预览，视频仍跳转播放页。
- **前端**：补齐独立 `/timeline` 路由和侧边栏/移动抽屉入口，时间轴默认显式请求 `sort=time_desc`。
- **前端**：修复 Windows 路径下目录浏览面包屑出现 `/D:` 的问题，并为扫描提供可感知加载态。
- **安全**：移除硬编码默认 JWT 密钥，改为启动时生成随机密钥并提示用户设置 `JWT_SECRET` 环境变量
- **安全**：HLS 切片路由增加路径遍历防护（过滤 `..` 和 `/`）
- **安全**：Cookie 添加 `SameSite=Strict` 属性，防止 CSRF 攻击
- **安全**：Content-Disposition 文件名使用 `url.PathEscape` 转义，防止 HTTP 头注入
- **并发**：修复 `GetProgress` 中 `BufferedRanges` 的锁外反序列化数据竞争
- **并发**：客户端断开后流式传输 goroutine 主动 cancel context，防止 goroutine 泄露
- **并发**：HLS m3u8 writer 确保 Close() 总是被调用并写入 `EXT-X-ENDLIST`
- **并发**：播放会话 map 增加 30 分钟 TTL 清理机制，防止内存泄露
- **CGO**：修复 `findEncoderByName`/`findQSVEncoder` 违反 CGO 指针规则的问题（改为返回 `bool`）
- **基础设施**：硬件检测失败时降级继续而非阻断整个流程
- **基础设施**：`detectOnce` 增加 `recover()` 防止 panic 后返回 nil
- **基础设施**：`TranscodeSession.Start()` 增加重复启动检查
- **基础设施**：移除冗余的 `killProcessGroup` 调用（`exec.CommandContext` 已自动处理）
- **基础设施**：`isIntelGPU()` 添加 `//go:build linux` 平台构建标签
- **基础设施**：`setupTestDB` 从业务代码迁移到测试工具文件
- **性能**：`ScanLibrary` N+1 查询改为批量查询 + map 查找
- **性能**：流式传输从逐包 Flush 改为 `io.CopyBuffer`
- **性能**：HLS `GetM3U8`/`GetSegment` 锁内不再执行磁盘 I/O
- **健壮性**：ffprobe 增加 10 秒超时，防止永久阻塞
- **健壮性**：`envInt` 无效值增加警告日志
- **健壮性**：搜索参数中 `%` 和 `_` 通配符转义
- **健壮性**：DB 插入失败增加 WARN 日志
- **测试**：`play_handler_test.go` 宽松断言改为精确状态码断言
- **测试**：`handler_test.go`、`jwt_test.go`、`library/service_test.go` 补充错误路径和边界条件测试
- **测试**：`watcher_test.go` 增加超时时间减少 flaky 风险
- **前端**：Mock 数据统一为单一数据源（`mocks/data.ts`）
- **前端**：VideoPlayer 事件监听器在 `destroyPlayer` 中清理
- **前端**：`LibraryPage` 工具函数提取为模块级
- **前端**：auth store 移除硬编码 `'cookie_auth'` 字面量
- **前端**：mock 模式从运行时 `localStorage` 判断改为构建时 `VITE_USE_MOCK` 环境变量
- **规则**：`architecture-invariants.md` ORM 规则修正为允许 GORM（ADR-0023）
- **规则**：`architecture-invariants.md` 删除 Bukkit/Spigot 残留内容

### 移除
（无）

### 修复（二期 FR 审查修复）
- **CRITICAL**：SMB 流式播放中 `smbReadSeeker.Close()` 不再调用 `client.Disconnect()`，避免 HTTP 分块传输中途断开 SMB 会话
- **CRITICAL**：`GetProgress` 的 `exists` 检查移入 `RLock` 内，消除 nil 指针解引用风险
- **HIGH**：`openSMBFile` 加载凭据后检查 `creds == nil`，给出明确错误提示而非 panic
- **HIGH**：`saveSMBConfig` 主密码改为从 `SMB_MASTER_PASSWORD` 环境变量读取
- **HIGH**：`router.go` 所有播放路由添加 `parseMediaID` 错误处理，与 HLS 路由一致
- **HIGH**：`MultiPipeline.RunMulti` 添加 `dsts` 参数被忽略的 WARN 日志
- **HIGH**：`Credentials` 增加 `Domain` 字段，支持企业 Windows AD 环境
- **MEDIUM**：`Service.smbCreds` 添加 `smbCredsMu` 读写锁保护并发安全
- **MEDIUM**：`Watcher.pathToLib` 读取加 `RLock` 保护
- **MEDIUM**：`HLSSegmentWriter.Close` 添加 `closed` 标志防止重复关闭
- **MEDIUM**：`WriteSegment` 移除每次 `Sync()`，改为 `Close()` 时一次性 sync
- **MEDIUM**：`BrowseDirectory` 入口添加 `filepath.Clean` + `..` 路径遍历校验
- **MEDIUM**：`smbfs.normalize` 添加 `..` 过滤
- **MEDIUM**：`smb.Client.EnsureConnected` 使用 `sync.Once` 消除竞态窗口
- **MEDIUM**：`GetSubtitles` 对 SMB 路径返回空列表
- **MEDIUM**：`GetSubtitleContent` 空内容返回 204
- **MEDIUM**：`LibraryPage` catch 块不再静默吞错，添加错误状态和 UI 提示
- **MEDIUM**：`LibraryPage.activeTab` 同步到 URL query 参数
- **MEDIUM**：`LibraryPage` paths 变化时使用 `useRef` 追踪初始化，不重置浏览状态
- **MEDIUM**：`SubtitleEntry` 接口统一到 `types/index.ts`，消除重复定义
- **MEDIUM**：`VideoPlayer` 自动播放被阻止时显示"点击播放"提示
- **MEDIUM**：`VideoPlayer.hlsRef` 类型从 `unknown` 改为 `Hls`
- **LOW**：`master_test.go` 使用 `strconv.Itoa` 替代自定义 `itoa`
- **LOW**：`credentials.go` 添加 `saltLen`/`nonceLen` 常量
- **LOW**：`Watcher.Stop` 先关闭 watcher 再关闭 done 通道
- **LOW**：`Credentials.Password` 添加 `json:"-"` 标签
- **LOW**：新增 `credentials_test.go` 覆盖加解密 roundtrip、错误密码、空输入

> 发版时把"未发布版本"段切成 `## [X.Y.Z] - YYYY-MM-DD`，再新建空的"未发布版本"段。
