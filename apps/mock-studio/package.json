{
  "name": "@jianvideo/mock-studio",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "coverage": "vitest run --coverage",
    "dev": "vite --host 127.0.0.1",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@jianvideo/benchmark": "workspace:*",
    "@jianvideo/mock": "workspace:*",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@jianvideo/eslint-config": "workspace:*",
    "@jianvideo/typescript-config": "workspace:*"
  }
}
